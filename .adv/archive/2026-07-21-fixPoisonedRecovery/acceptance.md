# Acceptance

Reviewed at: 2026-07-21T15:24:43.698Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | Poisoned-history recovery across the catch-gated tools (verification-evidence, task, design-concern, contract) reaches the disk projection deterministically without depending on the workflow signal throwing — matching gate.ts's existing probe-first pattern. | pass | Probe-first pattern applied to all 6 catch-gated tools (verification-evidence, task, design-concern, contract, gate, archive) + #253 archive path. shouldTakeRecoveryBranch gates before fireSignalAndRefresh. AC5 sweep 414/414 pass (tr_mruqwco7_8968572d). |
| SC2 | success_criterion | A recorded verification-evidence disposition actually clears its `VERIFICATION_EVIDENCE_MISSING` acceptance/release blocker, including under `target_path` routing. | pass | tk-288ea2eded66: completeGateViaRecovery reads durable disk projection for readiness evaluation; disposition clears VERIFICATION_EVIDENCE_MISSING blocker despite stale Temporal state. Regression test in gate.test.ts asserts. |
| SC3 | success_criterion | Recovery records audited divergence and returns a clear recommended next action (archive / status-repair) without auto-terminalizing. | pass | All recovery writes emit reconciliationWarning (RECOVERY_RECONCILIATION_WARNING); recovery_audit captures reason/evidence/recovered_at on gate/disposition/subagent writes. Note: saveRecoveredChangeStatus audit persistence gap flagged for harden (pre-existing). |
| SC4 | success_criterion | **`adv_change_archive recoveryMode:"poisoned_history"` with precise `WorkflowNotFoundError` evidence writes the archive bundle from the disk projection instead of failing on the pre-bundle Temporal read, restoring the documented archive→status_repair recovery path (#253).** | pass | tk-5c07a5407439: archive handler uses shouldTakeRecoveryBranch + loadChange (disk projection); 37/37 change.archive-phase9.test.ts pass; bundle creation reached without Failed to query Workflow when precise WorkflowNotFoundError evidence supplied. |
| AC1 | acceptance_criterion | With `recoveryMode:"poisoned_history"` + precise `recoveryEvidence`, `adv_verification_evidence_disposition`, `adv_task_update`/`adv_task_add`, `adv_design_concern_disposition`, and `adv_contract_mint`/`adv_contract_review_matrix_set` probe the workflow BEFORE firing the signal and take the disk-direct recovery branch without relying on a signal throw. | pass | Per-tool AC5 regression tests (6 tools): verification-evidence.test.ts:240, task.test.ts:771/1615/1830, design-concern.test.ts:276, contract.test.ts:799/829, gate.test.ts:544/626, change.archive-phase9.test.ts:1488. All mock fireSignalAndRefresh to resolve silently and assert probe-first writer fires. |
| AC2 | acceptance_criterion | Poisoned classification of a RUNNING execution requires precise `recoveryEvidence` (`isPreciseWorkflowRecoveryEvidence`) or an authoritative `WorkflowTaskFailed` history event; `describe()` metadata alone never authorizes recovery. | pass | 3 reviewer-flagged C2 sites fixed (change.ts:3657, gate.ts:1295, worktree/state.ts:285); describe() no longer sole authority. C2 regression tests: change.test.ts:4448/4497, gate.test.ts:1857/1902, state-session-lifecycle.test.ts:408. Note: 5+1 additional describe-only sites deferred as follow-up per user scope decision (task.ts L1090/1485/1765, contract.ts L441/678, change.ts:4741 workflow_terminate). |
| AC3 | acceptance_criterion | Under `target_path` routing (untrusted target + explicit confirmation), the recovery write lands on the target disk projection, and a subsequent readiness check no longer reports the dispositioned gap. | pass | tk-0e8aac2b67dd: target_path routing audit confirmed all 12+ probe-first sites route through target-resolved activeStore. Regression test in verification-evidence.test.ts. |
| AC4 | acceptance_criterion | Recovery is idempotent: re-running returns the same result with no duplicate projection writes. | pass | Idempotency tests: change.archive-phase9.test.ts:726 (re-run after interrupted terminal projection), :921 (re-running after PR-merged pending_merge recovery); change.test.ts:5081. Disk-direct writers use content-hash dedup. |
| AC5 | acceptance_criterion | A regression test reproduces the "fire-and-forget signal resolves without throwing against a poisoned workflow" case and asserts the recovery branch is reached without relying on a signal throw, for each fixed tool. | pass | AC5 sweep tr_mruqwco7_8968572d: 414/414 pass across 11 recovery-touching files including C2 regression tests. |
| AC6 | acceptance_criterion | Live proof: pokeedge-web `fixWorktreeViteCache` acceptance is healed by the fixed recovery path. | pass | User-approved structural+deploy interpretation. Deployed bundle SHA 574009ed from trunk HEAD 0882abb4 (PR #275). pokeedge-web temporal_health_ok:true. fixWorktreeViteCache archived (mergeCommitSha=03b0c249). Reviewer READY: report fixPoisonedRecovery|tk-0527666d76fa|adv-reviewer|3. |
| AC7 | acceptance_criterion | **NEW (#253): With a genuinely absent workflow and `adv_change_archive changeId:X phase9:"run" recoveryMode:"poisoned_history" recoveryEvidence:"...WorkflowNotFoundError..."`, the archive does not throw `Failed to query Workflow` on the pre-bundle read; it proceeds to write the archive bundle from the disk projection, after which `adv_change_status_repair` is eligible. A regression test reproduces the absent-workflow archive case and asserts bundle creation is reached without a hard Temporal read failure. ** | pass | tk-5c07a5407439: 2 regression tests in change.archive-phase9.test.ts (37/37 pass) asserting archive reaches probe-first bundle creation on poisoned_history + WorkflowNotFoundError without throwing Failed to query Workflow. |
| C1 | constraint | Do NOT weaken the recovery evidence gate: `recoveryEvidence` must remain precise poisoned/completed evidence (`isPreciseWorkflowRecoveryEvidence`); all audit fields (recoveryReason, compatibilityReason, priorApprovalEvidence for acceptance) remain required. | respected | isPreciseWorkflowRecoveryEvidence remains the authority predicate; all audit fields (recoveryReason, compatibilityReason, priorApprovalEvidence) remain required at gate.ts L708-742, change.ts L2119-2125. No gates weakened. |
| C2 | constraint | `describe()` is used only for run identity + open/closed detection; never as sole poison authority. | respected | 3 reviewer-flagged sites fixed: describe() not consulted in change.ts archive recovery catch, gate.ts status query catch, worktree/state.ts classifier. 5+1 additional sites documented as follow-up per user-approved 3-site scope (task.ts L1090/1485/1765, contract.ts L441/678, change.ts:4741). |
| C3 | constraint | Disk-projection recovery is break-glass: it MUST be idempotent, audited (recovery_audit), and record the deliberate divergence. | respected | Recovery writes are idempotent (content-hash dedup) and record divergence via reconciliationWarning. recovery_audit persisted on gate/disposition/subagent writes. Note: saveRecoveredChangeStatus audit-persistence gap flagged for harden (pre-existing writer, not modified by this change). |
| C4 | constraint | Execution is sequenced AFTER `fixTemporalQueryRecovery` lands; build on its refactored classification layer (do not fork/duplicate `classifyCompletedOrPoisonedRecovery`). **(Dependency satisfied: fixTemporalQueryRecovery is archived on trunk as of 2026-07-19.)** | respected | classifyCompletedOrPoisonedRecovery consumed (not forked); isPreciseWorkflowRecoveryEvidence reused from ../temporal/recovery-classification. fixTemporalQueryRecovery archived on trunk 2026-07-19. |
| C5 | constraint | **The archive-path fix (#253) must reuse the same classification + probe-first helpers as the sibling tools; do not add an archive-only bypass that skips the evidence gate.** | respected | Archive path (#253) reuses shouldTakeRecoveryBranch + isPreciseWorkflowRecoveryEvidence — same helpers as sibling tools. No archive-only bypass. |
| C6 | constraint | Preserve and reuse gate.ts's existing probe-first recovery pattern; apply it to the siblings and the archive path rather than diverging from it. | respected | shouldTakeRecoveryBranch applied uniformly across 6 catch-gated tools + archive path, mirroring gate.ts probe-first semantics. |
| DONT1 | avoidance | No auto-terminate / reset / reseed of healthy or merely UNKNOWN-state workflows — poisoned classification stays witness-first and evidence-driven. | respected | No auto-terminate/reset/reseed. shouldTakeRecoveryBranch requires operator-supplied precise evidence (opt-in); classification stays witness-first. |
| DONT2 | avoidance | No auto-terminalization of diverged changes: recovery records divergence and surfaces a recommended next step only. | respected | Recovery records divergence (reconciliationWarning) and surfaces recommended next step; no auto-terminalization. |
| DONT3 | avoidance | Prevention (Temporal Worker Versioning / deploy-safe worker rollover) is OUT OF SCOPE — tracked as P2 (backlog bl-F5_tYP9R). Temporal Reset tooling is OUT OF SCOPE. | respected | No Temporal Worker Versioning or Reset tooling added. Prevention tracked as P2 backlog bl-F5_tYP9R. |
| DONT4 | avoidance | Do NOT re-do the Temporal query/signal classification refactor owned by `fixTemporalQueryRecovery`; consume it. | respected | classifyCompletedOrPoisonedRecovery consumed unchanged from fixTemporalQueryRecovery; no re-implementation. |
| DONT5 | avoidance | **The live-but-reachable wedged-workflow facet of #198 (a RUNNING execution stuck on an unsatisfiable readiness blocker, not poisoned) is OUT OF SCOPE here: it is served by the already-shipped `adv_change_workflow_terminate` tool (terminate the exact wedged run) followed by disk-projection recovery / status_repair. This change stays witness-first poisoned-only; it does not add live-workflow reset semantics.** | respected | #198 live-wedged workflow served by existing adv_change_workflow_terminate (not modified to add reset semantics). This change stays poisoned-only. |

