# Acceptance

Reviewed at: 2026-08-04T20:47:49.788Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | The ADR artifact documents each decision with rationale, alternatives considered, and consequences | pass | adv-reviewer READY (attempt 3): Decisions 1 and 3 each retain substantive rationale, alternatives considered, and consequences; ADR-001 and ADR-003 present with context/decision/consequences. |
| SC2 | success_criterion | Each affirmative decision has a clear path to a downstream implementation change | pass | adv-reviewer READY: Implementation Strategy sequences D3 then D1, both independently shippable with no dependency outside this change; each decision names its affected components and levers. |
| SC3 | success_criterion | The decisions collectively address the regression-spiral root cause, not just symptoms | pass | adv-reviewer READY: LBP Analysis establishes both decisions move ADV from 'Temporal is the system of record' to 'disk projection is the system of record; Temporal is the write-ahead log' — the shared root cause, not per-symptom patches. |
| AC1 | acceptance_criterion | Given the evidence of ADV becoming non-operational during Temporal outages (2026-08-02, 2026-08-04), when Decision 1 is resolved, then the decision establishes a read-only out-of-band recovery surface, with limited write deferred to an explicit follow-up. | pass | adv-reviewer READY: Decision 1 verdict is AFFIRMATIVE for a read-only out-of-band recovery surface, enumerates covered read tools, and defers limited write to an explicit follow-up recorded in agreement.md Deferred Questions and executive-summary.md. |
| AC2 | acceptance_criterion | Given the evidence of `adv_doctor` reporting healthy while `adv_status view:"health"` times out (2026-08-04), when Decision 3 is resolved, then the decision requires health probes to exercise the critical read path. | pass | adv-reviewer READY: Decision 3 requires a bounded adv_status view:summary probe routed through addNonHealthyFinding (doctor.ts:358), superseding the optimistic healthy default when the critical read path fails. |
| AC3 | acceptance_criterion | Evidence: ADR foundation-change provenance verified by review of archived change records (`makeReadsDiskAuthoritative`, `gateMutationSuccessDisk`, `makeToolReadsWorkerFree`, `fixMultiSessionTemporalRead`). | pass | adv-reviewer independently resolved archive commits 2ac4ceeb (makeReadsDiskAuthoritative), f0277b29 (gateMutationSuccessDisk), 5a2e0d94 (makeToolReadsWorkerFree), b0294359 (fixMultiSessionTemporalRead), plus implementation commit 1879fb64, via git show -s; archive commits contain .adv/archive bundles. |
| C1 | constraint | Must not introduce a second parallel write surface (bypass is read-only initially; limited write is follow-up) | respected | adv-reviewer READY: Decision 1 is read-only; mutations return typed RECOVERY_MODE_READ_ONLY rather than writing. No second parallel write surface is introduced; limited write is explicitly deferred. |
| C2 | constraint | Must preserve Temporal workflow replay safety for any workflow code changes | respected | adv-reviewer READY: this change contains no workflow code changes at all, so replay safety is trivially preserved. The persistence-boundary invariant additionally records that workflow code may not perform filesystem or network I/O. |
| C3 | constraint | The ADR must be approved at the design gate before implementation begins | respected | Design gate completed 2026-08-04T20:21:37Z with user-approved scope reduction before any implementation. No implementation exists: git status clean, trunk..change/resolveAdvPersistenceRecovery empty. |
| DONT1 | avoidance | Do not implement in this change — it is decision-only | respected | adv-reviewer verified git status clean and git log trunk..change/resolveAdvPersistenceRecovery empty. adv_task_checkpoint returned status:clean at 5dfcc43 — nothing to commit. |
| DONT2 | avoidance | Do not defer the decisions by creating more per-symptom tickets | respected | adv-reviewer explicitly assessed the Decision 2 split and concluded: moving one independently invalidated architectural decision to a dedicated decision change is a scope correction, not per-symptom deferral. Both retained decisions reached AFFIRMATIVE verdicts rather than being deferred. |
| DONT3 | avoidance | Do not conflate "no file-backed runtime" (legacy prevention) with "no out-of-band read" (recovery prevention) | respected | adv-reviewer READY: Decision 1 explicitly distinguishes the AGENTS.md rule's original intent (blocking legacy SQLite/file-backed mutation paths) from the read-only recovery scope it supersedes, and states no mutation path may bypass Temporal. |

