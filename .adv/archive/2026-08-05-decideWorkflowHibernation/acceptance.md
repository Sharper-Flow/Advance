# Acceptance

Reviewed at: 2026-08-05T04:36:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | The ADR records a decision for every open question with rationale, alternatives, and consequences, so a further review round does not re-litigate resolved ground. | pass | adv-reviewer attempt 2 (design v4, amended contract) verified ADR-002 and DEC-1..DEC-5 each carry rationale, alternatives and consequences. Attempt 1 had found DEC-5 lacked alternatives; restored in v4 with four (mitigation-only framing, defer entirely, fail loudly instead of logging, ship hibernation first). |
| SC2 | success_criterion | The hibernation verdict is justified on architectural-shape grounds against Temporal's documented entity-workflow guidance, with cost treated as context rather than as the deciding factor. | pass | Verdict rests on five architectural arguments (P40 correctly applied; epics have no terminal path; retention reclaims only closed workflows; Temporal's deletion-signal guidance matches DEC-2; both preconditions solved). Cost is explicitly stated as not load-bearing. Attempt 3 confirmed the struck entity-workflow claim does not reappear doing justificatory work anywhere, including the design tail. |
| SC3 | success_criterion | Every assumption that discovery or later review overturned is recorded with its evidence anchor. | pass | Nine overturned assumptions recorded, each with an evidence anchor. Assumptions 4, 6 and 9 were introduced by this change's own drafts rather than the proposal, which is why SC3 was broadened at discovery re-entry to 'discovery or later review'. |
| AC1 | acceptance_criterion | Given the proposal asserts no running-worker identity report exists and its cost is unestimated, when the ADR is written, then it records that assertion as refuted and names bundle-generation parity (`plugin/src/temporal/worker-bundle-manifest.ts:29`, `plugin/src/temporal/worker-heartbeat.ts:99-117`, `plugin/src/temporal/worker-roll.ts:133-216`) as the resolved mechanism. | pass | ADR records the 'no running-worker identity report exists' assertion as refuted (Overturned Assumption 1) and names bundle-generation parity with all three required anchors: worker-bundle-manifest.ts:29, worker-heartbeat.ts:99-117, worker-roll.ts:133-216. Attempt 1 independently re-verified those citations against source. |
| AC2 | acceptance_criterion | Given the hibernation and terminal race was unresolved, when the ADR is written, then it records self-completion by signal as the resolution and records external cancel or terminate as unusable because RequestCancelWorkflowExecution returns OK against an already-completed workflow. | pass | DEC-2 records self-completion by signal as the resolution, and records external cancel as unusable because RequestCancelWorkflowExecution returns OK against an already-completed workflow (temporalio/temporal#2860), plus external terminate as wrong because no Workflow Task is scheduled so no cleanup runs. |
| AC3 | acceptance_criterion | Given the 71.9 MB unreclaimable, 0.8 to 1.6 MB per day, and 38 percent never-terminal figures were sampled immediately after a manual cleanup pass and therefore omit that cleanup as a variable, when the ADR records its hibernation verdict, then the verdict's stated basis is architectural shape and those figures are recorded as invalid for steady-state reasoning with the omitted cleanup variable named. | pass | Ruled SATISFIED by adv-reviewer attempt 2 on all three sub-conditions independently: (a) verdict's stated basis is architectural shape; (b) the figures are recorded as invalid for steady-state reasoning; (c) the omitted variable is NAMED, not implied — a manual cleanup pass performed immediately before sampling, including ~74 change closures. |
| AC4 | acceptance_criterion | Given 40 epic workflows are running and none has ever closed, when the ADR is written, then epic workflows are covered by the same verdict and the absence of a routine epic close path is recorded as a distinct finding. | pass | DEC-3 covers epics under the same verdict AND records the absent routine close path as a distinct finding (40 running, zero ever closed; completion machinery exists at workflows.ts:2494-2522 but nothing drives it). Both halves present. |
| AC5 | acceptance_criterion | Given the orphan history pool of roughly 93.5 MB across 20 workflows, when the ADR records it, then it separates established fact (history scanner healthy, worker.historyScannerEnabled defaults true, ADV calls only terminate and never delete or reset) from unestablished mechanism (what produces orphans and whether the set is still growing), and states the latter as unresolved. | pass | DEC-4 separates established fact (scanner healthy; worker.historyScannerEnabled defaults true per common/dynamicconfig/constants.go:3372-3376; ADV only terminates, zero delete/reset call sites) from unestablished mechanism, states the latter unresolved, and records the ~50x population discrepancy as the reason neither number is adopted. |
| AC6 | acceptance_criterion | Given signals for unregistered names are discarded when a workflow completes, when the ADR is written, then rq-hib04 is recorded with its relationship to the verdict stated explicitly, including any ordering constraint the verdict creates. | pass | DEC-5 states both halves required by the amended wording: the work's value does not depend on the verdict (the hazard is pre-existing on paths ADV already runs), AND the affirmative verdict creates a ship-first ordering constraint because hibernation increases completion events, which is when buffered signals are lost. |
| AC7 | acceptance_criterion | Evidence: sourced Temporal lifecycle findings captured at `~/.config/opencode/skills/agent-temporal-lifecycle/SKILL.md` verified by file existence and by source-URL citation for each external claim. | pass | ~/.config/opencode/skills/agent-temporal-lifecycle/SKILL.md exists (24486 bytes). Mechanically verified exhaustively, not sampled: 8/8 F-facts carry a source URL, 6/6 A-anchors carry an appropriate anchor, all 16 body URLs appear in the grouped source index, frontmatter parses as valid YAML. Extended this cycle with the entity-workflow lifetime findings, both quotes verified verbatim by fetching docs.temporal.io/design-patterns/entity-workflow directly. |
| C1 | constraint | Must not implement hibernation, orphan reclamation, or setDefaultSignalHandler in this change, because it is decision-only per the parent change. | respected | git diff trunk...HEAD is empty; worktree clean; zero commits ahead of trunk. No implementation of hibernation, orphan reclamation or setDefaultSignalHandler. Deliverables are the ADR (Temporal artifact) and the skill (global config, outside the repo). |
| C2 | constraint | Must preserve the signal and query only workflow surface, with no defineUpdate in workflow-reachable code. | respected | No code changed, so no defineUpdate could be introduced. Design validator separately confirmed no conflict with rq-changeWorkflowSignalOnly01, since DEC-2's mechanism is a signal. |
| C3 | constraint | Must not present the orphan root cause as established, and must not run any destructive Temporal or SQLite command against live state. | respected | DEC-4 states the orphan mechanism as not established and adopts neither disputed population number. All Temporal and SQLite operations during this change were reads or a single scoped repair signal; no destructive command was run against live state. |
| C4 | constraint | Must obtain ADR approval at the design gate before any downstream implementation begins. | respected | Design gate completed 2026-08-05T04:26:19Z with approval evidence recorded, before any downstream implementation. addWorkflowHibernation was created as a record only; no implementation has begun. |
| DONT1 | avoidance | Do not re-litigate the ruled-out mechanisms without new evidence. | respected | No ruled-out mechanism was re-argued. The ruled-out set (in-workflow durable signal queue, continueAsNew as count-bounding, auto-archive, creation ceiling, shared workflow, lazy-spawn, evaluateWorkerDeploymentReadiness, dualWriteAfterMutation as durability basis, verifyDeployedBuildIdentity alone) does not reappear as justification. Verified by adv-reviewer attempts 2 and 3. |
| DONT2 | avoidance | Do not assert that an existing mechanism guarantees a property without tracing declaration through consumer to observable effect. | respected | Four DONT2 violations occurred during authoring and all four were caught and corrected rather than shipped: Assumptions 1, 4, 6 and 9. Each is recorded in the Overturned Assumptions table with its refuting anchor. Attempt 3 swept the previously-unread design tail and found no new instance. |
| DONT3 | avoidance | Do not defer the decision by spawning more per-symptom tickets. | respected | The decision is made and recorded, not deferred. Downstream records are its named consequences with an explicit ordering constraint (rq-hib04 ships before hibernation), not tickets substituting for a verdict. The user separately reviewed the earlier reflexive change-creation and judged the three follow-ups warranted on their merits. |
| OOS1 | out_of_scope | The read-path 8s deadline in `plugin/src/temporal/retry-wrapper.ts:203`, owned by fixHealthViewTimeoutHealth, fixChangeReadTimeouts, and fixTemporalReadDiagnosis. | not_applicable | Read-path 8s deadline (retry-wrapper.ts:203) not addressed; owned by fixHealthViewTimeoutHealth, fixChangeReadTimeouts, fixTemporalReadDiagnosis. Recorded in the design as Overturned Assumption 5 to prevent it being misread as workflow-count pressure. |
| OOS2 | out_of_scope | Worker process memory of roughly 3 GB across 3 workers. | not_applicable | Worker process memory (~3 GB across 3 workers) not addressed. Distinct resource with no workflow-lifecycle interaction. |
| OOS3 | out_of_scope | Per-session task-queue accumulation, already solved by four terminal changes. | not_applicable | Per-session task-queue accumulation not addressed; already solved by four terminal changes. Cited in the design only as precedent that ADV solves this pattern class by automatic reaping. |
| OOS4 | out_of_scope | Implementing any reclamation of orphaned history. | not_applicable | No orphan reclamation implemented. DEC-4 explicitly defers it until the mechanism is established, per P40, and routes the investigation to investigateOrphanWorkflow with DDC4 as its opening obligation. |

