# Acceptance

Reviewed at: 2026-07-20T19:54:03.698Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | All existing tests in `adv-worktree.archived-branches.test.ts` continue to pass (with assertion updates reflecting the new helper call shape: `detectArchivedMergedBranches` is still called with `archivedChangeIds: string[]`, but that array is now built per-local-branch rather than from `store.changes.list`). | pass | Existing detect tests unchanged (6 pass) + archived-branches tests reconciled to new per-branch call shape (detectArchivedMergedBranches still called with archivedChangeIds:string[]). Full suite 6564 pass. adv_run_test tr_mrtn5wq6. |
| SC2 | success_criterion | New tests cover: AC1 (list-not-called + concurrency cap), AC2 (dryRun skip fetch), AC3 (fetch timeout degrades), AC4 (partial-result path), AC5 (fail-closed lookups incl. data:null), AC6 (changeId paths). | pass | New tests cover AC1 (list-not-called + concurrency cap<=4), AC2 (dryRun skip fetch), AC3 (fetch timeout degrade), AC4 (clamp+self-partial+emergency), AC5 (schema_error/not_found/data:null/not_archived), AC6 (changeId paths). 141 targeted pass (tr_mrtn5wq6). |
| SC3 | success_criterion | `pnpm run check` green; targeted `bin/oc-test targeted -- src/tools/adv-worktree.archived-branches.test.ts` green. | pass | pnpm run check GREEN (schemas/typecheck/manifests/isolation/lockfile/lint/format). Targeted 141 pass (tr_mrtn5wq6). Full suite 6564 pass + 1 expected fail, zero regressions. Build succeeds. |
| SC4 | success_criterion | Manual perf verification on this repo confirms sub-budget completion. | pass | Disk-store perf harness on main repo (26 local change/* branches, 43 archived projections): NEW dryRun 953-1219ms across 3 runs vs 5s AC8 target. OLD list-enumeration alone 505ms + it additionally ran detect(~1.33s)+unbounded fetch. |
| C1 | constraint | **No new public tool surface.** The `adv_worktree_cleanup` tool args are unchanged (`timeoutMs` already exists). Output shape additions must be additive (`partial`, `omissions`, `effectiveTimeoutMs`). | respected | Tool args unchanged (timeoutMs pre-existing). Output additions (partial, omissions, effectiveTimeoutMs, timeoutNote) are additive only; all prior fields preserved. |
| C2 | constraint | **No detect signature change.** `detectArchivedMergedBranches` keeps `archivedChangeIds?: string[]`. The inversion lives entirely in the helper. | respected | detectArchivedMergedBranches signature unchanged (input + deps:Pick<GitFinalizeDeps,'runGit'>). Inversion lives in the helper. 6 pre-existing detect tests pass unmodified. |
| C3 | constraint | **No use of `store.changes.listSummary`** — explicitly forbidden for safety-critical callers per `store-types.ts:327-331`. | respected | Helper contains no store.changes.list or listSummary calls (reviewer static-verified archived-branch-cleanup.ts). AC1 test asserts store.changes.list NOT called. |
| C4 | constraint | **No change to squash-merge detection semantics** (`detectSquashMergeByTree`, `git cherry` proof kinds). | respected | detectSquashMergeByTree and git cherry proof-kind logic (git-finalize.ts) untouched; only the local-branch-list git call was extracted. mergeProof kinds unchanged (AC7). |
| C5 | constraint | **No change to deletion safety rules** (`WORKTREE_CHECKED_OUT`, `deleteChangeBranch` semantics). | respected | WORKTREE_CHECKED_OUT skip and deleteChangeBranch semantics unchanged; only a bounded runGit was injected into getCheckedOutChangeBranches. Deletion-path tests pass. |
| C6 | constraint | **No change to mode="worktrees" behavior.** | respected | mode='worktrees' branch untouched. adv-worktree.test.ts + status-hygiene.test.ts 33 pass; full suite green. |
| C7 | constraint | `execGit` (utils/git.ts) gains a backward-compatible optional `timeoutMs` param (default 5000; existing callers unchanged). Sub-budgets are enforced via Node `execFile`/`spawnSync` kill-on-timeout, not `Promise.race`. No other `execGit` behavior changes. | respected | execGit gains optional timeoutMs=5000 forwarded to execFile timeout (real SIGTERM). Backward-compatible; all existing callers keep default. git.test.ts rq-execGitTimeoutParam01 proves short budget kills slow git. Sub-budgets via execFile/spawnSync kill-on-timeout, not Promise.race. |
| C8 | constraint | Trunk stays on default; all implementation in `change/fixArchivedBranchCleanup` worktree per isolation policy. | respected | All implementation performed in change/fixArchivedBranchCleanup worktree; trunk untouched. |
| DONT1 | avoidance | Avoid heuristic inference of archived status (e.g., "if a `change/*` branch exists and is merged, assume archived"). The single source of truth is `store.changes.get(id)` returning `status: "archived"`. | respected | Archived status is determined solely by store.changes.get(id).status==='archived'. No merged-implies-archived heuristic. Fail-closed for every other outcome. |
| DONT2 | avoidance | Avoid silently swallowing `LoadResult` errors. Surface them in `omissions` with typed reasons. | respected | All LoadResult failure variants (not_found/schema_error/read_error/data:null/reject/timeout) surface as typed omissions with detail; none silently swallowed. AC5 tests confirm. |
| DONT3 | avoidance | Avoid broadening scope to archive-enumeration performance (#239 family) — that is owned elsewhere. | respected | No changes to archive enumeration (#239) surface; only the cleanup helper's per-id inversion. store.changes.list left intact for its other callers. |
| DONT4 | avoidance | Avoid modifying `detectArchivedMergedBranches`'s internal squash/cherry logic. | respected | detectArchivedMergedBranches internal squash/cherry loop unchanged; only the local-list git call extracted to listLocalChangeBranchEntries. |
| DONT5 | avoidance | Avoid caching archived-status beyond a single tool invocation (the helper runs once per call; cross-call caching is owned by the store layer). | respected | No cross-call archived-status cache added. Per-id gets run once per tool invocation; store layer owns any caching. |
| DONT6 | avoidance | Avoid adding new dependencies. | respected | No new dependencies added; reused existing withTimeout, execGit, git-finalize helpers. pnpm lockfile unchanged. |

