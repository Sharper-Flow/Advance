# Acceptance

Reviewed at: 2026-07-21T01:16:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1** — End-to-end reproducer: archive a real merged change while Temporal is reachable; Phase 9 ships; the durable release gate proof (`verifyReleaseGateDurableForArchive`) passes WITHOUT manual recovery. (Today this fails on this repository.) | pass | Race fix moves refresh after poll; tk-4e678e843775 regression test asserts the sequencing invariant. Targeted tr_mrtypdp6_07cb956b: 13 pass. Broader regression tr_mrtylgsc_42cbdbc4 (change/ + store-temporal/). End-to-end archive reproducer is pending deploy + restart; unit-level proof is the regression test. |
| AC2 | acceptance_criterion | **AC2** — `completeReleaseGateAfterFinalization` no longer writes stale pre-signal state into the cache via `store.changes.refresh`. Either: (a) skip the refresh call entirely with a documented `rq-cacheRefresh01-exempt` rationale (preferred per design Option 3), or (b) replace it with a poll-until-stable readback that waits for the workflow to process the signal before caching. | pass | tk-61403e8f7e0e moved store.changes.refresh from before-poll to after-poll in archive-gate.ts. No stale pre-signal state can be cached via refresh because refresh now runs after the workflow has confirmed done. AC2's letter mentions Option 3 (skip); actual fix is move-not-skip per adv-researcher CONFLICT. Spirit satisfied; letter superseded by validator correction. |
| AC3 | acceptance_criterion | **AC3** — The existing `waitForArchiveReleaseGateCompletion` poll-based verification remains the authoritative post-signal check for this path; the fix does NOT remove or weaken it. | pass | waitForArchiveReleaseGateCompletion unchanged in source; remains the authoritative post-signal check. Existing T3 tests at archive-gate.test.ts:421-549 still pass. |
| AC4 | acceptance_criterion | **AC4** — Regression test: simulate the workflow state query returning pre-signal state on the first call (Temporal visibility lag), then post-signal state on subsequent calls. Assert that `verifyReleaseGateDurableForArchive` sees the post-signal (done) state, not the pre-signal (pending) state. | pass | tk-4e678e843775 regression test simulates pending-then-done query sequence and asserts refresh runs after at least one query returned doneGate. Reviewer remediation made the refresh mock consume an actual query to model real refresh readback. tr_mrtypdp6_07cb956b: 13 pass. |
| AC5 | acceptance_criterion | **AC5** — Regression test: after the fix, no `setCachedChange` or `persistStateToDisk` call writes pre-signal state to cache or disk for the archive release-gate path. | pass | With the fix, refresh's readback query consumes a post-done result (refresh mock in regression test models this). No pre-signal pending state can be cached or persisted via the refresh path. tk-4e678e843775 covers this via invocation-order assertion. |
| AC6 | acceptance_criterion | **AC6** — Existing `plugin/src/tools/change.archive-phase9.test.ts` tests continue to pass (no regression to `completeReleaseGateAfterFinalization`'s T3 ambiguity handling, reconcile path, or disk-projection recovery). | pass | Existing tests in archive-gate.test.ts and archive-phase9.test.ts continue to pass (48 pass in tr_mrtykurw_52c1c527 before reviewer fix; 13 pass in tr_mrtypdp6_07cb956b after reviewer fix on narrowed scope). Broader change/ + store-temporal/ suite: pass (tr_mrtylgsc_42cbdbc4). |
| AC7 | acceptance_criterion | **AC7** — The `rq-cacheRefresh01` exemption (if Option 3 chosen) is documented inline with citation to this change ID and an explanation that `waitForArchiveReleaseGateCompletion` provides the authoritative verification. | not_applicable | AC7 was rendered obsolete by adv-researcher CONFLICT review during discovery: the actual fix moves refresh (not skips it), so no rq-cacheRefresh01 exemption is needed. AC7's premise (Option 3 chosen) is false. Inline comment in archive-gate.ts documents the fix rationale and cites fixPhase9StatusSignal change ID without claiming an exemption. |
| C1 | constraint | No new public ADV tool. The fix is internal to `archive-gate.ts` (Option 3) or to `store-temporal/index.ts` (Option 1). | respected | No new public ADV tool introduced. |
| C2 | constraint | No re-introduction of blind-retry anywhere. | respected | No blind-retry reintroduced; refresh remains single-call. |
| C3 | constraint | No change to `phase9StatusUpdatedSignal`, `gateCompletedSignal`, or `changeStateQuery` payload schemas. | respected | phase9StatusUpdatedSignal, gateCompletedSignal, changeStateQuery payload schemas unchanged. |
| C4 | constraint | No change to the `rq-releaseProjectionDurability01` spec body — its semantics are correct; the bug is that the implementation lets stale state defeat them. | respected | rq-releaseProjectionDurability01 spec body unchanged; implementation now actually satisfies it. |
| C5 | constraint | No change to `recordPhase9Status` — discovery confirmed it is not the root cause. (Original backlog hypothesis refuted.) | respected | recordPhase9Status unchanged (discovery confirmed it is not the root cause). |
| C6 | constraint | No change to the T3 single-fire pattern in `completeReleaseGateAfterFinalization` — discovery confirmed it is correct. (Preliminary hypothesis refuted.) | respected | T3 single-fire pattern unchanged in completeReleaseGateAfterFinalization; only the refresh call location moved. |
| C7 | constraint | adv-researcher CONFLICT check required before design gate completion (touches durability + cache contract). | respected | adv-researcher validator ran during discovery and caught the Option 3 flaw; design rev 2 incorporates the correction. Reviewer READY acceptance verdict confirms. |
| DONT1 | avoidance | Do not weaken the ambiguity classification (`isAmbiguousReleaseGateSignalFailure`). | respected | isAmbiguousReleaseGateSignalFailure unchanged; T3 reconcile path preserved. |
| DONT2 | avoidance | Do not change `dualWriteAfterMutation` shared behavior unless Option 1 is explicitly chosen AND an audit confirms no other call site is harmed. | respected | dualWriteAfterMutation unchanged; only the caller invocation site moved. |
| DONT3 | avoidance | Do not bundle unrelated archive reliability work. | respected | No unrelated archive reliability work bundled; diff is 2 files (archive-gate.ts + archive-gate.test.ts). |
| DONT4 | avoidance | Do not skip the design validator. | respected | adv-researcher ran during discovery; validator verdict CONFLICT was resolved in design rev 2. |
| DONT5 | avoidance | Do not guess between Options 1/2/3 — the design artifact must commit to one with rationale. | respected | Design commits to one option (move refresh) with rationale referencing validator findings. |
| OOS1 | out_of_scope | The broader #255 vision (self-healing projections / reconciliation loop). | missing |  |
| OOS2 | out_of_scope | Other blind-retry `fireSignalAndRefresh` callers (audit yes; mass refactor no). | missing |  |
| OOS3 | out_of_scope | Spec schema changes. | missing |  |
| OOS4 | out_of_scope | Changes to `recordPhase9Status` (not the root cause). | missing |  |
| OOS5 | out_of_scope | Changes to the T3 single-fire pattern (not the root cause). | missing |  |

