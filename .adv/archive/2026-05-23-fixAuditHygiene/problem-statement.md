Advance works at the core runtime level, but current audit surfaces have correctness and hygiene bugs that can mislead agents and accumulate machine state: active-change lists can be incomplete and show wrong task counts due to a memo fast-path; synthetic test ADV dirs accumulate after interrupted test runs; OpenCode session-debt scans can inspect the wrong relative DB path; and WIP worktree discovery over-reports poisoned workflows unrelated to active worktrees. The desired outcome is trustworthy agent-facing status/list/WIP output plus safe cleanup prevention for synthetic test residue.