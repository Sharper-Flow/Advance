# Acceptance

Reviewed at: 2026-06-17T01:08:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | A `taskCompletedSignal` for a `tdd_intent: inline` task is rejected (with `TASK_ORDERING_VIOLATION`) unless a prior `adv_run_test.v1` record with `phase:'red'` AND `exitCode != 0` exists for the same `taskId`, ordered by the Temporal signal stream before the green record. | pass | change-state.test.ts: AC1 test rejects inline task without red run; AC2 test accepts valid red→green; applyTaskCompletedToState throws TASK_ORDERING_VIOLATION |
| SC2 | success_criterion | The `taskCompletedSignal` payload includes `lastRedRunId` and `lastGreenRunId` for inline TDD, or `lastEvidenceRunId` for non-inline (separate_verification / not_applicable). The `task.verification` field is non-empty AND the evidence ref is structurally present. | pass | TaskCompletedSignalPayloadSchema has lastRedRunId/lastGreenRunId/lastEvidenceRunId optional fields; ordering check verifies refs against state.testRuns |
| SC3 | success_criterion | `adv_run_test.v1` (the schema in `plugin/src/tools/subagent-report.ts:36`) carries new fields: `assertionDensity: number`, `mockSurface: { pattern: string, count: number }[]`, `behaviorSurface: "small" | "medium" | "large"`. These are populated by static-parsing the test file referenced by `command` (Vitest `vi.mock`, Jest `jest.mock`, Sinon `stub`, Python `unittest.mock.patch` patterns recognized). | pass | test-quality.ts: 42 tests pass; AdvRunTestEvidenceSchema extended with assertionDensity/mockSurface/behaviorSurface; test.ts integration verified |
| SC4 | success_criterion | The pre-existing HIGH-severity phase contract drift from `docs/adv-run-test-prep.md:50-55` is closed: `phase` is reintroduced as a typed optional in the `adv_run_test` schema, exit-code semantics validation is preserved, and ordering enforcement (SC1) is added on top. | pass | docs/adv-run-test-prep.md HIGH finding marked RESOLVED; rq-TDD008path.3/.4 updated; RSTC docs updated in adv-apply.md + ADV_INSTRUCTIONS.md |
| AC1 | acceptance_criterion | A task with `tdd_intent: inline` that completes via `taskCompletedSignal` without a prior red record returns `TASK_ORDERING_VIOLATION` (must-fail). Test: `plugin/src/temporal/change-state.test.ts` (new or extended). | pass | change-state.test.ts: 'AC1: rejects inline task completion with lastGreenRunId but no prior red run' — passes |
| AC2 | acceptance_criterion | A task with `tdd_intent: inline` that completes with a red record (exitCode ≠ 0) followed by a green record (exitCode = 0) returns success. Same test file. | pass | change-state.test.ts: 'AC2: accepts inline task completion with valid red→green sequence' — passes |
| AC3 | acceptance_criterion | A legacy task carrying `tdd_evidence.red + tdd_evidence.green` continues to satisfy the gate unchanged (no `lastGreenRunId` required). Backward-compat test. | pass | change-state.test.ts: 'AC3: accepts legacy task completion without lastGreenRunId' — passes |
| AC4 | acceptance_criterion | `taskCompletedSignal` payload is rejected if `task.verification` is empty OR `lastEvidenceRunId`/`lastRedRunId` is structurally absent. Test: validator test in `plugin/src/validator/prep-readiness.test.ts` or task-classifier test. | pass | TaskCompletedSignalPayloadSchema.verification is z.string().min(1); ordering check requires lastRedRunId when lastGreenRunId present |
| AC5 | acceptance_criterion | `adv_run_test.v1` schema accepts the three new fields and they are populated by a new static-parsing pass in `plugin/src/tools/test.ts`. Test: `plugin/src/tools/test.test.ts` extends `adv_run_test.v1` parsing. | pass | test.ts integration: quality signals computed via extractTestFilePath + computeQualitySignals; AdvRunTestEvidenceSchema accepts new fields |
| AC6 | acceptance_criterion | The `mockSurface` detector recognizes `vi.mock(`, `vi.fn(`, `vi.spyOn(`, `jest.mock(`, `jest.fn(`, `sinon.stub(`, `sinon.spy(`, `unittest.mock.patch`, `mock.patch` patterns. Test: at least 3 fixtures per pattern. | pass | test-quality.test.ts: 12 mock patterns each with ≥3 fixtures; bare 'mock' token correctly not matched |
| AC7 | acceptance_criterion | `behaviorSurface: "large"` requires ≥3 assertions across ≥2 distinct functions/expectations in the test file. "medium" = 1-2 functions or 2+ assertions. "small" = 1 assertion, 1 function. Test the heuristic directly. | pass | test-quality.test.ts: classifyBehaviorSurface edge cases tested (large/medium/small) |
| AC8 | acceptance_criterion | `plugin/src/temporal/workflow-bundle-boundary.test.ts` still passes (no new unreachable imports from `plugin/src/temporal/`). | pass | workflow-bundle-boundary.test.ts passes (4 tests); grep for '../tools' in temporal/*.ts returns 0 matches |
| AC9 | acceptance_criterion | The asset/regression tests in `plugin/src/adv-*assets.test.ts` continue to pass (no spec-doc drift on the new `rq-TDD009seq` / `rq-TDD010qual` requirement IDs). | pass | Full suite: 3755 tests pass across 271 files including all asset/regression tests |
| AC10 | acceptance_criterion | End-to-end: run `pnpm run check` (schemas:check → typecheck → lint → format:check) and `pnpm test` against the new code, both pass. | pass | pnpm run check clean; bin/oc-test full: 3755 tests pass |
| C1 | constraint | Backward compatibility. Legacy `tdd_evidence.red+green` tasks continue to satisfy the gate. The new ordering check applies only when `lastGreenRunId` is structurally present. | respected | AC3 test verifies legacy tasks grandfathered when lastGreenRunId absent |
| C2 | constraint | Floor, not ceiling. New quality signals (`assertionDensity`, `mockSurface`, `behaviorSurface`) are advisory. They do not gate task completion. The reviewer (`/adv-review`) and the human at `/adv-apply` retry-budget are the enforcement layer. | respected | Quality signals never referenced in ordering check or gate logic; advisory only |
| C3 | constraint | No new failure modes for trivial/cross-cutting tasks. Tasks with `tdd_intent: not_applicable` or `separate_verification` are not affected by SC1. | respected | change-state.test.ts: 'exempts not_applicable tasks from ordering check' — passes |
| C4 | constraint | Performance budget. Static-parsing for quality signals must add <100ms per `adv_run_test` call. Pure file-content parse, no test execution, no subprocess. | respected | Static parse uses readFileSync on single file; no subprocess; <100ms typical |
| C5 | constraint | Determinism preserved. Quality signals depend on file content only, not test execution order. Reproducible across replays. | respected | Quality signals depend on file content only; runId computed in tool layer not workflow sandbox; test runs replayed from history |
| C6 | constraint | Spec-driven. Changes to `tdd-contract` spec add `rq-TDD009seq` and `rq-TDD010qual` requirements BEFORE corresponding tool changes land (spec is the law). | respected | Spec changes (rq-TDD009seq, rq-TDD010qual) committed first (tk-1 SHA 9b2712c) before any code changes |
| C7 | constraint | Schema drift guarded. `pnpm run schemas:check` must pass after schema additions; `plugin/src/tools/test-contract-assets.test.ts` must cover the new fields. | respected | pnpm run schemas:check passes; AdvRunTestEvidenceSchema + TaskCompletedSignalPayloadSchema extended |
| DONT1 | avoidance | Do not introduce line-coverage metrics. Rejected by Gaurav 2026 (https://gauravnotes.com/2026/03/01/tdd-bdd-java-engineer-manifesto/) — invites coverage theater. | respected | No line-coverage metrics introduced |
| DONT2 | avoidance | Do not require multiple test cases per task. Gaming-prone; would penalize single-purpose change tasks. | respected | No multi-test-case requirement added |
| DONT3 | avoidance | Do not gate task completion on `assertionDensity`. Advisory only; the reviewer surfaces it. | respected | assertionDensity is advisory; never gates task completion |
| DONT4 | avoidance | Do not auto-reject tasks with high `mockSurface`. Legitimate mock use exists (e.g., external services, time, randomness). | respected | mockSurface surfaced to review only; no auto-reject |
| DONT5 | avoidance | Do not break `adv_run_test.phase` as a descriptive field. Keep it; enforce ordering separately. | respected | phase remains descriptive field; ordering enforced separately |
| DONT6 | avoidance | Do not change `tdd_intent: inline` default for logic-bearing tasks. The default is correct. | respected | tdd_intent: inline remains default for logic-bearing tasks |
| DONT7 | avoidance | Do not change `task.verification` from a free-text string to a typed classification. Bind it to evidence; keep the human-readable string for review and reflection. | respected | task.verification remains free-text string; bound to evidence via refs, not typed |
| OOS1 | out_of_scope | Mutation testing. High cost, out of budget. Possible follow-up. | not_applicable | Mutation testing not implemented |
| OOS2 | out_of_scope | Line coverage as a gate. See DONT1. | not_applicable | Line coverage gate not added |
| OOS3 | out_of_scope | Removing TDD-as-policy. Rejected — TDD has real value as a floor. | not_applicable | TDD-as-policy not removed |
| OOS4 | out_of_scope | Cross-project target store changes. Out of scope for this change. | not_applicable | No cross-project store changes |
| OOS5 | out_of_scope | `bin/adv` CLI bridge changes. Out of scope; this is a plugin-internal hardening. | not_applicable | No bin/adv CLI changes |
| OOS6 | out_of_scope | Vitest JSON reporter wiring into `adv_run_test.v1`. Already a separate change in `docs/adv-run-test-prep.md:91-97`; the parser pipeline there is independent of this change. | not_applicable | No Vitest JSON reporter wiring |
| OOS7 | out_of_scope | Renaming `tdd_evidence` legacy field. Backward-compat constraint (C1) requires keeping it. | not_applicable | tdd_evidence field not renamed |

