# Acceptance

Reviewed at: 2026-07-23T22:25:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | **SC1**: After this bootstrap ships + OpenCode restarts, the original `adoptOrphanedSessionQueues` workflow (orphaned on a previous-process session queue) becomes queryable/signalable via normal ADV tools — no recovery tooling (`adv_change_workflow_terminate`, `adv_change_close recoveryMode: poisoned_history`, out-of-band Temporal CLI) needed. | pass | orphan-session-adoption.itest.ts: a stranded workflow on an unpolled session queue becomes queryable after adoptNextOrphan() registers a real poller (handle.query returns truthy). Adoption is the recovery path; no adv_change_workflow_terminate/poisoned_history needed. |
| SC2 | success_criterion | **SC2**: Any future ADV change workflow orphaned by OpenCode process exit is adopted by the next live process within bounded time. | pass | plugin-init.ts onBeat calls adoptNextOrphan every 10s heartbeat; orphan-queue-adopter adopts one orphan/tick. Future orphaned workflows adopted within bounded time (<=heartbeat cadence). |
| AC1 | acceptance_criterion | **AC1**: Live worker enumerates `RUNNING` workflows on its project namespace and identifies session task queues (`advance-{projectId}-sess_*`) with ≥1 `RUNNING` workflow not currently polled. ** | pass | list-orphan-session-queues.ts enumerates RUNNING workflows via AdvAffectedProjects Visibility query, filters session-scoped queues (advance-{P}-sess_*) not in polled set. 6 unit tests cover filter/sort/group/idempotency. |
| AC2 | acceptance_criterion | **AC2**: Worker registers each such queue; within bounded time, signals/queries against workflows on that queue succeed. ** | pass | e2e: after adoptNextOrphan registers a real Worker on the stranded session queue, handle.query(getChangeStateQuery) succeeds (bounded-retry sentinel). |
| AC3 | acceptance_criterion | **AC3**: Queues with zero `RUNNING` workflows are NOT adopted. | pass | list-orphan-session-queues.ts:79 skips non-RUNNING; orphan-queue-adopter returns early on empty orphans. Unit test 'does nothing when there are no orphans'. |
| AC4 | acceptance_criterion | **AC4**: Startup completes without waiting for adoption if `workflowClient.list()` is slow/failing; degradation warning logged; retries on next heartbeat. | pass | plugin-init.ts: onBeat invocation is fire-and-forget void...catch(()=>undefined); adopter instantiated only after worker+temporalBundle ready. Adoption never blocks startup; Visibility failures swallowed, retried next heartbeat. |
| AC5 | acceptance_criterion | **AC5**: Adoption is idempotent — repeated heartbeat re-scans produce no duplicate `registerQueue` calls for already-polled queues. | pass | worker.queues read live each tick; list-orphan-session-queues skips already-polled. e2e asserts registerQueue called exactly once across two scans; unit test 'skips a queue already in worker.queues'. |
| AC6 | acceptance_criterion | **AC6**: Adoption observes `run-error` IPC, records the failure in `getTemporalWorkerDiagnostics`, and does not crash the worker. **Specific retry-cap and operator-surface policy deferred to `/adv-design` pending investigation of `run-error` IPC frequency and failure modes.** | pass | try/catch observes register failures; 3-attempt cap + 5-min cooldown (recordFailure); try/finally releases scanInFlight; heartbeat .catch prevents crash. Unit test 'applies cooldown after 3 failed attempts'. lastError now captured (remediation) for operator triage. |
| AC7 | acceptance_criterion | **AC7**: Adoption state is visible via existing diagnostics (`adv_temporal_diagnose` per-queue detail + `adv_status view:health` summary count). | pass | doctor.ts renders orphan_queue_adoption (50-entry cap + omittedTrackedQueues + disabled-note); status-health.ts adds {enabled,tracked,inCooldown} summary through status-health-plan.ts + status.ts. doctor.test.ts (3 cases) + status-health.session-queue.test.ts (2 cases). |
| AC8 | acceptance_criterion | **AC8**: Per-queue adoption completes within `< SERVICEABILITY_GRACE_MS` (90s) — one queue per heartbeat tick max. | pass | single-flight (one adoption/tick) + 8s tick timeout (DEFAULT_TICK_TIMEOUT_MS) + 10s heartbeat = 18s worst case < 90s SERVICEABILITY_GRACE_MS. Unit test 'enforces single-flight'. |
| AC9 | acceptance_criterion | **AC9**: Adoption applies indiscriminately to all orphan session queues regardless of wedge-isolation provenance (operator can manually `adv_change_workflow_terminate` if contamination concern). | pass | list-orphan-session-queues returns ALL orphans (only RUNNING + session-scoped + not-polled filters); no wedge-isolation opt-out. Operator can adv_change_workflow_terminate contaminated workflows. |
| C1 | constraint | **C1**: Additive only — no changes to new-workflow routing, session-id generation, `registerQueue` API/semantics, recovery-classification. | respected | Additive only: new helper + coordinator + env flag + diagnostics field. No routing/session-id/registerQueue-semantics changes. registerQueue signature unchanged. |
| C2 | constraint | **C2**: Non-blocking startup — adoption never blocks worker init. | respected | Adoption is fire-and-forget on the heartbeat; never blocks worker init. Same as AC4. |
| C3 | constraint | **C3**: `RUNNING`-only adoption — queues with zero `RUNNING` workflows never adopted. | respected | Strict RUNNING filter (list-orphan-session-queues.ts:79). |
| C4 | constraint | **C4**: No task-queue mutation — adoption only adds pollers, never re-routes workflows. | respected | registerQueue adds a poller; no taskQueue mutation / UpdateTaskQueue calls. Delta body asserts 'only adds pollers, never re-routes'. |
| C5 | constraint | **C5**: Idempotent — re-scans don't duplicate registrations. | respected | Idempotent via worker.queues live read. Same as AC5. |
| C6 | constraint | **C6**: Observe `run-error` IPC — adoption records failures; worker survives. | respected | run-error observed (catch); failure recorded in perQueueState -> diagnostics; worker survives (try/finally + heartbeat catch). Same as AC6. |
| C7 | constraint | **C7**: Bounded per-adoption — `< 90s` per queue, one queue per heartbeat tick. | respected | 8s tick timeout, one queue/tick. Same as AC8. |
| C8 | constraint | **C8**: Single-project scope — no cross-namespace adoption. | respected | Single projectId bound at construction; Visibility query project-scoped. No cross-namespace enumeration. |
| C9 | constraint | **C9**: Privacy — adoption logs use opaque `sess_*` ids only (no PIDs/paths), matching `adv_session_list` privacy contract. | respected | Diagnostics use opaque advance-{P}-sess_* queue names (no PIDs/paths); status-health redacts to counts only. Unit test 'summarizes ... without exposing queue identifiers'. |
| DONT1 | avoidance | **DONT1**: Option (b) default-route-to-project-queue for change workflows — workflow-start routing change with broader blast radius. Belongs in original `adoptOrphanedSessionQueues` proposal's design evaluation. | respected | No default-route-to-project-queue; workflow-start routes to session queue when sessionId provided. |
| DONT2 | avoidance | **DONT2**: Option (c) session-lifecycle exit hooks for marking queues adoptable — depends on reliable OpenCode exit hooks; doesn't help on crash/kill. Belongs in original proposal. | respected | No process-exit hook added; adoption is heartbeat-driven only. |
| DONT3 | avoidance | **DONT3**: Session-id surface inconsistency fix between `adv_session_list` (`sha256(pid:startTicks)[:8]`) and the plugin (`nanoid(8)`) — separate defect, belongs in original proposal. | respected | session-id.ts untouched; queue format sess_{id} preserved (read-only). |
| DONT4 | avoidance | **DONT4**: `recovery-classification.ts:82` reform to accept `query_failed` as `poisoned_history` — broader recovery-tool reform, belongs in `replaceRecoveryToolSprawl`. | respected | Recovery-classification taxonomy unchanged; doctor.ts only ADDS a diagnostics field. |
| DONT5 | avoidance | **DONT5**: `registerQueue` API or semantics changes — bootstrap only adds callers. | respected | registerQueue signature unchanged across worker-multi/in-process/out-of-process; change only consumes it. |
| DONT6 | avoidance | **DONT6**: Cross-project / cross-namespace adoption — bootstrap is single-project. | respected | Single-namespace; client bound at init; no cross-namespace enumeration. Same as C8. |

