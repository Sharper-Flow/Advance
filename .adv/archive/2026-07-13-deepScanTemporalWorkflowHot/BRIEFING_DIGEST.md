# Archive Briefing Digest

**Change ID:** deepScanTemporalWorkflowHot
**Title:** Deep-scan Temporal workflow hot path
**Status:** archived
**Generated:** 2026-07-13T01:22:15.775Z

## Identity Anchors

- CHANGE
- STATUS
- TERMINAL_GATE_SUMMARY

## Archive Digest

**Status:** archived

| Gate | Status |
| --- | --- |
| proposal | done |
| discovery | done |
| design | done |
| planning | done |
| execution | done |
| acceptance | done |
| release | pending |

## Epic Context

Epic: optimizeAdvPerformanceStructure · Deep-scan Temporal workflow hot path (order 6)

## Durable Facts

Showing 74 of 74 durable facts.

- **[agenda]** follow_ups: FOLLOW-UP (test/determinism): add committed replay-history fixtures for ACCEPTANCE_EXECUTIVE_SUMMARY_PROOF_PATCH and STATE_BACKED_ACCEPTANCE_PROOF_PATCH / STATE_BACKED_GATE_ARTIFACT_PROOF_PATCH branches; wire into replay-determinism.test.ts so Worker.runReplayHistory guards all live patch markers.
- **[agenda]** follow_ups: FOLLOW-UP (perf measurement): add a micro-benchmark for snapshotState() + projectChangeState at representative and near-AGGREGATE_HARD_CAP state sizes to determine whether the JSON deep-clone is a real hotspot before optimizing.
- **[agenda]** follow_ups: FOLLOW-UP (perf, conditional): if benchmark shows the deep clone dominates, evaluate structuredClone (first VERIFY availability/behavior inside the Temporal workflow isolate) or a shaped-subset projection, each behind a new replay/determinism test.
- **[agenda]** follow_ups: DOC: note that CAN history threshold is env-tunable via ADV_TEMPORAL_CHANGE_HISTORY_THRESHOLD (default 5000) for operators with large-history changes.
- **[agenda]** follow_ups: WARNING: packet omitted explicit IN_SCOPE/OUT_OF_SCOPE/DONE_WHEN/STOP_WHEN/VERIFICATION anchors; proceeded under the prose research objective (replay determinism, handler cost, projection/serialization, history growth, test/perf strategy) without inferring identity fields.
- **[archive_only_evidence]** sources: Temporal TS SDK — patched()/deprecatePatch() versioning: patched() returns true when not replaying and when replaying histories produced with the same patchId; else false. deprecatePatch() phases out a patch after all live runs are post-migration. Confirms the repo's DISCOVERY/ACCEPTANCE/STATE_BACKED patch markers are the canonical determinism-safe evolution mechanism.
- **[archive_only_evidence]** sources: Temporal TS SDK — continue-as-new to bound history: Long-lived signal/query workflows accumulate history and can exceed per-workflow limits; monitor workflowInfo().continueAsNewSuggested and hand off to a fresh run passing state as seed. Matches repo's shouldContinueAsNewFromInfo + seedState pattern.
- **[archive_only_evidence]** sources: Temporal TS SDK — allHandlersFinished drain before CAN/complete: Canonical CAN recipe awaits condition(allHandlersFinished) before continueAsNew so in-flight signal/update handlers are not interrupted. Repo does exactly this at both the terminal-complete and CAN paths (workflows.ts:1678, 1732).
- **[archive_only_evidence]** sources: Temporal TS SDK — standard API usage (patched + continueAsNewSuggested): Canonical durable-singleton workflow combining setHandler queries/signals/updates, patched branching, and continueAsNewSuggested-gated CAN. Directly mirrors changeWorkflow shape.
- **[archive_only_evidence]** sources: Temporal TS SDK — Worker.runReplayHistory / runReplayHistories: Replay committed histories against the current bundle to detect non-determinism regressions in CI; supports batch histories. Repo uses runReplayHistory in replay-determinism.test.ts but with only ONE fixture.
- **[archive_only_evidence]** sources: Temporal TS SDK — TestWorkflowEnvironment time-skipping: createTimeSkipping() env skips server time for fast deterministic workflow tests; repo already wraps this via withTestWorkflowEnvironment across queries/signals/epic/projection/CAN itests.
- **[archive_only_evidence]** sources: Repo — snapshotState deep-clone on every projection: snapshotState() does JSON.parse(JSON.stringify(state)) and is called on every scheduleChangeProjection / projectChangeState / archive activity. Cost is O(state size) per signal when projectionChangesDir is set.
- **[archive_only_evidence]** sources: Repo — size caps validated against Temporal payload/history limits: ARTIFACT_HARD_CAP 256KB, AGGREGATE_HARD_CAP ~1.8MB, tests assert <2MB per-payload and CAN-seed limits. Layer-2 size guard rejects via state-mutation (not throw), preserving workflow liveness.
- **[archive_only_evidence]** sources: Repo — history threshold + CAN seed: DEFAULT_CHANGE_HISTORY_THRESHOLD 5000 events (env-tunable), shouldContinueAsNewFromInfo honors continueAsNewSuggested OR historyLength>=threshold; seedState hand-copies ~40 fields.
- **[archive_only_evidence]** sources: Repo — single replay fixture: Only one committed history fixture (discovery-gate patch). Three patch markers exist in workflows.ts but only one is covered by a replay fixture.
- **[archive_only_evidence]** architecture_assessment: The changeWorkflow hot path is by-the-book against Temporal TS canon: (1) determinism-safe evolution via patched()/deprecatePatch() markers with documented deprecation plans; (2) history bounding via continueAsNewSuggested||historyLength threshold with allHandlersFinished drain before both terminal-complete and CAN — matching the official CAN recipe exactly; (3) signal handlers never throw (Layer-2 state-mutation rejection) so a bad payload cannot fail the workflow, which is the correct pattern per Temporal's handling-messages guidance; (4) size caps proven <2MB payload/CAN-seed ceilings by unit test. Query handlers are synchronous shallow returns (spread/derive), which is replay-safe and cheap. The one genuine hot-path cost is snapshotState()'s JSON.parse(JSON.stringify(state)) full deep-clone executed on EVERY projection-triggering signal (and every archive activity) — O(state bytes) CPU + transient allocation per signal, growing with tasks/documents/subagent_reports accumulation. This runs inside the workflow sandbox on the replay-critical path, so it also re-executes on every replay. It is correct and safe, but it is the measurable serialization hotspot. Secondary: replay-regression coverage is thin (one fixture for three live patch markers), so a determinism regression in the acceptance/state-backed patch branches would not be caught by the committed-history replay gate.
- **[agenda]** follow_ups: {
  "packet_defect": false,
  "missing_packets": [],
  "warnings": ["TASK_SCOPE/IN_SCOPE/OUT_OF_SCOPE/DONE_WHEN/STOP_WHEN/VERIFICATION anchors were not provided in the orchestrator packet; this report uses the change title plus the human-readable intent and does not infer identity fields."],
  "consumer_warnings": []
}
- **[archive_only_evidence]** findings: FINDING-001 [hotspot] changeWorkflow signal handler fan-out (workflows.ts:1213-1654): ~36 wf.setHandler calls each wrap their reducer via signalMutation/signalAsync. Every successful mutation then triggers upsertSignalSearchAttributes (rebuild + wf.upsertSearchAttributes) and, depending on options.projectAfter, an unawaited scheduleChangeProjection which itself calls the proxyActivities-bound writeChangeProjection. Replay determinism is preserved (proxy activities are deterministic) but the per-signal overhead stack is the dominant cost for active changes.
- **[archive_only_evidence]** findings: FINDING-002 [hotspot] gateCompletedSignal uses an ad-hoc promise chain (workflows.ts:1357-1370). The chain grows on each concurrent gateCompleted: every concurrent accept from C3 (and the 3-agents burst in concurrent-signaling.itest.ts) appends a then-chain segment to gateCompletionChain. A single failing completeGateWithReadiness blocks all subsequent segments only if they await gateCompletionChain; ordering is also enforced per-signal via the shared gate state, but the chain shape itself is a leak source if any completion forgets to await.
- **[archive_only_evidence]** findings: FINDING-003 [risk] snapshotState() deep-clones the full ChangeWorkflowState on every projected signal (workflows.ts:717-718). Even with ARTIFACT_HARD_CAP=256KB per artifact, the accumulated state size + subagent_reports + tasks + documents is JSON.parse(JSON.stringify(state))'d before each projection activity. With seedState.documents accumulating toward AGGREGATE_HARD_CAP (~1.8MB), each successful projection signal performs two serializations of the full state, dominating replay cost and signal-handler latency.
- **[archive_only_evidence]** findings: FINDING-004 [hotspot] buildChangeSearchAttributes (search-attributes.ts:175-239) recomputes every attribute on every signal completion. The function filters state.worktrees by status, sorts branch and path arrays via localeCompare, derives bucket via deriveBucket(bucketCtxFromState(state, nowMs)), and normalizes lifecycleState. Called once per successful signal in afterSuccess(); safe for replay but ~O(worktrees log worktrees) per signal.
- **[archive_only_evidence]** findings: FINDING-005 [risk] applySubagentReportSubmittedToState performs 3 unbounded scans plus a partial seenReportIds writes (change-state.ts:1031-1060). state.seenReportIds grows unboundedly across the workflow lifetime and is part of the continueAsNew seed — there is no ring-buffer / cap. With cross-project fan-in reports this can grow into per-event footprint pressure on history length.
- **[archive_only_evidence]** findings: FINDING-006 [risk] getReadyTasksFromChangeState is O(n^2) on the hot query path (change-state.ts:1498-1524). getReadyQuery (workflows.ts:696) is bound to this and is called per-peer-agent query in multi-session scenarios; nested state.tasks.find inside per-task deps.filter produces quadratic scan that grows with task count.
- **[archive_only_evidence]** findings: FINDING-007 [hotspot] eval chain in completeGateWithReadiness (workflows.ts:955-1209) is the deepest nested call in the workflow. evaluateGateReadiness → priorGateBlockers → discoveryContractBlockers/acceptanceContractBlockers → checkUnresolvedDesignConcerns (latestDesignerReportsByTask + nested dispositions.some) → checkRequiredObligationReleaseBlockers → checkOpsFollowupReleaseBlockers. Each is safe but cumulative cost scales linearly with state.tasks + state.subagent_reports + state.contract.items on every gate completion.
- **[archive_only_evidence]** findings: FINDING-008 [risk] checkAggregateSize in applyContentWithSizeGuard (change-state.ts:575-589) JSON.stringify's a fresh document projection on every artifact update to enforce ~1.8MB aggregate cap. With frequent artifact updates this is repeated full-stringify of all 6 documents even when content size is well below any cap (the early-return path skips it via size > ARTIFACT_HARD_CAP, but the aggregate branch always runs after the per-artifact check).
- **[archive_only_evidence]** findings: FINDING-009 [structure] PATCH markers enforce replay-safe branching at gate completion (workflows.ts:895-922, 962, 975, 990). Four wf.patched markers gate which activity sequence is replayed for older histories. The replay-determinism.test.ts fixture covers DISCOVERY_CONTRACT_READINESS_PATCH but not the other three — coverage gap.
- **[archive_only_evidence]** findings: FINDING-010 [pattern] Bundle boundary is enforced by two complementary tests (workflow-bundle-imports.test.ts: webpack bundler forbids fs/node:* / better-sqlite3; workflow-bundle-boundary.test.ts: transitive boundary forbids wf.defineUpdate, storage/, tools/, tool-registry.ts, plugin-init.ts, node:*). Belt-and-suspenders approach. New helpers must keep these invariants, especially through change-state.ts which is reached by ~36 apply* functions.
- **[archive_only_evidence]** findings: FINDING-011 [risk] continueAsNew seed (workflows.ts:1684-1733) explicitly spreads 33 fields including subagent_reports and documents. With long-running changes and large state, the seed itself approaches the 2MB Temporal payload cap; AGGREGATE_HARD_CAP (~1.8MB) is the structural guard. Per-payload cap is enforced at the reducer level but signal-fired probes (e.g. explore queries that read snapshots) can still threaten the seed at rotation time.
- **[archive_only_evidence]** hotspots: plugin/src/temporal/workflows.ts (functions changeWorkflow / epicWorkflow, ~2010 lines; ~36 signal handlers, ~16 query handlers, gateCompletionChain, continueAsNew seed) — orchestration complexity
- **[archive_only_evidence]** hotspots: plugin/src/temporal/change-state.ts (1694 lines, 80 exported symbols; hot reducers: applySubagentReportSubmittedToState, applyTaskAddedToState, getReadyTasksFromChangeState, applyContentWithSizeGuard) — central reducer
- **[archive_only_evidence]** hotspots: plugin/src/temporal/gate-readiness.ts (1107 lines, 30 functions; deeply nested readiness check + advisory criteria evaluations) — gate readiness is the deepest call in the workflow
- **[archive_only_evidence]** hotspots: plugin/src/temporal/contracts.ts (733 lines, 18 interfaces; ChangeWorkflowState surfaces 33 seedState fields) — schema surface
- **[archive_only_evidence]** hotspots: plugin/src/temporal/search-attributes.ts (249 lines; buildChangeSearchAttributes rebuilds attrs on every signal) — derived projection recomputed per signal
- **[archive_only_evidence]** hotspots: plugin/src/temporal/epic-state.ts (909 lines; recomputeEpicProgress invoked from 14 apply* functions) — epic hot path
- **[archive_only_evidence]** risks: Replay determinism drift via Date()/Math.random() etc — change-state.ts injects `let logicalTick = 0` and `new Date(workflowEpoch + logicalTick++).toISOString()` (workflows.ts:556-559) for workflowNow() so timestamps stay deterministic; any refactor that injects a transient Date.now() or new Date() inside a reducer or signalMutation will not surface locally but will break replay the moment a worker reload changes the code path. Vitest's replay-determinism.test.ts covers DISCOVERY_CONTRACT_READINESS_PATCH but the 3 acceptance PATCH markers and acceptanceContractBlockers are not covered by replay fixtures.
- **[archive_only_evidence]** risks: Aggregation overshoot pressure — state.seenReportIds grows monotonically and is included in the continueAsNew seed (workflows.ts:1723, change-state.ts:1031-1060). Without a ring-buffer or hard cap, an agent loop or fixture can balloon the seed beyond AGGREGATE_HARD_CAP.
- **[archive_only_evidence]** risks: Projection write storms — scheduleChangeProjection (workflows.ts:761-770) issues an unawaited proxyActivities call per projected signal. Backpressure is bounded only by the activity's startToCloseTimeout (10s) and maximumAttempts 3 (workflows.ts:192). A burst of 150 signals (concurrent-signaling fixture) plus 9 gate completions schedules 9 unawaited activity calls with no global semaphore; the worker activity slot pool becomes the de-facto throttle.
- **[archive_only_evidence]** risks: Gate completion ordering edge — the gateCompletionChain pattern (workflows.ts:1357-1370) interleaves a Promise per concurrent gateCompleted. If a single completeGateWithReadiness throws (outside Temporal system-failure rejections), the chain swallows the error after marking gate stuck (workflows.ts:927-937). A throw that escapes the chain (e.g. an unhandled error inside markGateStuckForBlockers' evaluateGateCriteria try/catch) would tear down the workflow; the try/catch around criteria (workflows.ts:1191-1202) only shields evaluateGateCriteria, not the broader chain.
- **[archive_only_evidence]** risks: Cross-project coordination signal hot path — crossProjectCoordinationUpdatedSignal (workflows.ts:1419-1423) calls applyCrossProjectCoordinationUpdatedToState; if active changes fan into cross-project traffic at burst, the signalMutation wrapper still triggers buildChangeSearchAttributes which serializes affectedProjects + worktrees arrays; with N>50 active peer changes this dominates the per-signal cost.
- **[archive_only_evidence]** risks: wf.upsertSearchAttributes cost — per-signal applyAndUpsertSearchAttributes (workflows.ts:720-731) calls wf.upsertSearchAttributes via the SDK. Each call is a workflow task scheduling decision point on the hot path; not catastrophic but combined with projection writes it doubles the command count per signal for non-rejected mutations.
- **[archive_only_evidence]** risks: Dependency on large artifact bulk-paths — every state-changing signal that passes `projectAfter: true` (gateAwaitingApproval, gateStuck, phase9StatusUpdated, archiveRequested, changeCancelled) triggers a full-state deep-clone snapshot. For changes near the 1.8MB aggregate cap this is a ~3.6MB JSON.parse round-trip per signal; combined with the bundle boundary test enforcing no fs reach into the workflow, snapshot cost cannot be offloaded to I/O.
- **[unresolved_action]** open_questions: What is the realistic maximum state size encountered in production? Are any active changes known to approach AGGREGATE_HARD_CAP? (No telemetry source in this repo observed; would need Temporal admin Visibility or runtime instrumentation.)
- **[unresolved_action]** open_questions: Are replay fixtures for the 3 acceptance PATCH markers (ACCEPTANCE_EXECUTIVE_SUMMARY_PROOF_PATCH, STATE_BACKED_GATE_ARTIFACT_PROOF_PATCH, STATE_BACKED_ACCEPTANCE_PROOF_PATCH) committed or pending? Only DISCOVERY_CONTRACT_READINESS_PATCH has a committed history under plugin/src/temporal/__tests__/replay/histories/.
- **[unresolved_action]** open_questions: What is the typical peer-session query rate against getStateQuery / getReadyQuery / getInvestmentReportQuery? Deriving hot path priority requires telemetry that this repo does not contain.
- **[unresolved_action]** open_questions: Is there an explicit signal-coalescing strategy for projection writes? scheduleChangeProjection issues one proxy call per signal without debouncing; would a microtask-batched projection (with last-write-wins within a short window) materially reduce activity slot pressure?
- **[unresolved_action]** open_questions: Are there any recordings in .adv/reflections/ of workflow-perf incidents (history-length, replay failures, projection lag) to ground the bounded follow-ups?
- **[unresolved_action]** open_questions: For changes with many tasks and many subagent reports, would O(n^2) in getReadyTasksFromChangeState become observable at the scale exercised by concurrent-signaling.itest.ts (30 tasks) or does the next test boundary sit much higher?
- **[unresolved_action]** open_questions: Does the per-signal upsertSignalSearchAttributes traffic defeat the rebuild caching strategy, or is the rebuild cheap relative to the upsert round-trip?
- **[agenda]** follow_ups: Assessment should enumerate the exact live patch-marker set and confirm whether ACCEPTANCE_EXECUTIVE_SUMMARY_PROOF_PATCH is live or superseded by STATE_BACKED_ACCEPTANCE_PROOF_PATCH, so the replay-fixture follow-up covers every truly-live marker.
- **[agenda]** follow_ups: seenReportIds cap follow-up must include a continue-as-new seed/size-guard interaction test to prove the cap does not corrupt carried state across rotation.
- **[agenda]** follow_ups: Ready-task index follow-up (change-state.ts:1505-1528) should carry a determinism-preserving transition/state-machine test since derivation runs inside workflow-reachable code.
- **[archive_only_evidence]** sources: Temporal SDK — patched(): patched(patchId) returns true when not replaying and true during replay only if history was produced by a worker with the same patchId; else false. Run new code on true, old on false to preserve determinism. Confirms repo wf.patched() branching pattern is by-the-book.
- **[archive_only_evidence]** sources: Temporal SDK — deprecatePatch(): deprecatePatch(patchId) phases out a marker only after ALL live runs/histories are post-migration; premature removal yields undefined replay behavior. Confirms the repo's documented 'keep marker until histories archived then wf.deprecatePatch' deprecation plan is correct, and that removing markers on static inspection alone would be unsafe.
- **[archive_only_evidence]** sources: Temporal SDK — canonical patched/deprecatePatch + continueAsNew workflow: Official sample uses setHandler + condition + patched('new-logic') if/else + continueAsNewSuggested-gated continueAsNew in a single workflow. Structurally matches changeWorkflow: handler registration, patched branches, and history-length/suggested continue-as-new.
- **[archive_only_evidence]** sources: Temporal SDK — allHandlersFinished before continueAsNew: Recommended recipe: on continueAsNewSuggested, stop intake then `await condition(allHandlersFinished)` before continueAsNew, so in-flight signal/update handlers drain and are not lost across rotation. Matches workflows.ts:1732-1733 (await wf.condition(wf.allHandlersFinished); await wf.continueAsNew(seed)) exactly.
- **[archive_only_evidence]** sources: Local — snapshotState deep clone: snapshotState() = JSON.parse(JSON.stringify(state)). Deterministic under replay (pure, no I/O/ordering nondeterminism), so correctness-safe; cost is a size-scaling performance candidate, not a correctness bug. Supports design's measurement-first guardrail before structuredClone/shaped-projection.
- **[archive_only_evidence]** sources: Local — patch markers vs replay fixtures: Four live patch-marker constants (DISCOVERY_CONTRACT_READINESS, STATE_BACKED_GATE_ARTIFACT_PROOF, STATE_BACKED_ACCEPTANCE_PROOF, ACCEPTANCE_EXECUTIVE_SUMMARY_PROOF) gate divergent command sequences; only one committed history fixture (DISCOVERY_CONTRACT_READINESS) is replay-tested. Confirms the 'incomplete replay fixtures' coverage gap and the per-marker replay-fixture follow-up.
- **[archive_only_evidence]** sources: Local — O(n^2) ready-task lookup: For each pending task, blocked_by deps resolve via nested state.tasks.find(...) — quadratic in task count. Correctness is fine; it is a growth/performance candidate for a bounded index (Map by id) with a scoped follow-up + test. Matches design.
- **[archive_only_evidence]** sources: Local — unbounded seenReportIds + continue-as-new seed: seenReportIds is a spread-append de-dupe array with no cap, carried verbatim into the continue-as-new seed (workflows.ts:1723). Growth-bound candidate; capping intersects the size-guard/CAN-seed invariant, so it needs its own proposal + tests (design defers it correctly). Aggregate size guard exists (change-state.size-guard.test.ts) but does not cap this array specifically.
- **[archive_only_evidence]** architecture_assessment: The design is an evidence/report-only change that validates existing Temporal mechanics rather than mutating them. Every non-trivial claim in its 'Initial Evidence' block is confirmed in the repository: per-signal JSON deep clone (workflows.ts:717-718), signal/search-attribute fan-out (upsertSignalSearchAttributes), O(n^2) ready-task lookup (change-state.ts:1505-1528), unbounded seenReportIds carried into the CAN seed (workflows.ts:1723), gate-readiness fan-out (gate-readiness.ts), and incomplete replay fixtures (1 of 4 live patch markers covered). Against official Temporal TypeScript sources, the existing patterns are canonical: wf.patched()/wf.deprecatePatch() usage and its documented 'keep until histories archived' deprecation plan match the SDK contract; the `await condition(allHandlersFinished)` drain before continueAsNew matches the SDK's recommended recipe line-for-line; and the JSON deep clone is deterministic under replay (pure, no nondeterministic ordering/IO), so it is a size-scaling performance candidate, not a correctness defect. The design's guardrails (no clone replacement on static inspection alone; measurement-first before snapshotState/projection changes; per-marker replay fixtures; no changes to caps/thresholds/signals/queries/patch markers/bundle boundaries) are the correct, conservative dispositions and align with Temporal's own versioning/patching safety guidance. No unsafe or unsupported design direction was found: the change explicitly implements nothing in workflow code and defers all mutations to scoped follow-ups with verification strategies.
- **[wisdom_candidate]** wisdom_candidates: [success] Research-only Temporal assessments can validate performance hypotheses without unsafe optimization by pairing local symbol evidence with official SDK citations, explicit no-code boundaries, and measurement-first follow-ups.
- **[archive_only_evidence]** verification: tests_run=git diff --check && git status --short && git diff --name-only, Firecrawl scrape of all four cited official Temporal SDK sources; citation-term inspection results=pass — Clean git checks produced no output: no tracked, staged, unstaged, or untracked product changes and no whitespace errors. All seven required categories are present in the executive summary and map to existing local evidence: snapshot/projection (workflows.ts:717-770), signal/search-attribute fan-out (720-770, 1213-1654; search-attributes.ts:175-248), ready-task lookup (change-state.ts:1498-1530), seenReportIds seed growth (1031-1060; workflows.ts:1682-1733), live patch branches plus replay fixture (895-922, 962/975/990; replay-determinism.test.ts:21-70), and gate readiness (gate-readiness.ts:799-854; workflows.ts:955-1209). The cited official Temporal SDK sources were fetched successfully and contain patched/deprecatePatch, continueAsNew, allHandlersFinished, and Worker.runReplayHistory evidence. Executive summary limits clone replacement to benchmark plus isolate verification; preserves caps, threshold/drain semantics, signals/queries, patch markers, and workflow-bundle boundaries; it recommends no unsafe patch removal or unmeasured clone replacement.
- **[unresolved_action]** required_main_agent_actions: Amend the executive summary and AC4 review-matrix row so each follow-up explicitly names file/symbol scope, verification method, and whether measurement is required before any code change.
- **[unresolved_action]** required_main_agent_actions: Re-run acceptance review after that artifact correction; do not complete release/archive while AC4 evidence remains internally inconsistent.
- **[unresolved_action]** required_main_agent_actions: Optional hardening: replace Temporal SDK `main` URLs with immutable commit permalinks and source-to-claim mapping before archive.
- **[wisdom_candidate]** wisdom_candidates: [convention] Research-only release evidence needs each follow-up to carry an explicit target, verification method, and measurement-before-code disposition; a global evidence map does not make an individual recommendation bounded.
- **[archive_only_evidence]** verification: tests_run=git status --short && git diff --check && git log --oneline -5, ADV artifact-only review of agreement, executive summary, and acceptance matrix results=pass — Worktree status and diff check were clean; HEAD f13befa1 matches both research task checkpoints. Agreement, executive summary, and acceptance matrix confirm no code, configuration, deployment, archive, or ops change. Review found AC4 evidence inconsistency: the matrix says all four follow-ups name files/symbols, but the listed follow-ups do not.
- **[unresolved_action]** required_main_agent_actions: Record AC4 as satisfied; clarification is evidence-only and requires no workflow or product-code change.
- **[archive_only_evidence]** verification: tests_run=git status --short && git diff --name-only && git diff --cached --name-only, bin/oc-test targeted -- src/temporal/__tests__/replay-determinism.test.ts results=pass — Clean worktree: no tracked, staged, or untracked repository changes. All four follow-ups name concrete files/symbols or line-bounded regions, define a verification method, and explicitly label Measurement required Yes/No. Replay fixture test passed: 1 file, 1 test.
- **[unresolved_action]** required_main_agent_actions: Proceed through the release gate using the READY hardening evidence; then archive only after release completion. Do not revisit AC4 unless the executive summary follow-up text changes.
- **[unresolved_action]** required_main_agent_actions: No code or deployment action required for this evidence-only change.
- **[wisdom_candidate]** wisdom_candidates: [convention] Evidence-only performance assessments should name each follow-up's concrete code location, executable verification method, and explicit Measurement required: Yes/No to make AC review release-ready.
- **[archive_only_evidence]** verification: tests_run=bin/oc-test targeted -- src/temporal/__tests__/replay-determinism.test.ts, git status --short && git diff --check && git diff --stat results=pass — Replay fixture passed: 1 file, 1 test, including Worker.runReplayHistory (exit 0). Worktree status, diff check, and diff stat emitted no output after verification: clean, no code/config/product delta. AC4 recheck: all four follow-ups name plugin/src locations/symbols, concrete verification, and Measurement required Yes/No: replay coverage (No), serialization measurement (Yes), idempotency growth (No), ready-task indexing (No). Named symbols verified locally: snapshotState at workflows.ts:717, scheduleChangeProjection at :761, seenReportIds at change-state.ts:1031-1060 and workflows.ts:1723, getReadyTasksFromChangeState at change-state.ts:1498. Gate state: acceptance done; release is only remaining gate, so archive is not yet eligible.
- **[epic_terminal_note]** epic.membership: optimizeAdvPerformanceStructure · Deep-scan Temporal workflow hot path (order 6)

## Contract / AC Coverage

| ID | Kind | Status |
| --- | --- | --- |
| AC1 | acceptance_criterion | pass |
| AC2 | acceptance_criterion | pass |
| AC3 | acceptance_criterion | pass |
| AC4 | acceptance_criterion | pass |
| AC5 | acceptance_criterion | pass |
| C1 | constraint | respected |
| C2 | constraint | respected |
| C3 | constraint | respected |
| C4 | constraint | respected |
| OOS1 | out_of_scope | not_applicable |
| OOS2 | out_of_scope | not_applicable |
| OOS3 | out_of_scope | not_applicable |

## Unresolved Actions

- What is the realistic maximum state size encountered in production? Are any active changes known to approach AGGREGATE_HARD_CAP? (No telemetry source in this repo observed; would need Temporal admin Visibility or runtime instrumentation.)
- Are replay fixtures for the 3 acceptance PATCH markers (ACCEPTANCE_EXECUTIVE_SUMMARY_PROOF_PATCH, STATE_BACKED_GATE_ARTIFACT_PROOF_PATCH, STATE_BACKED_ACCEPTANCE_PROOF_PATCH) committed or pending? Only DISCOVERY_CONTRACT_READINESS_PATCH has a committed history under plugin/src/temporal/__tests__/replay/histories/.
- What is the typical peer-session query rate against getStateQuery / getReadyQuery / getInvestmentReportQuery? Deriving hot path priority requires telemetry that this repo does not contain.
- Is there an explicit signal-coalescing strategy for projection writes? scheduleChangeProjection issues one proxy call per signal without debouncing; would a microtask-batched projection (with last-write-wins within a short window) materially reduce activity slot pressure?
- Are there any recordings in .adv/reflections/ of workflow-perf incidents (history-length, replay failures, projection lag) to ground the bounded follow-ups?
- For changes with many tasks and many subagent reports, would O(n^2) in getReadyTasksFromChangeState become observable at the scale exercised by concurrent-signaling.itest.ts (30 tasks) or does the next test boundary sit much higher?
- Does the per-signal upsertSignalSearchAttributes traffic defeat the rebuild caching strategy, or is the rebuild cheap relative to the upsert round-trip?
- Amend the executive summary and AC4 review-matrix row so each follow-up explicitly names file/symbol scope, verification method, and whether measurement is required before any code change.
- Re-run acceptance review after that artifact correction; do not complete release/archive while AC4 evidence remains internally inconsistent.
- Optional hardening: replace Temporal SDK `main` URLs with immutable commit permalinks and source-to-claim mapping before archive.
- Record AC4 as satisfied; clarification is evidence-only and requires no workflow or product-code change.
- Proceed through the release gate using the READY hardening evidence; then archive only after release completion. Do not revisit AC4 unless the executive summary follow-up text changes.
- No code or deployment action required for this evidence-only change.
