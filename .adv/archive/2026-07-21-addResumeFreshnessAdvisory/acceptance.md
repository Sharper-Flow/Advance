# Acceptance

Reviewed at: 2026-07-21T15:20:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | Stale resumed change surfaces a bounded Resume Freshness advisory before the Gate Machine proceeds. | pass | resolver emits findings via fetchChangeContextSnapshot before snapshot render; pipeline test tk-338d015a8ee6 scenario 2 confirms stale change triggers advisory |
| SC2 | success_criterion | Fresh resumed change (`lastActivityAgeMinutes ≤ 60`) produces zero advisory noise. | pass | AC1 trigger guard skips resolver when lastActivityAgeMinutes <= 60; pipeline test scenario 1 verifies zero store calls + no Freshness line for fresh change |
| SC3 | success_criterion | Advisory stays within the bounded tool-call cost ceiling (~5 calls per stale resume). | pass | DDC1-8 caps enforced in resume-freshness-resolver.ts (RESUME_FRESHNESS_BUDGET_MS=8000, SIBLING_OVERLAP_SCAN_CAP=50, ARCHIVED_SINCE_SCAN_CAP=50, CODEBASE_DRIFT_COMMIT_CAP=100); primary-only invocation bound in status-enrich.ts |
| SC4 | success_criterion | Advisory inherits `/adv-coordinate`'s `repo_backed_fact | adv_backed_fact | judgment_call | freshness_limited` taxonomy — no new label space. | pass | ResumeFreshnessLabel type in resume-freshness.ts verbatim from /adv-coordinate taxonomy; no new labels introduced |
| AC1 | acceptance_criterion | Advisory fires iff the change's `lastActivityAgeMinutes` (output of `adv_change_list`, already computed at `plugin/src/tools/change.ts:849`) exceeds 60. | pass | RESUME_FRESHNESS_TRIGGER_MINUTES=60; resolveResumeFreshness entrypoint returns skipped:true when <=60; pipeline test scenario 5 confirms 60min=skip vs 61min=fire boundary |
| AC2 | acceptance_criterion | Advisory emits one or more stable finding codes: `resume:sibling_overlap`, `resume:archived_duplicate`, `resume:codebase_drift`, `resume:freshness_limited`. | pass | ResumeFreshnessCode type union enumerates exactly 4 codes; resolver tests emit each code; freshness_limited fallback covered in 3 entrypoint tests |
| AC3 | acceptance_criterion | Advisory does not block any gate transition. | pass | advisory is informational; resolver never calls gate mutation tools; pipeline test scenario 2 confirms snapshot renders normally after resolver runs |
| AC4 | acceptance_criterion | Advisory does not mutate ADV state (no close, supersede, task/gate mutations from the advisory itself). | pass | resolver code path uses only store.changes.get/list (read-only); appendResumeFreshnessRecommendation never invokes adv_change_close; resolver tests verify no store mutator calls |
| AC5 | acceptance_criterion | `buildChangeContextSnapshot` formatter stays pure — no storage import added to `plugin/src/utils/context-snapshot.ts`. | pass | plugin/src/utils/context-snapshot.ts imports only from ./workflow-directive (existing); ContextSnapshotResumeFreshness defined as local structural type to avoid storage import |
| AC6 | acceptance_criterion | Optional `resumeFreshness` field threads through all snapshot emit sites (gate.ts, change.ts ×2, status-enrich.ts ×2, change/recovery.ts, compaction-context.ts, context-snapshot-fetch.ts) without breaking existing emissions when the field is absent. | pass | resumeFreshness is optional field; buildChangeContextSnapshot accepts it; formatter renders nothing when undefined; 52 existing formatter tests still pass + 6 new tests confirm field handling |
| AC7 | acceptance_criterion | Each finding code + fresh-skip path + `freshness_limited` fallback covered by automated tests. | pass | resume-freshness-resolver.test.ts covers each code (sibling_overlap HIGH+MEDIUM, archived_duplicate HIGH+skip-parent, codebase_drift freshness_limited fallback), fresh-skip path, budget timeout path |
| AC8 | acceptance_criterion | Advisory scope is current-project only — no cross-project fan-out (Q1 decision). | pass | resolver uses store.changes.list({}) and list({includeArchived:true}) — both current-project scoped by Store invariant; no target_path routing in resolver |
| AC9 | acceptance_criterion | User dismissals are not persisted — finding re-raises on every resume (Q2 decision). | pass | resolveResumeFreshness is stateless (no module-level cache, no persistence layer); same input produces same output; no dismissal memory fields |
| AC10 | acceptance_criterion | `Freshness:` line displaces `Outcomes:` line first when snapshot is budget-constrained (Q3 decision, extends existing `dropOutcomes` rule). | pass | formatContextSnapshot 4-stage rule: dropOutcomes at optionalCount>=2, dropCurrent at >=3, dropSpacerOrWisdom at >=4; Freshness retained as last shed; context-snapshot.test.ts 4-optional scenario verifies <=10 lines |
| AC11 | acceptance_criterion | On a single HIGH-confidence `resume:archived_duplicate`, advisory surfaces a one-click "close+supersede" suggestion. Close executes only on explicit user acceptance via `adv_change_close` with approval evidence — agent does not auto-execute (Q4 decision). | pass | appendResumeFreshnessRecommendation emits only when exactly one repo_backed_fact archived_duplicate; snippet requires user to run adv_change_close with approvedByUser:true + approvalEvidence; status-enrich.test.ts wording-guard test asserts no 'one-click'/'button-click' |
| C1 | constraint | No new gate. Advisory is informational only. | respected | no new gate added; advisory is informational only; resolver cannot block gate transitions (no gate mutation calls) |
| C2 | constraint | Reuse-first — inherit `/adv-coordinate` taxonomy, `/adv-cleanup` duplicate normalization, `/adv-refactor` drift scanners, `appendRecencyRecommendation` plumbing. | respected | intersectFileLists extracted from validator/file-overlap.ts; execGit reused from utils/git.ts; pushStatusRecommendation reused from status-enrich.ts; no greenfield detection primitives |
| C3 | constraint | Bounded cost ceiling — ~3–5 tool calls per stale resume. | respected | hard caps: DDC2 50 siblings, DDC3 50 archives, DDC4 100 commits, DDC1 8s budget, DDC8 primary-only; ~3-5 calls per stale resume |
| C4 | constraint | Trigger band — `lastActivityAgeMinutes > 60` (matches "hot" exclusion boundary). | respected | RESUME_FRESHNESS_TRIGGER_MINUTES=60 matches existing 'hot' band boundary in appendRecencyRecommendation |
| C5 | constraint | Formatter purity — `buildChangeContextSnapshot` stays pure; resolver lives in storage layer (`plugin/src/storage/`). | respected | plugin/src/utils/context-snapshot.ts has no storage imports; resolver lives in plugin/src/storage/resume-freshness-resolver.ts |
| C6 | constraint | 10-line snapshot budget — explicit budget-shed rule extension; never silent overflow. | respected | formatContextSnapshot preserves 10-line cap in all combinations; 4-stage shed rule tested including 4-optional case |
| C7 | constraint | Stable finding codes only — no LLM-classified labels; codes must be structural and testable. | respected | ResumeFreshnessCode is fixed union type; labels are from inherited taxonomy; no LLM classification in resolver |
| DONT1 | avoidance | Do not make the advisory a gate or block any gate transition. | respected | no adv_gate_complete calls in resolver or recommendation emitter; advisory cannot block |
| DONT2 | avoidance | Do not greenfield detection logic — reuse existing `/adv-coordinate`, `/adv-cleanup`, `/adv-refactor` primitives. | respected | 7 existing primitives reused (taxonomy, intersectFileLists, execGit, pushStatusRecommendation, appendRecencyRecommendation pattern, fetchChangeContextSnapshot plumbing, buildChangeContextSnapshot signature extension) |
| DONT3 | avoidance | Do not mutate ADV state from the advisory itself (no close, supersede, task/gate mutations). | respected | resolver only invokes store.changes.get/list (read-only); no close/supersede/task/gate mutations fired from advisory path |
| DONT4 | avoidance | Do not persist user dismissal memory — re-raise on every resume (Q2 decision). | respected | no persisted dismissal state; resolveResumeFreshness is pure function of (store, changeId, input) |
| DONT5 | avoidance | Do not fan out cross-project — current-project scope only (Q1 decision). | respected | no target_path routing in resolver or injectors; current-project store only |
| DONT6 | avoidance | Do not silently exceed the 10-line snapshot budget. | respected | formatContextSnapshot hard 10-line cap; 4-stage shed rule structurally bounds all combinations |
| DONT7 | avoidance | Do not auto-execute close+supersede without explicit user acceptance — auto-recommend only (Q4 decision). | respected | appendResumeFreshnessRecommendation emits copy-pasteable snippet only; user must invoke adv_change_close themselves; wording uses 'one-command accept' never 'one-click'; wording-guard test enforces |

