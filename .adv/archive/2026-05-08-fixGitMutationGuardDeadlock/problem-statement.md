The git mutation guard (plugin/src/tools/git-guard.ts) creates a deadlock when worktree paths are inaccessible from the Bun sandbox. The guard blocks ALL git operations on dirty default branch (trunk), including recovery commands like `git stash`, `git checkout`, and `git switch`. Combined with Bun sandbox restrictions on worktree path access, there is no path to commit changes from inside an OpenCode session.