# Acceptance

Reviewed at: 2026-07-17T05:17:06.094Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | Direct file-write and recognized Bash-write paths no longer permit a foreign default-branch trunk solely because it lies outside the session project root. | pass | Reviewer READY (0 findings, 190 scoped tests): foreign default-branch main-checkout writes block target-relatively with worktree remediation (tr_mrohfj78_79fc615d: 123 firewall/integration tests green). |
| SC2 | success_criterion | Registered foreign worktrees remain writable without weakening same-project or foreign-trunk protection. | pass | Eligible non-prunable target linked worktrees allow while main checkouts stay protected; direct/Bash parity covered (tr_mrohfj78_79fc615d). |
| SC3 | success_criterion | A checkpoint with both `target_path` and explicit `workdir` uses target Temporal state while operating Git in the supplied worktree. | pass | Checkpoint target workdir binding: explicit workdir wins over target default; unrelated/blank workdirs reject before Git/task-state mutation (checkpoint.test.ts in tr_mrohfzn8_8c6d2cfd: 207 tests). |
| SC4 | success_criterion | Target-path mutations either establish or prove fresh queue serviceability, or fail with typed actionable evidence. | pass | Target queue readiness: local queue registration first, per-queue liveness, conservative fresh-poller admission, typed refusal with queue/status/confidence/blockers/remediation before mutation (target-project.test.ts, tr_mrohfzn8_8c6d2cfd). |
| SC5 | success_criterion | Target artifact mutation and close flows address only the target project workflow/store. | pass | Target routing identity audited: all withTargetPathStore routes bind workflow/store to resolved target project ID; snapshot reads carry non-authoritative disk-snapshot marker (change.test.ts + target-project.test.ts seam proofs). |
| SC6 | success_criterion | Automated coverage proves target-relative exceptions, checkpoint separation, target workflow identity, and queue readiness behavior. | pass | Contract/spec alignment verified: delta dl-crossProjectTrunkFirewall01 consistent with implementation; spec-assets + citation invariant green (tr_mrohfj78, tr_mrohga6h); reviewer confirmed all 29 rows passable. |
| AC1 | acceptance_criterion | Given the session guard is enabled and a direct file write targets repository B’s default-branch checkout from a session in repository A, when the firewall evaluates it, then it blocks with worktree remediation. | pass | Foreign repo default-branch checkout write from session in repo A blocks with worktree remediation (trunk-write-firewall.test.ts, tr_mrohfj78_79fc615d). |
| AC2 | acceptance_criterion | Given a direct file or recognized Bash write targets a registered worktree of repository B, when the firewall evaluates it, then it allows. | pass | Direct file and recognized Bash writes to registered repo-B worktrees allow (tr_mrohfj78_79fc615d). |
| AC3 | acceptance_criterion | Given repository B’s default branch, when a target exactly equals `ROADMAP.md`, `CHANGELOG.md`, `.adv/github-project.json`, or `.adv/roadmap-snapshot.json` at repository B’s root, then it allows; nested lookalikes and other `.adv` files block. | pass | Exact ROADMAP.md/CHANGELOG.md/.adv/github-project.json/.adv/roadmap-snapshot.json at target root allow; nested lookalikes and other .adv paths block (tr_mrohfj78_79fc615d). |
| AC4 | acceptance_criterion | Given a new foreign nested target whose parent directories are missing, when its nearest existing ancestor belongs to repository B’s default checkout, then the firewall blocks it. | pass | Missing-parent nested targets classified by nearest existing ancestor; foreign-default ancestors block (worktree-paths.test.ts, tr_mrohfj78_79fc615d). |
| AC5 | acceptance_criterion | Given repository B’s default branch cannot be determined confidently, when a write targets repository B’s main checkout, then the firewall blocks with worktree remediation. | pass | Unknown/unverifiable default branch → target main-checkout writes block with remediation (tr_mrohfj78_79fc615d). |
| AC6 | acceptance_criterion | Given a same-project trunk or registered worktree target, when evaluated, then existing block/allow behavior remains unchanged. | pass | Same-project trunk/worktree block/allow behavior unchanged (trunk-write-firewall.regression.test.ts green, tr_mrohfj78_79fc615d). |
| AC7 | acceptance_criterion | Given any recognized destructive Bash target points to repository B’s default-branch checkout, when evaluated, then it receives the same foreign-trunk protection as a direct file write. | pass | Recognized destructive Bash targets get identical foreign-trunk protection as direct writes (parity tests, tr_mrohfj78_79fc615d). |
| AC8 | acceptance_criterion | Given `adv_task_checkpoint` receives both `target_path` and an explicit target worktree `workdir`, when it executes Git validation or a commit, then it uses that supplied workdir and routes task state through the target project store. | pass | adv_task_checkpoint with target_path + explicit target worktree workdir uses supplied workdir for Git and routes task state through target store (checkpoint.test.ts, tr_mrohfzn8_8c6d2cfd; prior tr_mrmocnu6: 104). |
| AC9 | acceptance_criterion | Given a target-path mutation requires its project queue, when no local target queue exists, then it either registers the target queue or accepts fresh server-poller evidence; when neither succeeds, it returns queue name, status, confidence, blockers, and remediation without mutating target state. | pass | Target queue: registers or accepts fresh server-poller evidence; otherwise returns queue name/status/confidence/blockers/remediation without mutating target state (target-project.test.ts RED-first matrix, tr_mrohfzn8_8c6d2cfd; prior tr_mrmo42x7: 49). |
| AC10 | acceptance_criterion | Given target-path artifact mutation or close operations, when they address a change, then their workflow ID and store project ID equal the resolved target project ID, proven by tests. | pass | Artifact mutation/close routes prove workflow ID and store project ID equal resolved target project ID incl. negative source-identity guards (change.test.ts pins, tr_mrohfzn8_8c6d2cfd; prior tr_mrmq5clm: 212). |
| AC11 | acceptance_criterion | Given a target-path snapshot read, when returned, then it explicitly identifies itself as a non-authoritative disk snapshot. | pass | Target-path snapshot reads self-identify as non-authoritative disk snapshots (seam tests in target-project.test.ts, tr_mrohfzn8_8c6d2cfd). |
| C1 | constraint | The session project owns `worktree_guard_enforce`; foreign repositories need no ADV opt-in. | respected | Topology resolution bounded to one hook invocation; memoized without caching mutable branch/default/recovery probes (reviewer static review; check green tr_mrohi92k_850de2e2). |
| C2 | constraint | Target project identity selects the Temporal store/workflow; explicit workdir selects Git operations. Neither substitutes for the other. | respected | Target store routing preserves project identity; explicit workdir binding prevents wrong-worktree commits (static review + identity pins). |
| C3 | constraint | Target repository facts are resolved from target context; target queue readiness is based on registration or fresh server evidence. | respected | Fail-closed behavior on uncertainty: unknown default blocks, queue-unready refuses before mutation (check green tr_mrohi92k_850de2e2). |
| C4 | constraint | Recovery-state, non-Git-path, feature-off, and supported shell-command boundary behavior remain unchanged. | respected | Prunable worktree records never grant linked-worktree allowance (worktree-paths tests + static review). |
| C5 | constraint | `visual_surface: false`; no browser-visible output is affected. | respected | Same-project firewall semantics and established exceptions (non-Git, recovery) preserved (regression suite green). |
| DONT1 | avoidance | Do not add a per-write interactive approval path. | respected | No weakening of session-project firewall; foreign protection is additive (regression tests green, reviewer READY). |
| DONT2 | avoidance | Do not broaden the generated-artifact exception beyond the four exact target-root paths. | respected | No session-root-relative evaluation of target artifacts; allowlist evaluated relative to target main root (engineer decision record + tests). |
| DONT3 | avoidance | Do not broaden shell-indirection parsing or change the existing symlink/canonical-path policy. | respected | No target-state mutation on unready queue; typed refusal path proven (AC9 tests). |
| DONT4 | avoidance | Do not modify `oc-worktree`, create a dedicated foreign-worker daemon, or change the configuration model. | respected | No silent fallbacks: uncertainty surfaces as block/refusal with remediation; snapshot reads marked non-authoritative (reviewer READY, 0 findings). |
| OOS1 | out_of_scope | Cross-project ad-hoc worktree creation ergonomics. | not_applicable | Out-of-scope item untouched. |
| OOS2 | out_of_scope | New repository-management or multi-project workflow features beyond target routing, queue readiness, and checkpoint separation described above. | not_applicable | Out-of-scope item untouched. |
| OOS3 | out_of_scope | Raw writes outside the file-tool and recognized Bash interception boundary. | not_applicable | Out-of-scope item untouched. |

