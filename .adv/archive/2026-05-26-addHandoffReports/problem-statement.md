ADV intends sub-agents to run noisy task, research, review, and scanner loops while the main `adv` agent stays clean for orchestration. Current report persistence only covers `adv-engineer` and `adv-reviewer`; researcher, recon, and scanner lanes return transient prose/output. Documentation also references nonexistent `enforceTaskPolicy` runtime enforcement. The change should make optimized sub-agent handoffs durable and compact while correcting enforcement claims.