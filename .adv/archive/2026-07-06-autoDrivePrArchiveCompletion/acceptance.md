# Acceptance

Reviewed at: 2026-07-06T22:42:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | On the `pr_auto_merge` / `merge_queue` archive path with auto-merge armed, ADV completes the archive flow end-to-end without a second human turn — no manual re-invocation of archive or `adv_archive_repair` required when the PR merges. | pass | Auto-drive end-to-end via Phase 9.5 (adv-archive.md:506-533) + syncDefaultBranchAfterMerge helper. 6 commits. 4610 tests pass. Prose-orchestration known limitation (ct-1), not blocking. |
| SC2 | success_criterion | After the watched PR reaches `MERGED`, the local default branch is up to date with `origin/{default}` in the common (non-diverged) case, without the human running `git pull`. | pass | Real-git test 'clean ff-only' (git-finalize.test.ts:3001-3022) confirms afterHead==origin/trunk, 2-element ffCommits. |
| SC3 | success_criterion | The shipped-invariant bar is preserved: release completes only on positive `pr_merged`/reachability evidence, never on a bare timeout or false signal. | pass | 'does not record release-done' type-shape test (git-finalize.test.ts:3097-3121). adv-archive.md:528 explicit 'never escalate non-terminal to Shipped.' DONT3 regression guard (3653-3674). |
| AC1 | acceptance_criterion | When archive finalization returns route `pr_auto_merge` or `merge_queue` with auto-merge armed, ADV spawns `adv-ci-waiter` to watch the PR to terminal state and does not poll CI from the main agent. | pass | adv-archive.md:512-518 spawns adv-ci-waiter via Task tool. rq-releaseFinalization02.1 in spec.json. Regression guard 'git-finalize.ts does NOT reference Task-spawn or CI-wait APIs' (3639-3646). |
| AC2 | acceptance_criterion | When the watched PR reaches `MERGED` and `origin/{default}` reachability is proven, ADV fast-forwards the local default branch in place (`fetch` + `merge --ff-only`) without switching branches. | pass | FIXED ce-1/ce-2/ce-3: rev-list status check + HEAD-on-default guard + merge-path destructive-ops spy. Real-git tests green. 96 tests in git-finalize.test.ts. |
| AC3 | acceptance_criterion | When the local default branch has diverged from `origin/{default}` (local-only commits), ADV does NOT auto-merge or reset; it leaves the local branch untouched, surfaces the divergence with reconcile instructions, and still completes release on proven origin reachability. | pass | Helper diverged branch returns {status: 'diverged', localOnlyCommits} without merge/reset. Real-git test (git-finalize.test.ts:3024-3044) confirms local HEAD unchanged. |
| AC4 | acceptance_criterion | After a successful merge + sync, ADV drives archive to completion (`Shipped.` + worktree cleanup) via `verifyReleaseEvidenceFromMain`. | pass | adv-archive.md:521-525 re-calls adv_change_archive through verifyReleaseEvidenceFromMain. DONT3 regression guard confirms no redrive path. |
| AC5 | acceptance_criterion | When the waiter returns a non-terminal outcome (timeout / blocked / red-CI it cannot fix), the change stays active and ADV reports `Pending auto-merge.` / `Blocked.` with the PR URL and exact retry command — never a false `Shipped.`. | pass | adv-archive.md:526-529 renders 'Pending auto-merge.' with PR URL + retry command. rq-releaseFinalization04.1/.2 in spec.json. |
| AC6 | acceptance_criterion | Direct-push, no-remote, and `ff-only` archive paths are unchanged (regression-guarded). | pass | Regression guard 'syncDefaultBranchAfterMerge is exported but not invoked internally' (3595-3622). Pre-push gate green. Diff purely additive. |
| AC7 | acceptance_criterion | No CI-wait or Task-spawn import is added to worker-bundle-reachable modules (`git-finalize.ts` and its static imports); the worker-bundle boundary test stays green. | pass | Regression guards 'no new temporal/* imports' (3624-3637) + 'no Task-spawn or CI-wait APIs' (3639-3646). workflow-bundle-boundary.test.ts green (6 passed). |
| C1 | constraint | Main checkout NEVER switches branch; the default branch is updated in place only. | respected | FIXED ce-2: MAIN_NOT_ON_DEFAULT guard via git rev-parse --abbrev-ref HEAD after fetch. Destructive-ops spy covers both diverge + merge paths. |
| C2 | constraint | The main agent must not poll CI (P37); `adv-ci-waiter` is the only permitted waiter. | respected | adv-archive.md:518 P37 enforcement. Regression guard (3639-3646) confirms no CI-wait in helper. |
| C3 | constraint | Reuse existing contracts — `adv-ci-waiter` remediation, `verifyReleaseEvidenceFromMain` completion, and the `pr_merged` reachability proof. Do not reinvent merge/detection logic. | respected | Helper uses GitFinalizeDeps + mirrors reconcileChangeBranchWithDefault. Command references verifyReleaseEvidenceFromMain + syncDefaultBranchAfterMerge. |
| C4 | constraint | Layer boundary — no Task-spawn or CI-wait logic inside worker-bundle-reachable modules. | respected | Regression guards (3624-3637, 3639-3646). workflow-bundle-boundary.test.ts green. |
| C5 | constraint | Never mark release done without positive reachability/merge evidence. | respected | 'does not record release-done' type-shape test (3097-3121). Helper has no store/signal surface. |
| C6 | constraint | No force-push of the default branch; no `reset --hard` of the default branch when it has local-only commits. | respected | FIXED ce-1 + ce-3: helper only runs fetch / rev-list / merge --ff-only / log. Destructive-ops spy covers both paths. |
| DONT1 | avoidance | Do not change direct-push / no-remote / `ff-only` archive paths (they already complete synchronously). | respected | Regression guard (3595-3622) confirms purely additive. No existing function modified. |
| DONT2 | avoidance | Do not re-implement squash-merge re-detection (delivered by parent `fixPhase9SquashMergeRedetect` via `changeTipSha`). | respected | Helper has no changeTipSha/squash detection. Tip-SHA delegated to parent change (referenced in spec body). |
| DONT3 | avoidance | Do not route completion through `redriveArchivedUnmergedBranch` — that path re-arms a still-pending PR, it is not the completion-from-merged path. | respected | Regression guard (3653-3674) reads Phase 9.5 section, asserts redrive absent and verifyReleaseEvidenceFromMain present. |
| DONT4 | avoidance | Do not modify `adv-ci-waiter`'s internal remediation behavior (reused as-is). | respected | git log d99bc31e^..beca5a22 --name-only confirms zero .opencode/agents/adv-ci-waiter.md / instructions/oc-ci-wait.md modifications. |
| DONT5 | avoidance | Do not run a blind `git pull` on trunk (verified: the diverged-with-conflict case wedges the main checkout in a half-merge, violating C1). | respected | Helper grep — no pull invocation. Destructive-ops spy covers merge path (ce-3 fix). |

