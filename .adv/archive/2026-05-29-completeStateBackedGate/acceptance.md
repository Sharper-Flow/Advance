# Acceptance

Reviewed at: 2026-05-29T04:38:18.815Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1 — Non-recovery acceptance state-backed.** When `state.documents.executiveSummary` is non-empty AND `state.artifacts.executiveSummary.{path,contentHash}` are set, `adv_gate_complete gateId: "acceptance"` advances regardless of pre-existing disk-file presence. Verified by integration test that creates a change, runs it through gates without prior disk-writes for executive-summary, asserts acceptance completes. | pass | workflows.signal-handlers.test.ts 'completes acceptance state-backed with no pre-existing disk file (AC1, AC5, AC7)' — acceptance gate reaches 'done' from state.documents.executiveSummary + state.artifacts metadata with no disk file. New STATE_BACKED_ACCEPTANCE_PROOF_PATCH branch in workflows.ts reads proof from state via stateBackedAcceptanceProof(). |
| AC2 | acceptance_criterion | **AC2 — Recovery path disk inspection preserved.** When acceptance gate completion uses `recoveryMode: "poisoned_history"`, `inspectArtifactActivity` continues to verify disk content against `state.artifacts.executiveSummary.contentHash`. Verified by replay test exercising the existing recovery flow. | pass | gate.test.ts 'poisoned-history acceptance recovery writes disk projection' + recovery suite pass unchanged. git diff 8408c1cf..HEAD shows gate.ts and _recovery-writers.ts have ZERO diff — recovery path inspectArtifactActivity + hash comparison fully preserved per C2/C4. |
| AC3 | acceptance_criterion | **AC3 — Replay safety.** A versioned patch marker is added around the workflow-side change. Old histories replay deterministically; new histories use the new path. Verified by deterministic-replay test. | pass | workflows.signal-handlers.test.ts 'checks state-backed acceptance patch before the legacy disk-inspect patch (AC3)' — asserts wf.patched(STATE_BACKED_ACCEPTANCE_PROOF_PATCH) precedes wf.patched(ACCEPTANCE_EXECUTIVE_SUMMARY_PROOF_PATCH) so old histories fall through to legacy branch deterministically. workflow-bundle-boundary.test.ts + workflows.test.ts pass. |
| AC4 | acceptance_criterion | **AC4 — Legacy compatibility.** Existing changes that pre-date `removePositionalArtifactApi` and have on-disk acceptance artifacts continue to complete acceptance cleanly. Verified by a regression test fixture. | pass | archive.test.ts 'archives cleanly when the source change dir is absent (legacy/no-disk safe)' + 'includes executive-summary.md and acceptance.md from the source change dir in the bundle'. Legacy on-disk acceptance handled via same readdir sibling-copy path in createInRepoArchive. |
| AC5 | acceptance_criterion | **AC5 — Test coverage.** New regression test asserts no-disk acceptance completion through the full workflow signal path. Existing `fixGateArtifactReadiness` pattern tests extended to cover acceptance. | pass | New no-disk acceptance test through full workflow signal path (workflows.signal-handlers.test.ts). Existing acceptance pattern tests extended/updated to state-backed model (acceptance-projection test seeds state.documents; stale-hash test reframed to state-missing-content blocker). Full suite pnpm test --maxWorkers=4 exit 0. |
| AC6 | acceptance_criterion | **AC6 — Documentation parity.** The `fixGateArtifactReadiness` recovery note (acceptance carve-out) is updated to reflect the completed migration. Inline comments in `workflows.ts` explain state-backed vs recovery-path division. | pass | docs/temporal-recovery.md updated: State-backed acceptance note added to poisoned-history-recovery distinction section; 'Stuck ... gates' section title+symptom+procedure extended to acceptance/executive-summary.md. workflows.ts inline comments explain state-backed vs recovery division + STATE_BACKED_ACCEPTANCE_PROOF_PATCH rationale + deprecation plan. |
| AC7 | acceptance_criterion | **AC7 — Archive-bundle executive-summary materialization.** At acceptance gate completion (inside the new patched branch), `state.documents.executiveSummary` is written to disk at `state.artifacts.executiveSummary.path` via `writeArtifactActivity`, mirroring the existing acceptance.md projection write at workflows.ts:803. Ensures `createArchive`/`createInRepoArchive` include executive-summary.md in the bundle. Verified by integration test through acceptance + archive asserting `_executiveSummary` resolves from bundle. | pass | State-backed acceptance branch materializes executive-summary.md + acceptance.md to active change dir via writeArtifactActivity (proxy kind union extended with executiveSummary). archive.test.ts confirms createInRepoArchive readdir-copy includes executive-summary.md in bundle. workflows.signal-handlers no-disk test asserts executive-summary.md readable on disk after acceptance. |
| AC8 | acceptance_criterion | **AC8 — readAgreement Temporal-first.** `readAgreement` in `tools/contract.ts` reads Temporal `state.documents.agreement` FIRST, falling back to disk then archive — matching `readArtifact` ordering. Ideally delegates to `readArtifact(store, change.id, "agreement")`. Verified by unit test: Temporal-fresh + disk-stale agreement → minted contract uses Temporal content. | pass | contract.test.ts 'adv_contract_mint uses Temporal-fresh agreement over stale disk (AC8)' — Temporal-fresh agreement (3 contract items) used over stale disk (2 items). readAgreement now delegates to readArtifact(store, change.id, 'agreement') Temporal-first → disk → archive. No import cycle (change.ts does not import contract.ts). Unused readFile/join imports removed. |
| AC9 | acceptance_criterion | **AC9 — updateArtifacts cache invalidation.** `store-temporal/changes.ts updateArtifacts` calls `invalidateChange(changeId)` after the content-signal fan-out (before return), matching `save`/`close`/`refresh`/bulk-close. Ensures a `store.changes.get(changeId)` immediately following `adv_change_update` returns fresh `state.documents`/`state.artifacts` rather than stale cached content. Verified by a store-level test: update artifact content, then immediate `get`, assert returned `documents[kind]` equals the new content (RED today: returns stale cached value). | pass | changes.test.ts 'invalidates change cache after updateArtifacts (AC9)' — invalidateChange(changeId) now called (was 0 in RED). Single invalidateChange call added before return in updateArtifacts, matching save/close/refresh/bulk-close. Root cause of stale-contract symptom. |
| C1 | constraint | **C1 — No production-write-flow disk dual-write.** MUST NOT restore disk writes for `agreement`/`design`/`executiveSummary`/`acceptance` in the artifact-update write path (`store-temporal/changes.ts` fireContentSignalsSequentially). `no-disk-writes-invariant.test.ts` continues to hold. AC7's archive-time materialization is a separate lifecycle (one-time at acceptance gate), identical in pattern to the existing acceptance.md write at workflows.ts:803. | respected | No production-write disk dual-write added. fireContentSignalsSequentially unchanged. AC7 materialization is a separate one-time acceptance-gate lifecycle (mirrors existing acceptance.md write). no-disk-writes-invariant test passes in full suite (exit 0). |
| C2 | constraint | **C2 — `inspectArtifactActivity` interface unchanged.** Continues to exist and remains used by recovery path + archive bundle materialization. Only its USE SITE in the non-recovery acceptance gate changes. | respected | git diff 8408c1cf..HEAD shows activities.ts has ZERO diff — inspectArtifactActivity interface + implementation unchanged. Only its non-recovery acceptance use-site changed (now state-backed). |
| C3 | constraint | **C3 — Workflow replay determinism.** All workflow code changes gated behind a `patched` marker. The new `writeArtifactActivity(executiveSummary)` (AC7) sits inside the new patched branch. | respected | All workflow changes gated behind STATE_BACKED_ACCEPTANCE_PROOF_PATCH marker. AC7 writeArtifactActivity(executiveSummary) sits inside the new patched branch. AC3 patch-ordering test + workflow-bundle-boundary determinism guards pass. |
| C4 | constraint | **C4 — Bounded scope.** Scope limited to: (a) acceptance non-recovery readiness predicate, (b) archive-bundle composition (AC7), (c) `readAgreement` Temporal-first fix (AC8), (d) `updateArtifacts` cache-invalidation fix (AC9). Do NOT touch the recovery path. Do NOT touch `inspectArtifactActivity`'s implementation. Do NOT broaden AC8/AC9 into a sweeping reader/cache refactor — AC8 is `readAgreement` only; AC9 is the single missing `invalidateChange` call in `updateArtifacts`. | respected | git diff --stat 8408c1cf..HEAD: only 4 fix surfaces + tests + docs touched (9 files). Recovery path (gate.ts, _recovery-writers.ts) zero diff. inspectArtifactActivity (activities.ts) zero diff. AC8 limited to readAgreement (contract.ts +27 lines incl import). AC9 single invalidateChange call (changes.ts +9). Not broadened. |
| C5 | constraint | **C5 — Worktree isolation (P32).** All edits via the change worktree; no trunk edits. | respected | All edits made in change worktree /home/jon/.local/share/opencode/worktree/.../change/completeStateBackedGate on branch change/completeStateBackedGate. No trunk edits. Each task git-checkpointed in worktree. |
| C6 | constraint | **C6 — Test-driven.** RED evidence first; GREEN implementation; targeted + full test pass before review. | respected | TDD red→green across all 3 fix surfaces: AC9 (RED tk-846d, GREEN tk-9498), AC8 (RED tk-8699, GREEN tk-ecf0), AC1/5/7 (RED tk-926f, GREEN tk-8c1f). Each RED confirmed failing before GREEN. Targeted + full suite (exit 0) pass before review. |

