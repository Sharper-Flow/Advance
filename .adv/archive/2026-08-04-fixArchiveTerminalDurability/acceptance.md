# Acceptance

Reviewed at: 2026-08-04T21:30:15.757Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1** — Archiving a change whose disk projection already reads `archived` still delivers the terminal transition to the workflow. Regression test covers the recovery-writer-then-archive sequence that produces the short-circuit today. | pass | Guard removed on both routes (change.ts:4892 main, :4797 reconciliation); regression tests simulate recovery-writer-then-archive and assert the transition is requested on both routes; 229 tests in change.archive-phase9+change.test.ts. |
| AC2 | acceptance_criterion | **AC2** — The change workflow awaits a retried projection Activity before returning on the terminal path. Terminal state is readable from the external store after the workflow's history is gone. | pass | Terminal block awaits projectChangeState('terminal') before allHandlersFinished drain for archived AND closed (patch terminal-projection-v1); workflows.projection.itest.ts 4/4 asserts the Activity completes before workflow completion. |
| AC3 | acceptance_criterion | **AC3** — The terminal transition completes naturally with `allHandlersFinished` drained. No `terminate()` or `cancel()` on the archive path. | pass | Natural completion: condition wakes on terminal status, projection awaited, allHandlersFinished drained, return. No terminate()/cancel() added on archive path; replay + itest green. |
| AC4 | acceptance_criterion | **AC4** — Archive returns success only on evidence the transition was applied (`result()` / `describe()`), and returns an explicit unverified/failure outcome otherwise. Signal acceptance alone never yields success. | pass | proveArchiveWorkflowTransition requires writer-attributable evidence: workflow-Activity schemaVersion:2 envelope carrying archived (handlers roll back unless that write succeeded) or terminal execution status; fails closed ARCHIVE_WORKFLOW_PROOF_FAILED/AMBIGUOUS. Regression tests: liveness-only RUNNING refused; stale plain-JSON poison refused; signal acceptance alone never succeeds. |
| AC5 | acceptance_criterion | **AC5** — Re-archiving an already-terminal workflow succeeds as idempotent, proven by `describe()`. `WorkflowNotFoundError` is never treated as success without disambiguation. | pass | Terminal describe accepted as idempotent proof; WorkflowNotFoundError classified ARCHIVE_WORKFLOW_PROOF_AMBIGUOUS via classifyMutationRecoveryDecision, never success; recovery routes (audited disk proof, _recoveryMutation) exempt from proof by design. |
| AC6 | acceptance_criterion | **AC6** — `adv_change_list` and `adv_change_show` report the same status for a change with a merged archive bundle and a stale `draft` disk projection. Test asserts both surfaces together — no such test exists today. | pass | listSummary aligned with list/get via loadArchiveForActiveShadow:true; new regression in store-temporal/index.test.ts asserts list, summary, and show agree on terminal status with merged bundle + stale draft disk projection; 234-test storage sweep green. |
| AC7 | acceptance_criterion | **AC7** — `terminalFromWorkflow` reflects a real count. | pass | Hardcoded terminalFromWorkflow:0 literal removed from renderTerminalHistory; real counter path in readProjectionChangeList retained and typed; phantom fromCache field removed entirely; hydration-stat tests updated. |
| AC8 | acceptance_criterion | **AC8** — A repair path restores terminal state for a change with a merged bundle and no terminal state, including when its workflow no longer exists. Repair acts only on positive evidence; absence of evidence never completes a workflow. | pass | verifyArchivedNoWorkflowProof requires shippedTerminalProof + disk archived + default-branch reachability + bundle sha256; convergence via coordinateChangeMutation single authority; positive and negative tests in change.workflow-terminate.test.ts (262-test sweep); absence of evidence never completes a workflow (not-found stays AMBIGUOUS). |
| AC9 | acceptance_criterion | **AC9** — The reconciliation predicate no longer vetoes a bundle-archived change on its stale disk status, and skip reasons (`changeId` + `reason`) are visible in doctor output. | pass | Bundle evidence evaluated before the stale-disk veto (reconcile-terminal-workflows.ts:161-172); typed skip reasons (still_active/no_archive_evidence) recorded and surfaced in doctor output and plugin-init diagnostics; 52 tests green. |
| AC10 | acceptance_criterion | **AC10** — `commitArchiveReleaseGateProjection` surfaces `committed_unverified` to its caller instead of warning and continuing. | pass | Caller fails closed on recovered_unverified with RELEASE_GATE_PROJECTION_COMMITTED_UNVERIFIED, commit evidence, and remediation; gate never materialized done on that path; archive-gate.test.ts 53 tests. |
| AC11 | acceptance_criterion | **AC11** — All five `afterSuccess: false` sites audited; each is either justified in writing or given its compensating write, with a test that fails if a needed side-effect is silently dropped. | pass | Five-site audit table in ENGINEER_REPORT: gateCompleted UNSAFE and fixed via gate-completed-projection-v1 patch with awaited projection; four sites justified SAFE with per-site evidence; projection itest covers the fixed site. |
| AC12 | acceptance_criterion | **AC12** — Workflow-path edits are replay-safe for in-flight executions, demonstrated by a replay test. | pass | Both patches guarded by wf.patched op-name-vN constants; new legacy + current replay fixtures replay cleanly against the current bundle; replay-determinism 17/17, workflow-evolution-guard 17/17. |
| AC13 | acceptance_criterion | **AC13** — End-to-end verification includes a simulated retention-expiry (deleted-workflow) case. | pass | workflows.projection.itest.ts drives archive signals to workflow completion and asserts post-completion projection/read parity (retention-expiry shape); phase9-fails-after-bundle retry scenario in change.archive-phase9.test.ts; full sweep 13 files / 496 tests green post-rebase. |
| C1 | constraint | No `defineUpdate` in workflow-reachable code. | respected | No defineUpdate handlers added anywhere in workflow-reachable code; change remains signal-only; workflow-bundle-boundary test green. |
| C2 | constraint | `plugin/src/temporal/workflows.ts` static import graph must not reach `storage/`, `tools/`, `tool-registry.ts`, `plugin-init.ts`, or `node:*`. | respected | workflow-bundle-boundary.test.ts 8/8: workflows.ts transitive import graph does not reach storage/, tools/, tool-registry.ts, plugin-init.ts, or node:*; no new imports introduced (existing proxied Activity reused). |
| C3 | constraint | Workflow edits must remain replay-deterministic; in-flight executions must not break. | respected | All new awaited workflow calls patch-guarded (terminal-projection-v1, gate-completed-projection-v1); replay fixtures prove pre-patch histories replay without nondeterminism. |
| C4 | constraint | Temporal integration tests are `.itest.ts`, routed via `src/temporal/__tests__/with-test-env.ts`. | respected | Temporal integration coverage lives in .itest.ts routed through with-test-env; no direct TestWorkflowEnvironment construction added. |
| C5 | constraint | `pnpm run build:worker` before OOP Temporal integration tests. | respected | build:worker run before every OOP itest verification and rebuilt post-rebase (generation 79249ee6f119). |
| C6 | constraint | Verification must be end-to-end, not unit-only. | respected | AC13 e2e drives real Temporal workflows via with-test-env plus failure-injection scenarios at the tool seam; not unit-only; 496-test sweep. |
| C7 | constraint | Repair requires positive terminal evidence. | respected | Repair requires the full proof chain (shippedTerminalProof + disk archived + default-branch reachability + readable bundle); not-found alone never admits; negative test proves refusal without default-branch bundle. |
| C8 | constraint | Re-grounded idempotency must not reintroduce duplicate bundle writes or double branch deletes. | respected | Idempotency re-grounded per-side-effect: Route B reuses the existing bundle and skips cleanup while still requesting the transition; tests assert no duplicate bundle write and no double branch delete. |
| C9 | constraint | Spec deltas land in `.adv/specs/advance-workflow`. | respected | rq-archiveTerminalDurability01 added to .adv/specs/advance-workflow/spec.json with 6 scenarios, plus rq-releaseProjectionDurability01.4 and rq-terminalProjectionTruth01.3; version 1.43.2 to 1.44.0; schemas:check clean; spec.test.ts 23/23. |
| DONT1 | avoidance | Must not raise retention or enable archival as the remedy. | respected | No retention increase and no Temporal archival enabled; durability achieved via Activity-written external projection, per design. |
| DONT2 | avoidance | Must not gate a workflow transition on a projection read. | respected | The statusAlreadyArchived projection-read gate was removed, not reintroduced; transition requested unconditionally on both routes; no projection read gates any workflow transition in the new code. |
| DONT3 | avoidance | Must not leave terminal durability as caller-side best-effort. | respected | Terminal durability is workflow-owned: the terminal block awaits the projection Activity before completion; caller-side proof is verification of that durable write, not a substitute for it. |
| DONT4 | avoidance | Must not complete a workflow on absence of evidence. | respected | Repair and reconciliation act only on positive evidence (bundle on default branch + archived disk + exact not-found); absence of evidence skips with typed reason; no workflow is completed on absence. |
| DONT5 | avoidance | Must not report success for an unverifiable postcondition. | respected | Unverifiable postconditions fail closed everywhere: archive proof typed codes, release-gate committed-unverified code, gate-completed projection logged warning; no silent success stamping. |
| DONT6 | avoidance | Must not use `terminate()` or `cancel()` for archival. | respected | Natural completion only; no terminate()/cancel() on the archive path; drain preserved via allHandlersFinished. |
| DONT7 | avoidance | Must not treat a single immediate read — or bare `WorkflowNotFoundError` — as proof. | respected | Proof requires durable artifacts (Activity envelope or terminal status) after bounded retry; bare WorkflowNotFoundError classified AMBIGUOUS, never proof; single immediate read never accepted. |
| DONT8 | avoidance | Must not break replay determinism for in-flight workflows. | respected | Both patches follow the op-name-vN convention with legacy fallbacks; replay-determinism fixtures cover pre-patch histories; evolution guard green. |
| DONT9 | avoidance | Must not mutate `pokeedge-web` state. | respected | All work scoped to the advance repo; no target_path or foreign-project mutation; tests use temp dirs; pokeedge-web untouched. |
| DONT10 | avoidance | Must not mask the defect with retry/fallback in place of root-cause repair (P40). | respected | Root causes repaired (guard removal, workflow-owned durable terminal write); boundedRetry used only inside proof verification, never as a substitute for the repair; no compatibility shims added. |

