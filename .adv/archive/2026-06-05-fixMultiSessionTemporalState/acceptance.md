# Acceptance

Reviewed at: 2026-06-05T19:31:45.147Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1:** `adv_temporal_diagnose` returns `workerAlive: true` when any session's worker polls the task queue, even if the calling session owns no worker subprocess. `worker_process_alive` remains as a separate diagnostic field. | pass | `src/temporal/health-probe.test.ts` covers server poller fresh state setting worker_alive=true while worker_process_alive=false; bin/oc-test full 3524/3524 pass. |
| AC2 | acceptance_criterion | **AC2:** After session A archives a change, session B's next `adv_wip_state` call shows that change with `status: "archived"` (not `draft`) without requiring session B to restart. Memo busting extends Layer A1 to invalidate stale memo entries when an archive bundle exists for a non-terminal cached change — no TTL, just bundle-presence check. | pass | `src/storage/store-temporal/index.test.ts` covers archive-bundle memo busting for stale non-terminal cached changes; full suite pass. |
| AC3 | acceptance_criterion | **AC3:** `adv_change_archive phase9:"run"` returns within MCP timeout with a `phase9: "pending"` status when finalization is dispatched async. `phase9:"skip"` behavior is unchanged. | pass | `src/tools/change.archive-phase9.test.ts` covers explicit phase9:run async pending dispatch and unchanged phase9:skip; full suite pass. |
| AC4 | acceptance_criterion | **AC4:** `adv_change_show` includes phase9 finalization progress (`phase9_status`) when pending, observable by the agent to confirm completion. | pass | `src/tools/change.archive-phase9.test.ts` + workflow state mapping cover phase9_status persistence/readback via adv_change_show; full suite pass. |
| AC5 | acceptance_criterion | **AC5:** `adv_worktree_triage` classifies `change/*` branches with unmerged commits and no Temporal registry as `missing_from_temporal_unmerged` with resume guidance, not deletion guidance. | pass | `src/tools/worktree/triage.test.ts` covers unregistered change/* branch with unmerged commits classified missing_from_temporal_unmerged; full suite pass. |
| AC6 | acceptance_criterion | **AC6:** All existing tests pass. New tests cover each fix's behavior. | pass | bin/oc-test full: 259 files / 3524 tests pass (adv_run_test evidence, exit 0). bin/oc-test smoke pass. New tests cover all five fixes incl Fix 5. |
| AC7 | acceptance_criterion | **AC7:** `adv_change_validate` and `adv_change_archive` tolerate a peer active change whose workflow query throws (e.g. `WorkflowNotFoundError` from an evicted workflow): `loadValidationContext` skips the unrecoverable peer instead of crashing, validation still runs for the target change, and the skipped peer contributes no capabilities to conflict detection. Mirrors the existing `listResolvedChanges` per-change tolerance. | pass | Two new tests in `src/tools/change.test.ts`: validate + archive-dryRun with a peer whose changes.get throws WorkflowNotFoundError. RED reproduced crash (validate threw; archive returned VALIDATION_CONTEXT_FAILED); GREEN both pass after the loadValidationContext guard. change.test.ts 50/50. |
| C1 | constraint | Changes to `health-probe.ts` must preserve the `worker_process_alive` field (backward compat diagnostic) | respected | TemporalHealth still exposes worker_process_alive as a separate field; typecheck + full suite green. |
| C2 | constraint | Memo busting must not degrade `adv_change_list` latency >20% for typical workloads (≤50 changes) | respected | Memo busting uses per-call archive-bundle presence check; no polling TTL or repo-wide scan; store-temporal tests pass. |
| C3 | constraint | Phase9 async must not break `phase9:"skip"` path or dry-run mode | respected | Archive phase9 tests cover dry-run/skip preservation and explicit run async dispatch; full suite pass. |
| C4 | constraint | Triage sub-classification must not change behavior for `missing_from_temporal_unknown` cases | respected | Triage tests preserve legacy missing_from_temporal for unknown/merged cases while adding proven-unmerged subclass. |
| C5 | constraint | Validation-context guard must not suppress genuine validation errors for the target change; it only tolerates per-peer hydration failures (skip peer, log warning) | respected | Fix 5 guard catches only the per-peer hydration get; target-change validation errors still flow (existing `strict mode fails when validation has errors` DUPLICATE_REQUIREMENT_ID test still passes). Guard does not wrap the target change load. |
| DONT1 | avoidance | No distributed locking or cross-session event bus (out of scope) | respected | No distributed lock or cross-session event bus introduced; Fix 5 is a local try/catch guard on an existing read loop. |
| DONT2 | avoidance | No changes to Temporal workflow signal/query surface beyond what phase9 async requires | respected | Only phase9 async added a workflow signal/state (phase9StatusUpdated). Fix 5 touches no workflow signal/query surface — tool-layer guard only. Message-surface tests + full suite pass. |
| DONT3 | avoidance | No modifications to `adv_worktree_delete` or `adv_worktree_cleanup` behavior | respected | No modifications to adv_worktree_delete or adv_worktree_cleanup; Fix 5 is confined to change.ts loadValidationContext + tests. |

