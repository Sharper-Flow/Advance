# Acceptance

Reviewed at: 2026-06-26T01:59:45.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1 — No supported ATC command or agent assets.** `lgrep_search_text` or equivalent exact search for active source references shows no `.opencode/agents/adv-atc.md`, `.opencode/command/adv-atc.md`, manifest entry, or README/ADV_INSTRUCTIONS command listing advertising `adv-atc` as supported. Historical changelog/archive references may remain only if not deployed or user-facing as current support. | pass | Exact `adv-atc` source search after implementation shows only historical CHANGELOG and absence assertions in tests; source assets `.opencode/agents/adv-atc.md` and `.opencode/command/adv-atc.md` deleted; manifest/docs current support removed. Reviewer READY after fixing final spec mention. |
| AC2 | acceptance_criterion | **AC2 — Manifest and command registry clean.** `plugin/src/manifest.ts` and manifest tests no longer register or expect `adv-atc`; command count/rosters are updated intentionally. | pass | `plugin/src/manifest.ts` no longer contains `adv-atc`; `src/manifest.test.ts` asserts 26 commands, no `adv-atc` property, and absent source assets. Targeted manifest suite passed. |
| AC3 | acceptance_criterion | **AC3 — Deployment removes stale ATC.** `./scripts/deploy-local.sh --fix` removes any installed global `adv-atc.md` agent/command asset, and `./scripts/deploy-local.sh --check` reports all ADV entries present with no ATC drift. | pass | `./scripts/deploy-local.sh --fix` passed and removed stale global ATC command/agent files; explicit absence check passed for `~/.config/opencode/agents/adv-atc.md`, command/commands variants, and backup adv-atc path. |
| AC4 | acceptance_criterion | **AC4 — Remaining agents/tools clean.** `deploy-local --check` reports `adv.md` and any remaining managed agents have tool allowlists matching the plugin registry. | pass | `./scripts/deploy-local.sh --check` passed after fix: config all ADV entries present; `adv.md` allowlist matches plugin registry (70 tools). |
| AC5 | acceptance_criterion | **AC5 — Runtime docs/specs aligned.** Current docs/specs stop describing `/adv-atc` as supported; any spec references to ATC are removed, retired, or converted to explicit historical/non-supported notes with deterministic tests. | pass | Removed current ATC docs/spec requirements from ADV_INSTRUCTIONS, README, CLI surface matrix, advance-workflow spec/docs, advance-meta ATC clause, and delegation-defaults current primary lists. Targeted docs/spec/delegation tests passed; reviewer fixed final delegation-defaults utility-command mention. |
| AC6 | acceptance_criterion | **AC6 — Sub-agent safety preserved.** Tests still prevent primary agents (`adv`, `plan`, `build`) from appearing as spawnable sub-agents; removing `adv-atc` does not weaken delegation safety checks. | pass | `delegation-matrix.test.ts` and `phantom-subagent-roster.test.ts` now pin remaining primary agents (`adv`, `plan`, `build`) and continue forbidding primary-agent subagent routing. Targeted and full suites passed. |
| AC7 | acceptance_criterion | **AC7 — Verification green.** At minimum, targeted tests covering manifest/assets/deploy/delegation pass, `pnpm run check` passes, and a full suite or agreed release-equivalent suite passes before acceptance. | pass | Targeted final suite passed (263 tests), post-review targeted suite passed (158 tests), `pnpm run check` passed, and `bin/oc-test full` passed after review fix. |
| AC8 | acceptance_criterion | **AC8 — Local runtime updated.** `pnpm run build`, `./scripts/deploy-local.sh --fix`, and dotfile backup sync for changed OpenCode surfaces complete successfully. | pass | `pnpm run build`, `./scripts/deploy-local.sh --fix`, `./scripts/deploy-local.sh --check`, and dotfile backup sync completed. Toolbox backup repo shows expected `adv.md` update and `adv-atc.md` deletion; explicit backup absence check passed. |
| C1 | constraint | **C1 — Keep `adv` interactive lifecycle intact.** No removal or behavior change to the main `adv` orchestrator gates/checkpoints beyond deleting ATC references. | respected | Changes removed ATC references only; `adv` agent remains present and deploy-local check validates `adv.md` frontmatter/tool drift. No gate/checkpoint behavior changed. |
| C2 | constraint | **C2 — Keep ADV sub-agents.** Do not remove `adv-engineer`, `adv-reviewer`, `adv-designer`, `adv-researcher`, `adv-temporal-repair`, `adv-tron`, `explore`, or `general`. | respected | No ADV sub-agent files were removed; deletion limited to `.opencode/agents/adv-atc.md`, the primary ATC agent. Deploy output shows remaining agents synced; tests passed. |
| C3 | constraint | **C3 — No replacement async pipeline.** Do not add a new autonomous mode, alias, wrapper, or hidden ATC successor in this change. | respected | No alias, shim, wrapper, hidden successor, or replacement async pipeline was added. Reviewer checked clean removal/no stale legacy code and verdict READY. |
| C4 | constraint | **C4 — Source-of-truth deploy path.** Modify source repo assets and deploy through `scripts/deploy-local.sh`; do not hand-edit deployed `~/.config/opencode` or runtime plugin files as source of truth. | respected | Implementation edited source worktree and used `scripts/deploy-local.sh --fix`; no deployed file was used as source of truth. Backup sync was copied from deployed current state after deploy. |
| C5 | constraint | **C5 — Backup policy.** After deploy/config surface changes, sync non-secret current state into `backups/dotfiles/` before completion. | respected | Non-secret dotfile backup sync done: `/home/jon/toolbox/backups/dotfiles/opencode/agents/adv.md` updated and `adv-atc.md` removed. Backup absence check passed. |
| C6 | constraint | **C6 — Historical records allowed.** Changelog/archive text may remain if clearly historical and not part of current runtime/docs/spec support. | respected | Remaining `adv-atc` source hit in `CHANGELOG.md` is historical; current runtime/docs/spec/manifest references removed or converted to absence assertions. |
| DONT1 | avoidance | **DONT1 — Do not leave a stale callable `adv-atc` installed globally.** | respected | Explicit absence check passed for global `adv-atc.md` agent and command paths after deploy-local fix. |
| DONT2 | avoidance | **DONT2 — Do not keep ATC in the command manifest or command matrix as a supported command.** | respected | Manifest test asserts `COMMAND_MANIFEST` lacks `adv-atc`; CLI surface matrix row removed and targeted tests passed. |
| DONT3 | avoidance | **DONT3 — Do not remove or weaken main ADV safety checkpoints.** | respected | Main ADV gate/checkpoint instructions remain; removals are ATC-only. Reviewer found no safety checkpoint weakening. |
| DONT4 | avoidance | **DONT4 — Do not route any workflow step to a primary agent as a sub-agent.** | respected | Primary-agent routing prevention tests pass and pin remaining primaries (`adv`, `plan`, `build`) as forbidden sub-agent targets. |
| DONT5 | avoidance | **DONT5 — Do not claim local deployment complete until `deploy-local --check` is clean after `--fix`.** | respected | `./scripts/deploy-local.sh --check` passed after `--fix`; local deployment not claimed until check was clean. |

