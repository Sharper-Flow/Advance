# Acceptance

Reviewed at: 2026-08-05T01:25:52.250Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | A healthy live CLI response passes the check, including one carrying `resume_projection_state.schema_version: 1` | pass | bun test deploy-local-cli-check.test.ts: healthy live payload with nested resume_projection_state.schema_version 1 passes (exit 0). Confirmed end-to-end against the REAL installed CLI: old function FAIL, new function PASS, same binary same moment. |
| SC2 | success_criterion | A stale disk-only response still fails the check | pass | bun test: disk-only payload with top-level schema_version 1 fails (non-zero exit). Detection preserved. |
| SC3 | success_criterion | The check's failure output identifies which assertion failed and the observed value | pass | bun test: failure diagnostics section asserts observed source, observed schema_version, unparseable-output distinction, bounded length under 200 chars, and no diagnostic on success. |
| AC1 | acceptance_criterion | Given an installed CLI emitting a healthy live status payload with `source: "temporal"` and a nested `resume_projection_state.schema_version: 1`, when `scripts/deploy-local.sh --fix` runs, then the ADV CLI section reports no issues. | pass | Real-CLI proof: extracted old and new function, ran each against $HOME/.local/bin/adv. Old FAIL (exit 1), new PASS (exit 0). The false positive that has fired on every deploy since 2026-07-28 is gone. |
| AC2 | acceptance_criterion | Given a status payload carrying a top-level `schema_version: 1` (the stale disk-only shape), when the check runs, then it fails, preserving the detection `rq-advCliLocalInstall01` requires. | pass | bun test case 'rejects a stale disk-only payload with a top-level schema_version' asserts non-zero exit. The jq assertion .schema_version != 1 is root-scoped, so only a top-level version triggers it. |
| AC3 | acceptance_criterion | Given a fail-closed live error payload (`source: "temporal"` with `live: false`, `error`, and `remediation`), when the check runs, then it passes, because `rq-advCliLocalInstall01` explicitly accepts live error metadata as live-status metadata. | pass | bun test case 'accepts fail-closed live error metadata' asserts exit 0 for a live:false payload with error and remediation. The jq assertion deliberately does NOT require .live == true. |
| AC4 | acceptance_criterion | Given the check fails, when it reports, then the message names the failed assertion and the observed value rather than a generic expectation string. | pass | bun test: five diagnostic cases verify the observed source, schema_version, unparseable distinction, bounded length, and silence-on-success. Real-CLI run confirmed the diagnostic renders end-to-end ('CLI not executable at /nonexistent/adv'). |
| C1 | constraint | Must conform to `rq-advCliLocalInstall01` as written; this change fixes the implementation, not the law | respected | The jq assertion .source == 'temporal' and (.schema_version != 1) expresses rq-advCliLocalInstall01 as written: accept source temporal (success or fail-closed error), reject top-level schema_version 1 (disk-only). No spec change made or needed. |
| C2 | constraint | Must not require a new tool dependency — `jq` is already a hard dependency of `deploy-local.sh` | respected | jq appears 38 times in deploy-local.sh and is a hard precondition (check_jq at lines 790 and 1140). The fix adds one more use; no new dependency. |
| C3 | constraint | Must not weaken detection: a genuine stale disk-only payload must still fail | respected | bun test case for disk-only payload asserts non-zero exit. Negative guard in deploy-local.test.ts (/grep -q.*schema_version/) prevents the old heuristic from returning. |
| C4 | constraint | Must not modify `resume_projection_state`, which is correct and deliberately unconditional per DONT5 | respected | git diff confirms bin/adv and bin/lib/live-status.ts are untouched. Only scripts/deploy-local.sh and test files changed. |
| DONT1 | avoidance | Do not change `bin/adv` status payload shape to satisfy the validator — the payload is correct | respected | git diff shows no changes to bin/adv or any status payload builder. |
| DONT2 | avoidance | Do not delete the disk-only detection outright; `rq-advCliLocalInstall01` requires it | respected | The jq assertion includes (.schema_version != 1), preserving disk-only detection. bun test confirms disk-only still fails. |
| DONT3 | avoidance | Do not substitute `live: true` for the disk-only check — that would reject the fail-closed live error shape the spec accepts | respected | The jq assertion deliberately does not reference .live. Comment records why: the law accepts fail-closed live error metadata, so requiring liveness would trade a false positive for a false negative. |

