# Acceptance

Reviewed at: 2026-07-14T04:36:47.015Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | A change can append a valid add-operation delta for an existing or valid new capability through an agent-callable tool. | pass | adv_delta_add tool tests cover valid add deltas for existing and valid-new capability keys; full suite 5185/5185 passed. |
| SC2 | success_criterion | The writer rejects malformed capability identifiers, malformed requirement data, missing scenarios, invalid identifiers, and duplicate requirement IDs without changing existing deltas. | pass | Tool/reducer tests reject malformed capability, IDs, missing scenarios, duplicate delta IDs, duplicate requirement IDs, and existing-spec collisions before store mutation. |
| SC3 | success_criterion | The added delta survives the normal Temporal/store state path and appears in `adv_change_show` readback for that change. | pass | Temporal signal/store tests cover specDeltaAdded append, state.deltas readback, cache refresh, and deterministic signal rejection. |
| SC4 | success_criterion | Existing archive delta application consumes the written add delta without a direct spec-file workaround, including creation of a valid new capability spec when applicable. | pass | Archive activity and full-suite coverage verify existing archive delta application; implementation leaves archive writer unchanged and supports valid new capability add shape. |
| SC5 | success_criterion | Target-path trust and poisoned-history recovery conventions match other change-mutating tools. | pass | Tool mirrors target confirmation/recovery contract from change mutation tools; poisoned-history disk-projection write is explicitly refused with typed remediation. |
| AC1 | acceptance_criterion | Given a draft change and a valid existing or new capability plus add delta with a valid `rq-*` requirement and at least one Given/When/Then scenario, when `adv_delta_add` is called, then change readback contains exactly that delta under the capability. | pass | spec-delta tool tests prove valid structured add is delegated through store.specDeltas.add and persisted under capability. |
| AC2 | acceptance_criterion | Given a malformed capability identifier, malformed requirement, duplicate requirement ID, or duplicate add delta, when `adv_delta_add` is called, then it returns a typed validation error and the change delta record is unchanged. | pass | Focused tool, reducer, and store tests prove invalid or duplicate input returns refusal before state mutation. |
| AC3 | acceptance_criterion | Given a valid delta recorded through the tool, when the existing archive delta application is invoked in its normal test path, then the target spec receives the requirement exactly once and archive validation remains fail-closed. | pass | Targeted archive/tool/state suite 76/76 and full suite 5185/5185 passed; archive remains fail-closed. |
| AC4 | acceptance_criterion | Given an explicitly approved cross-project target mutation, when `adv_delta_add` is called with that target, then it follows target confirmation and does not mutate an unconfirmed project. | pass | Tool contract tests cover target-path confirmation requirements and target routing. |
| AC5 | acceptance_criterion | Given a completed or poisoned workflow where the normal signal path fails, when recovery is requested with documented evidence, then the tool follows existing audited recovery semantics or refuses without modifying state. | pass | Tool tests cover recovery field requirements; normal poisoned-history recovery refuses disk-projection writes without mutating state. |
| C1 | constraint | Use existing typed delta and requirement schemas; do not use heuristic matching. | respected | DeltaAddSchema, RequirementSchema, and CapabilityKeySchema are reused; no heuristic matching added. |
| C2 | constraint | Scope the first safe surface to `operation: 'add'` only. `modify`, `remove`, and `rename` remain deferred because they require target-resolution and conflict semantics beyond this unblocker. | respected | Public tool accepts DeltaAdd only; modify/remove/rename remain unexposed. |
| C3 | constraint | Permit a valid new capability slug because existing archive application can create a spec from add deltas; do not require the capability to preexist. | respected | CapabilityKeySchema accepts valid existing or new kebab-case slugs; archive retains new-spec creation path. |
| C4 | constraint | Use the existing Temporal-first signal/store pattern; direct disk writes are prohibited. | respected | Tool uses Temporal store specDeltas.add and specDeltaAdded signal; no direct runtime file write path. |
| C5 | constraint | Keep archive application behavior unchanged except for receiving the newly recorded delta in its existing input shape. | respected | Archive activities and spec-delta application modules were not modified by this change. |
| C6 | constraint | Preserve append-only auditability and deterministic duplicate detection. | respected | Reducer and disk fallback enforce exact duplicate delta and requirement ID rejection; payload has timestamp/actor audit fields. |
| DONT1 | avoidance | Do not extend `adv_change_update` beyond narrative artifacts. | respected | adv_change_update remains narrative-only; new standalone adv_delta_add owns mutation. |
| DONT2 | avoidance | Do not allow partial persistence, silent overwrite, or cross-change delta leakage. | respected | Validation occurs before store mutation; duplicate/replay signal rejections leave delta record unchanged. |
| DONT3 | avoidance | Do not modify global specs at tool-call time. | respected | Tool mutates change-owned deltas only; archive remains sole global spec writer. |
| DONT4 | avoidance | Do not implement full CRUD merely because issue #64 lists modify/delete; add-only is the agreed first safe surface. | respected | Tool schema is restricted to add-only DeltaAdd; no full CRUD surface was added. |
| DONT5 | avoidance | Do not bypass target-path confirmation, workflow recovery evidence, or archive validation. | respected | Target confirmation and recovery evidence are structurally required; direct disk recovery is refused. |

