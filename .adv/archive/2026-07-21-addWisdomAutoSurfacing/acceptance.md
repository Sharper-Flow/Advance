# Acceptance

Reviewed at: 2026-07-21T19:25:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | `adv_task_show` returns relevant wisdom filtered by task capabilities — zero agent initiative required for recall. | pass | adv_task_show returns _relevantWisdom (top 5 by recency after FTS filter) when contract_refs.implements non-empty. plugin/src/tools/task.ts:484-491 with null-guard coercion; tested plugin/src/tools/task.test.ts:397-495. |
| SC2 | success_criterion | SEMANTIC `error_recovery` auto-generates reviewable wisdom drafts — agent reviews at checkpoint rather than remembering to capture. | pass | SEMANTIC error_recovery auto-generates WisdomDraft via maybeCreateWisdomDraftFromErrorRecovery helper. plugin/src/utils/wisdom-draft.ts:45-68; integrated in task.ts normal-path (line 1105+) and blocked-path (line 1068+) and poisoned-history path; 37 unit tests + 5 integration tests. |
| SC3 | success_criterion | Wisdom capture rate improves over baseline (0 entries in reflection `rf-5Vpj7LP2`) without adding workflow friction. | not_applicable | Wisdom capture rate improvement is a post-deployment metric; not verifiable at acceptance boundary. Baseline established in reflection rf-5Vpj7LP2. |
| AC1 | acceptance_criterion | `adv_task_show` returns `_relevantWisdom: Wisdom[]` (top 5 by recency) when `contract_refs.implements` is non-empty; returns `[]` otherwise. | pass | plugin/src/tools/task.test.ts:397-495 — D1+D2 test asserts top-5 recency-sort wins over FTS ranking; FTS returns older-first [ws-old, ws-new], output is newer-first. Null-guard added at task.ts:484. |
| AC2 | acceptance_criterion | `adv_task_show` returns `_episodeRecallHint: { namespace: string, query: string, top_k: 3 }` when capabilities-gated. Plugin emits hint only — agent runtime executes the actual `tools.episode.recall` call (preserves `adv.md:202` policy). | pass | plugin/src/tools/task.test.ts:497-524 — capabilities-gated hint suppression test. Plugin emits hint only; no MCP call (DONT2 preserved). |
| AC3 | acceptance_criterion | `adv_task_update` with `error_recovery.attempts[]` containing at least one SEMANTIC attempt creates exactly one `WisdomDraft` on the task. | pass | plugin/src/tools/task.test.ts:1466-1629 — SEMANTIC → draft creation, TRANSIENT/empty-attempts → suppression, dedup at-most-one suggested per task. Now also covers blocked-status path (correctness-4 fix). |
| AC4 | acceptance_criterion | `WisdomDraft` lifecycle: `suggested` → `promoted` (via `adv_wisdom_add from_draft_id`) | `dismissed` (auto at checkpoint with `dismiss_reason: "auto_checkpoint"` OR explicit user dismiss with `dismiss_reason: "user_dismissed"`). | pass | plugin/src/types/tasks.test.ts:22-154 + plugin/src/utils/wisdom-draft.test.ts:293-444 — WisdomDraftStatusSchema enum locks states; lifecycle helpers enforce one-way transitions; idempotent dismiss tested. |
| AC5 | acceptance_criterion | `adv_task_checkpoint` summary includes `drafts_pending_review: number` and `drafts_auto_dismissed: number` counts. | pass | plugin/src/tools/checkpoint.test.ts:531-660 — drafts_pending_review + drafts_auto_dismissed counts verified for mixed/all-promoted/no-drafts/dismiss-failure paths. |
| AC6 | acceptance_criterion | `adv_wisdom_add` accepts optional `from_draft_id: string`. When present: validates draft exists on same task, marks draft `status: "promoted"`, pre-populates `type` and `content` from draft (agent can override). Still passes through normal evidence-policy validation. | pass | plugin/src/tools/wisdom.test.ts:262-446 — FROM_DRAFT_ID_REQUIRES_SOURCE_TASK, DRAFT_NOT_FOUND, DRAFT_ALREADY_PROMOTED, DRAFT_DISMISSED, atomic promotion, backward-compat paths. Signal-name assertion tightened (test-weak-signal-assertion nit). |
| AC7 | acceptance_criterion | `WisdomDraft` is task-scoped — cancelled tasks' drafts do not appear in change-level wisdom queries. | pass | plugin/src/tools/wisdom.test.ts — rq-wisdomAutoSurfacing01.9 AC7 invariant test asserts drafts on cancelled tasks never appear in change-level wisdom queries. Architecture-only enforcement now guarded. |
| AC8 | acceptance_criterion | Generic `[ADV:RECORD_WISDOM]` system-block nudge retired; replaced with draft-aware prompt that fires only when drafts are pending review. | pass | plugin/src/index.ts:1266-1288 — PluginState extended with pendingWisdomDraftTasks; producer in experimental.chat.system.transform queries store.tasks.list and filters wisdom_drafts for suggested status. system-block.ts consumer reads field correctly. Tests in system-block.test.ts:298-417 + system-block-ac.verification.test.ts verify assembler behavior. |
| AC9 | acceptance_criterion | All enrichment (`_relevantWisdom`, `_episodeRecallHint`, drafts) is advisory-only — never used to complete gates, override specs/contracts, or replace task evidence. | pass | plugin/src/tools/advisory-only-invariant.test.ts — static-assertion test scans change-state.ts and workflows.ts and confirms enrichment fields never read in gate-completion code paths. |
| AC10 | acceptance_criterion | Existing callers of `adv_task_show` / `adv_task_update` / `adv_wisdom_add` / `adv_task_checkpoint` unaffected when new optional fields/features absent. | pass | plugin/src/types/tasks.test.ts:167-171 — TaskSchema accepts without wisdom_drafts → undefined. plugin/src/tools/wisdom.test.ts:434-447 — without from_draft_id → single signal, no store.tasks.show. |
| C1 | constraint | Advisory-only enrichment — no gate/spec/evidence override. | respected | advisory-only-invariant.test.ts guards that enrichment never enters gate-completion paths; task.ts:475 comment documents intent. |
| C2 | constraint | Reuse-first — `adv_wisdom_list` reader, existing `error_recovery` schema, existing task show shape. | respected | Reuses adv_wisdom_list reader pattern (FTS via store.wisdom.search), existing error_recovery schema (ErrorRecoverySchema), existing TaskSchema shape (additive optional wisdom_drafts field). |
| C3 | constraint | Capabilities-gated recall hint — no hint for tasks without `contract_refs.implements`. | respected | task.ts:477-478 — implementsRefs.length > 0 gate before any enrichment work. Tested task.test.ts:497-524. |
| C4 | constraint | Bounded cost — top 5 wisdom, `top_k: 3` recall hint, no MCP call from plugin source. | respected | task.ts:486 slice(0,5) for wisdom; task.ts:496 top_k:3 for recall hint; DONT2 forbids plugin MCP call. |
| C5 | constraint | Task-scoped drafts — cancelled tasks don't pollute change wisdom. | respected | WisdomDraft lives on task signal field; dismissed drafts never enter change-level wisdom (AC7 test). Cancelled task lifecycle naturally bounds draft visibility. |
| C6 | constraint | Low-friction checkpoint — drafts auto-dismiss with audit; no blocking. | respected | checkpoint.ts:494-511 — try/catch wraps dismiss; signal failure does not block checkpoint completion. |
| C7 | constraint | No tool sprawl — extend `adv_wisdom_add` with `from_draft_id`; no new promote tool. | respected | adv_wisdom_add extended with from_draft_id; no new tool introduced. wisdom.ts:144+ implements validation + atomic promotion. |
| C8 | constraint | No gate enforcement — wisdom capture remains optional. | respected | Enrichment is advisory-only (AC9 test). Drafts are productivity aids; auto-dismiss at checkpoint is opt-out via review/promotion. |
| DONT1 | avoidance | Do not use enrichment for gate/spec/evidence override. | respected | advisory-only-invariant.test.ts static-asserts enrichment fields absent from gate-completion paths. |
| DONT2 | avoidance | Do not add plugin↔MCP client bridge (defer to separate change). | respected | task.ts:492-497 — plugin emits hint object only; no tools.episode.recall invocation in plugin source. Agent runtime owns the call. |
| DONT3 | avoidance | Do not create drafts for TRANSIENT/ENVIRONMENTAL errors (only SEMANTIC). | respected | wisdom-draft.ts:51 — maybeCreateWisdomDraftFromErrorRecovery filters to error_class === 'SEMANTIC' only. TRANSIENT/ENVIRONMENTAL/FATAL excluded. |
| DONT4 | avoidance | Do not block checkpoint on unreviewed drafts (auto-dismiss with audit). | respected | checkpoint.ts:485-511 — auto-dismiss best-effort, never blocks completion. taskCompletedSignal fires before dismiss attempt. |
| DONT5 | avoidance | Do not persist dismissed drafts as wisdom (dismissed = discarded). | respected | dismissDraft/dismissAllSuggestedDrafts set status='dismissed' (terminal); never call store.wisdom.add for dismissed drafts. Promotion requires explicit from_draft_id call. |
| DONT6 | avoidance | Do not bypass `adv_wisdom_add` evidence-policy validation when promoting from draft. | respected | wisdom.ts:144-211 — from_draft_id path still requires type+content args (no auto-populate bypass); passes through normal validation. |
| DONT7 | avoidance | Do not fire `_episodeRecallHint` when capabilities empty (avoid wasted prompt work). | respected | task.ts:477-478 — implementsRefs.length === 0 suppresses both _relevantWisdom and _episodeRecallHint emission. |

