Fixed trunk-write-firewall false-positive blocking relative file paths when an active ADV change has a registered worktree but the OpenCode session runs from trunk. Root cause: normalizeToolTargetPath resolved relative paths against session directory (trunk). Fix: resolve against worktree path when session is on main checkout and active change worktree exists. Three files changed. All 3366 tests pass.