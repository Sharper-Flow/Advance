# Contract Traceability

**Change ID:** fixArtifactReadFallback
**Contract Version:** 1
**Rigor:** strict
**Reviewed:** 2026-08-04T04:13:39.507Z

## Contract Items

| ID | Kind | Status | Evidence Policy | Evidence |
| --- | --- | --- | --- | --- |
| AC1 | acceptance_criterion | pass | test | Commit 6bf9a1d4 adds the projection-documents fallback tier to readArtifact/readArtifacts in plugin/src/tools/change/artifacts.ts plus subread.runLocalCapable so the local projection tier stays reachable after the Temporal aggregate deadline is exhausted. RED test tr_mse17xsm_c44d32b5 reproduced the production defect before the fix; GREEN tr_mse1anem_1ea6b8d4 after. Covered in plugin/src/tools/change.read-artifact.test.ts. Reviewer attempt 1 confirmed AC1-AC5 met; attempt 2 re-confirmed READY at HEAD e83a3411. |
| AC2 | acceptance_criterion | pass | test | Provenance source-tagging added in commit 6bf9a1d4 reusing the existing LoadResult.source taxonomy, so every resolved artifact read declares its serving tier. Verified by reviewer attempts 1 and 2 against plugin/src/tools/change/artifacts.ts and plugin/src/tools/change.read-artifact.test.ts. Satisfies staged spec delta dl-ArtPathTruthProj01 scenario rq-artifactPathTruth01.5. |
| AC3 | acceptance_criterion | pass | test | Tier precedence implemented as Temporal Query -> projection documents -> legacy disk artifact -> archive bundle in plugin/src/tools/change/artifacts.ts (commit 6bf9a1d4), matching the design-validated ordering (R1: legacy markdown is archive-era only, so markdown-first risked serving stale pre-migration leftovers). Precedence ordering asserted in plugin/src/tools/change.read-artifact.test.ts; verified by reviewer attempts 1 and 2. |
| AC4 | acceptance_criterion | pass | test | The batched readArtifacts path shares the same fallback tier and provenance tagging as single readArtifact in plugin/src/tools/change/artifacts.ts (commit 6bf9a1d4). Batched-path parity asserted in plugin/src/tools/change.read-artifact.test.ts and plugin/src/tools/change.test.ts; confirmed by reviewer attempts 1 and 2. |
| AC5 | acceptance_criterion | pass | test | No retry is issued against a Query already failed with a no-poller or deadline condition (commit 6bf9a1d4). Design validation confirmed a no-poller Query returns FAILED_PRECONDITION and is excluded from the Temporal SDK grpc-retry set, and Query Tasks are not persisted, so not retrying is correct. No-retry behavior asserted in plugin/src/tools/change.read-artifact.test.ts; verified by reviewer attempts 1 and 2. |
| AC6 | acceptance_criterion | pass | test | Initially a gap: after commit 27714ae0 the queue_serviceability rows were emitted only inside the post-fix recheck verification block, so a normal adv_doctor run exposed no session-typed or project-typed row. Flagged by the orchestrator to reviewer attempt 2, confirmed as a real second gap, and fixed in commit e83a3411 which publishes the diagnostic-pass rows at top level in plugin/src/tools/doctor.ts. Each row carries queueName, queueType (session|project), graded serviceability, and owningSession on the session row. Asserted in plugin/src/tools/doctor.test.ts (assertion updated from parsed.verification.queue_serviceability to parsed.queue_serviceability). |
| AC7 | acceptance_criterion | pass | test | Serviceability retains the graded ServerPollerProbeStatus vocabulary (fresh|stale|none|unavailable) from plugin/src/temporal/queue-serviceability rather than collapsing to a boolean; DoctorQueueProbe carries both the graded status and the legacy serviceable boolean for backward compatibility (commits 27714ae0, 115bb0b6). Asserted in plugin/src/tools/doctor.test.ts; verified by reviewer attempts 1 and 2. |
| AC8 | acceptance_criterion | pass | test | Reviewer attempt 1 BLOCKED this change because plugin/src/tools/doctor.ts probed only the project queue and the current session queue, leaving ended peer-session queues entirely unprobed — so the motivating outage still reported green. Remediated in commit 115bb0b6: orphaned session queues are enumerated via the existing Visibility path listOrphanSessionQueues, probed with a cap of 8 queues and concurrency 4 reusing the shared 30s poller cache, and reported as INFORMATIONAL findings carrying queue name, owning session, graded status, and the stranded change IDs. Reviewer attempt 2 verified the remediation and returned READY. Covered by new tests in plugin/src/tools/doctor.test.ts including the informational-not-unhealthy assertion and the noise-suppression case. |
| AC9 | acceptance_criterion | pass | test | The legacy single-projectId health-probe signature retains its behavior and output shape (commits 27714ae0, 115bb0b6). Asserted by a dedicated legacy-signature test in plugin/src/tools/doctor.test.ts; independently re-verified by reviewer attempt 2. |
| AC10 | acceptance_criterion | pass | test | probeDoctorQueues returns early with only the project probe when getCurrentSessionId() yields no session id, preserving the prior output shape (plugin/src/tools/doctor.ts, commit 27714ae0). Commit e83a3411 keeps this intact — the top-level queue_serviceability block is emitted only when a session probe exists, and the test asserts both parsed.verification.queue_serviceability and parsed.queue_serviceability are undefined on that path. Asserted in plugin/src/tools/doctor.test.ts. |
| AC11 | acceptance_criterion | pass | test | Automated coverage added across plugin/src/tools/change.read-artifact.test.ts (+166 lines), plugin/src/tools/change.test.ts (+110), and plugin/src/tools/doctor.test.ts covering: projection fallback read path including the orphaned-workflow case, provenance tagging, tier precedence, graded doctor session-queue probing, the stranded-queue join, noise suppression, the probe cap, and the AC9/AC10 preservation paths. Final verification by adv-verifier at HEAD e83a3411: bin/oc-test full exit 0, 541 files passed / 1 skipped, 8298 tests passed / 0 failed; pnpm run check exit 0. |
| C1 | constraint | respected | static_check | The change is read-only across both surfaces. plugin/src/tools/change/artifacts.ts adds read fallback tiers only, and plugin/src/tools/doctor.ts adds diagnostic probes only; no active artifact-content disk write path was introduced. Discovery explicitly confirmed the no-disk-writes invariant governs the write API and is not violated by a read-only change. pnpm run check exit 0 including check-test-isolation; reviewer attempts 1 and 2 both examined the full diff against trunk and raised no write-path finding. |
| C2 | constraint | respected | static_check | Projection-sourced content exposes no filesystem path; artifact metadata carries readable:false and a machine-readable source tag instead of a fabricated path (commit 6bf9a1d4). Matches staged spec delta dl-ArtPathTruthProj01 scenario rq-artifactPathTruth01.4 ("No filesystem path is exposed for the projection-sourced content"). Verified by reviewer attempts 1 and 2 and asserted in plugin/src/tools/change.read-artifact.test.ts. |
| C3 | constraint | respected | static_check | Legacy disk artifact and archive-bundle tiers are retained as tiers 3 and 4 of the precedence chain in plugin/src/tools/change/artifacts.ts (commit 6bf9a1d4); the projection tier was inserted above them, not in place of them. Preserved per spec scenario rq-artifactPathTruth01.2. Verified by reviewer attempts 1 and 2. |
| C4 | constraint | respected | static_check | No task-queue topology change and no workflow re-routing. Both surfaces are read/diagnostic only: doctor.ts probes queues via DescribeTaskQueue and enumerates them via Visibility, and artifacts.ts reads state. No call to a queue-assignment or workflow-reset path exists in the diff. Verified by reviewer attempts 1 and 2 against the full trunk...HEAD diff. |
| C5 | constraint | respected | static_check | No retry is added against a fatal-classified TemporalQueryTimeoutError (see AC5), and the aggregate read budget is unchanged. The subread.runLocalCapable addition makes the LOCAL projection tier reachable after the Temporal deadline is exhausted; it does not extend or increase the Temporal read budget. Verified by reviewer attempts 1 and 2. |
| C6 | constraint | respected | static_check | The orphaned-queue probe added in commit 115bb0b6 reuses the existing shared 30s poller cache and is explicitly bounded: a cap of 8 probed queues with an omitted count surfaced when the cap truncates, and concurrency limited to 4. No unbounded per-call DescribeTaskQueue fan-out is introduced. Reviewer attempt 2 independently verified the cap and concurrency values. Commit e83a3411 additionally corrected the capped-probe message, which previously implied a rerun would yield the next slice when the cap deterministically selects the oldest N. |
| DONT1 | avoidance | respected | review | No consolidation of session-scoped task queues onto the project queue. Session and project queues remain distinct and are probed as separate typed rows (queueType session vs project) in plugin/src/tools/doctor.ts. The change increases visibility into per-session queues rather than removing them, which reinforces rather than regresses rq-isolSessionTaskQueue04.1. Reviewer attempts 1 and 2 raised no finding. |
| DONT2 | avoidance | respected | review | The broader Query-first to disk-first hydration flip was not implemented. AC3 preserves Query-first precedence, and the projection tier is a fallback only. adv_change_show, adv_change_list, adv_gate_status, and adv_wip_state hydration ordering is unchanged; only artifact readback gained a fallback tier. Reviewer attempts 1 and 2 confirmed scope containment with no scope_drift finding. |
| DONT3 | avoidance | respected | review | No ephemeral-workflow lifecycle rework was undertaken. Workflow lifecycle, start, and termination paths are untouched by the diff; the change is confined to read fallback and diagnostics. Reviewer attempts 1 and 2 confirmed no scope drift. |
| DONT4 | avoidance | respected | review | Genuine failures are not masked: every resolved read carries a machine-readable source tag (AC2), so projection, legacy-disk, and archive-bundle fallbacks are distinguishable from an authoritative live workflow read and a caller cannot infer workflow queryability from the presence of content. This was an explicit design-validation refinement (R2) adopted precisely to prevent masking. Reviewer attempt 1 verified it while blocking on an unrelated point; reviewer attempt 2 re-confirmed. |
| DONT5 | avoidance | respected | review | The false-green signal is not replaced with a false-red one. Graded fresh/stale/none/unavailable classification (AC7) avoids treating idle-but-healthy queues as failures, ended-session queue findings are INFORMATIONAL and do not flip the system to unhealthy, and the AC8 join deliberately suppresses quiet ended-session queues that own no non-terminal work. Reviewer attempt 2 explicitly endorsed leaving those queues alone and verified the informational severity classification in plugin/src/tools/doctor.test.ts. |
| OOS1 | out_of_scope | not_applicable | not_applicable | Out-of-scope contract item. No task-queue topology redesign or workflow re-routing was performed; see C4, verified respected. |
| OOS2 | out_of_scope | not_applicable | not_applicable | Out-of-scope contract item. The full bl-HiZJbUuy disk-first hydration flip was not implemented; see DONT2, verified respected. |
| OOS3 | out_of_scope | not_applicable | not_applicable | Out-of-scope contract item. The bl-MsU7dzK0 ephemeral workflow and disk-durable record architecture was not undertaken; see DONT3, verified respected. |

## Task References

| Task | Implements | Verifies | Respects | N/A Reason |
| --- | --- | --- | --- | --- |
| tk-02ba7a18ed0e | AC1, AC2, AC3, AC4, AC5 |  | C1, C2, C3, C5, DONT2, DONT4 |  |
| tk-dec9c6c1e876 | AC6, AC7, AC8, AC9, AC10 |  | C4, C6, DONT1, DONT5 |  |
| tk-974743c3e467 |  |  | C2, C3 | Stages durable spec law for the behavior implemented and tested by tk-02ba7a18ed0e; covers no acceptance criterion directly. |
| tk-ca430b764419 |  | AC11 | C1, C5, C6 |  |
