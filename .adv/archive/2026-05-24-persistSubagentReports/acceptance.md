# Acceptance

Reviewed at: 

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1.** New MCP tool `adv_subagent_report_submit` accepts a Zod-strict typed payload with discriminated union on `agent` (`'adv-engineer' | 'adv-reviewer'` fully typed; `'adv-researcher' | 'adv-tron'` reserved literals for future extension); rejects malformed input with a typed error response. | pass | Typed Zod schemas and adv_subagent_report_submit validation implemented; subagent-report tests and full pnpm test passed. |
| AC2 | acceptance_criterion | **AC2.** Successful submission persists to store via new `subagentReportSubmittedSignal` (signal-only per `rq-changeWorkflowSignalOnly01`); cache refreshed via `fireSignalAndRefresh`; cross-project via target store helper per `rq-crossProjectTaskMutation01`. | pass | adv_change_show include.subagentReports implemented; legacy prose parsing short-circuits when persisted reports exist; change/checkpoint/task tests passed. |
| AC3 | acceptance_criterion | **AC3.** Idempotency: replayed submissions with the same `(change_id, task_id, agent, attempt)` key are deduplicated in-handler via `seenReportIds`; `attempt` is **orchestrator-supplied** via spawn-prompt Context Packet (sparse keying allowed). | pass | follow_ups, blockers, and verification consumers covered in subagent-report tests; full pnpm test passed. |
| AC4 | acceptance_criterion | **AC4.** `adv_change_show include.subagentReports` returns persisted reports for the change. Reports stored as `task.subagent_reports[]` on `TaskSchema`. | pass | Agent asset tests parse markdown payload examples through Zod; ATTEMPT anchors pinned; relevant asset suites passed. |
| AC5 | acceptance_criterion | **AC5.** Three Layer-3 field consumers wired with passing tests: | pass | .adv/specs/delegation-defaults/spec.json strengthened; .adv/specs/subagent-reports/spec.json added; spec asset tests passed. |
| AC6 | acceptance_criterion | `follow_ups[]` → `adv_agenda_add` (category=`subagent-followup`, source=`(change_id, task_id, agent)`) | pass | adv_subagent_report_submit accepts strict supported payloads and rejects malformed/unsupported payloads in subagent-report tests. |
| AC7 | acceptance_criterion | `blockers[]` → task `error_recovery` (each blocker recorded with file/line/diagnosis) | pass | subagentReportSubmittedSignal persistence path uses fireSignalAndRefresh; target_path handled through withTargetPathStore; tests passed. |
| AC8 | acceptance_criterion | `verification[]` → cross-check vs `adv_run_test` records (warning surfaced when commands/exit-codes mismatch) | pass | seenReportIds and subagentReportKey dedupe repeated submissions; ATTEMPT packet added; tests passed. |
| AC9 | acceptance_criterion | **AC6.** Asset tests mechanically parse the markdown example JSON from `adv-engineer.md` and `adv-reviewer.md` through the Zod schema; schema-markdown drift fails the test. | pass | adv_change_show include.subagentReports returns persisted reports; reports stored on task.subagent_reports; change tests passed. |
| AC10 | acceptance_criterion | **AC7.** Regression test: simulated `MessageAbortedError` after `adv_subagent_report_submit` succeeds leaves the report persisted in store and queryable via `adv_change_show include.subagentReports`. | pass | follow_ups to agenda, blockers to task error_recovery, verification warnings covered by subagent-report tests. |
| AC11 | acceptance_criterion | **AC8.** Submission failure semantics: sub-agent retries 3 times with exponential backoff on tool-call failure (Zod rejection, Temporal signal failure, network); on total failure, task `error_recovery` records the classified failure visible via `adv_change_show`. Regression tests cover Zod-rejection path and Temporal-failure path. | pass | adv-engineer and adv-reviewer asset tests parse examples through Zod and pin ATTEMPT anchors. |
| AC12 | acceptance_criterion | **AC9.** **Hard cut applied:** | pass | workflow signal-handlers tests cover persisted report after message-loss scenario; focused and full tests passed. |
| AC13 | acceptance_criterion | Fenced-JSON sentinel emission instructions REMOVED from `.opencode/agents/adv-engineer.md` and `.opencode/agents/adv-reviewer.md` (replaced by tool-call exit protocol) | pass | AC8 remediation commit 443000fe adds durable task error_recovery on invalid payload and submit-signal failure; tests for both paths pass; adv-reviewer PASS/READY. |
| AC14 | acceptance_criterion | Host-side `extractStructuredOutput` in `task.ts:738-761` and `checkpoint.ts:381-394` short-circuits when `task.subagent_reports[]` for the matching `(task_id, agent)` is non-empty | pass | Fenced sentinel instructions removed from adv-engineer.md and adv-reviewer.md; checkpoint/task legacy extraction short-circuits when subagent_reports exist. |
| AC15 | acceptance_criterion | Existing `task.structured_output` write path remains functional for non-ADV callers and legacy data — no removal in this change | pass | adv-apply, adv-review, adv-harden, adv-engineer, and adv-reviewer contracts include ATTEMPT: N; asset tests passed. |
| AC16 | acceptance_criterion | **AC10.** Context Packet specs updated to include `ATTEMPT: N` line: | pass | subagent-reports spec added with conformance_required:false; delegation-defaults rq-delDefaults05 strengthened; spec tests passed. |
| AC17 | acceptance_criterion | `.opencode/command/adv-apply.md` § Apply Context Packet | pass | SubagentAgentSchema includes reserved adv-researcher and adv-tron literals while submit tool rejects unsupported variants for v1. |
| AC18 | acceptance_criterion | `.opencode/command/adv-review.md` and `.opencode/command/adv-harden.md` equivalents | pass | pnpm test, pnpm run check, pnpm run build, workflow-bundle tests passed after remediation. |
| AC19 | acceptance_criterion | `.opencode/agents/adv-engineer.md` and `.opencode/agents/adv-reviewer.md` document how to read and pass `ATTEMPT` through to the tool call | pass | Existing TaskStructuredOutputSchema / extractStructuredOutput tests remain green in full suite and related targeted suites. |
| AC20 | acceptance_criterion | Asset tests pin the packet contract (`ATTEMPT:` anchor required) | pass | Implementation and docs use tool-call report transport only for ADV sub-agent reports. |
| AC21 | acceptance_criterion | **AC11.** `delegation-defaults rq-delDefaults05` strengthened to require typed + persisted + ingest-validated reports; new `subagent-reports` capability spec added with `conformance_required: false` for v1. | pass | ATTEMPT supplied by context packet and persisted in report identity key. |
| AC22 | acceptance_criterion | **AC12.** Forward-compat: discriminator union type literal includes `'adv-researcher' | 'adv-tron'` slots even though their schemas are not populated in this change — prevents breaking schema migration later. | pass | AC8 remediation records total failure visibly through task error_recovery after tool retries fail; tests cover malformed and Temporal failure paths. |
| AC23 | acceptance_criterion | **AC13.** Full quality gates green: `pnpm test`, `pnpm run check`, `pnpm run build`; `plugin/src/temporal/workflow-bundle-boundary.test.ts` green; `plugin/src/temporal/workflows.test.ts` green. | pass | Layer-3 consumers implemented and tested in subagent-report suite. |
| AC24 | acceptance_criterion | **AC14.** No regression: existing `TaskStructuredOutputSchema` / `extractStructuredOutput` infrastructure continues to function for cases where it still fires (non-ADV callers; legacy task data). Existing tests in `extract-structured-output.test.ts`, `task-output.test.ts` remain green. | pass | No archive rendering added; reports exposed through change/task state only. |
| C1 | constraint | **C1.** Signal only — no `defineUpdate` on the change-workflow surface (`rq-changeWorkflowSignalOnly01`). | respected | No defineUpdate introduced; workflow mutation uses signals. |
| C2 | constraint | **C2.** Cache refresh via `fireSignalAndRefresh` (`rq-cacheRefresh01`); no direct `fireSignal(handle, ...)`. | respected | Tool uses fireSignalAndRefresh for report and task update signals. |
| C3 | constraint | **C3.** Cross-project mutation through target store helper (`rq-crossProjectTaskMutation01`). | respected | target_path execution routes through withTargetPathStore and target store. |
| C4 | constraint | **C4.** Workflow-bundle boundary preserved — new types in `plugin/src/types/` must remain workflow-safe (no `storage/`, `tools/`, `node:*` imports reachable from `temporal/workflows.ts`). | respected | plugin/src/temporal/workflow-bundle-boundary.test.ts passed. |
| C5 | constraint | **C5.** Schema strictness: `.strict()` at the tool boundary (P33). Existing `TaskStructuredOutputSchema.passthrough()` left unchanged. | respected | Strict Zod boundary for new report tool; legacy structured output tests remain green. |
| C6 | constraint | **C6.** Tool boundary: new tool fits `adv-engineer` / `adv-reviewer` bounded-emit category (sibling of `adv_run_test`, `adv_wisdom_add`); no other mutators added to their allowlists. | respected | Submit tool supports adv-engineer/adv-reviewer payloads and rejects reserved unsupported agents for v1. |
| C7 | constraint | **C7.** Replay determinism: no date/random in workflow code beyond patched APIs. New signal handler follows established `signalMutation` pattern in `workflows.ts:669-678`. | respected | Reducer dedupe and signal-handler tests passed; no nondeterministic external imports in workflow bundle. |
| OOS1 | out_of_scope | **OOS1.** A2A or external inter-agent wire protocol | respected | No A2A or external protocol implementation added. |
| OOS2 | out_of_scope | **OOS2.** Streaming progress / partial-report emission during sub-agent work | respected | No streaming partial report implementation added. |
| OOS3 | out_of_scope | **OOS3.** Modifying OpenCode host semantics (`Task` tool, sub-agent spawn primitives) | respected | No OpenCode host changes required or added. |
| OOS4 | out_of_scope | **OOS4.** Typed schemas for `adv-researcher` and `adv-tron` reports (slots reserved per AC12) | respected | adv-researcher and adv-tron remain reserved literals without full schemas. |
| OOS5 | out_of_scope | **OOS5.** Removing existing `TaskStructuredOutputSchema` / `extractStructuredOutput` infrastructure | respected | Archive rendering was not added. |
| OOS6 | out_of_scope | **OOS6.** Archive flow rendering of sub-agent reports (`ARCHIVE_SUMMARY.md` / `CONTRACT_TRACEABILITY.md`) | respected | Legacy structured-output infrastructure remains, with only short-circuit when persisted reports exist. |
| OOS7 | out_of_scope | **OOS7.** Schema-breaking changes (`schema_version` stays `1.0` semantics) | respected | No hybrid sentinel+tool emission retained for ADV sub-agent report contracts. |
| OOS8 | out_of_scope | **OOS8.** Cross-vendor agent interop | respected | No unrelated agent/command transport expansion added beyond scoped ADV report contracts. |

