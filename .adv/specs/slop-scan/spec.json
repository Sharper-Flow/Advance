{
  "$schema": "https://raw.githubusercontent.com/anomalyco/oc-plugins/main/advance/plugin/schemas/spec.schema.json",
  "name": "slop-scan",
  "title": "Slop Scan",
  "purpose": "Capability: /adv-slop-scan command — detect AI-generated code quality issues including defensive overkill, deep nesting, and other slop patterns",
  "version": "1.2.0",
  "updated_at": "2026-05-22T00:00:00.000Z",
  "requirements": [
    {
      "id": "rq-ss001",
      "title": "AST-First Detection Strategy",
      "body": "/adv-slop-scan must attempt AST-based analysis before falling back to regex or heuristic methods. One primary AST tool per language is used; fallback is a brace/indent counter annotated as degraded.",
      "priority": "must",
      "tags": ["detection", "ast", "phase1"],
      "scenarios": [
        {
          "id": "rq-ss001.1",
          "title": "TypeScript/JavaScript uses ESLint rules",
          "given": [
            "A TypeScript or JavaScript project",
            "ESLint is available in the project"
          ],
          "when": "Phase 1 detection runs",
          "then": [
            "ESLint max-depth rule is used for nesting detection",
            "ESLint complexity rule is used for cyclomatic complexity",
            "Results are mapped to MAINT-004 smell ID"
          ]
        },
        {
          "id": "rq-ss001.2",
          "title": "Python uses radon",
          "given": ["A Python project", "radon is available"],
          "when": "Phase 1 detection runs",
          "then": [
            "radon cc is used for cyclomatic complexity",
            "Results above complexity_threshold are mapped to MAINT-004"
          ]
        },
        {
          "id": "rq-ss001.3",
          "title": "Go uses gocyclo",
          "given": ["A Go project", "gocyclo is available"],
          "when": "Phase 1 detection runs",
          "then": [
            "gocyclo is used for cyclomatic complexity",
            "Results above complexity_threshold are mapped to MAINT-004"
          ]
        },
        {
          "id": "rq-ss001.4",
          "title": "Fallback when no AST tool available",
          "given": ["No AST tool is installed for the detected language"],
          "when": "Phase 1 detection runs",
          "then": [
            "A brace/indent counter is used as fallback",
            "All findings from fallback have detectionMethod: 'degraded'",
            "Report includes [DEGRADED: AST tool unavailable] annotation"
          ]
        }
      ],
      "meta": {
        "legacy_ids": ["rq-slopscan01.1", "rq-slopscan01.3"],
        "merged_from": "advance-meta",
        "supersedes": "Legacy advance-meta slop-scan AST/degraded fallback coverage."
      }
    },
    {
      "id": "rq-ss002",
      "title": "Configurable Detection Thresholds",
      "body": "Detection thresholds must have smart defaults and support per-project overrides via project.json features.slop_scan block. Defaults must not produce false positives for normal single-guard or single-catch patterns.",
      "priority": "must",
      "tags": ["config", "thresholds"],
      "scenarios": [
        {
          "id": "rq-ss002.1",
          "title": "Default thresholds applied when no config present",
          "given": ["project.json has no features.slop_scan block"],
          "when": "/adv-slop-scan runs",
          "then": [
            "nesting_depth_threshold defaults to 4",
            "defensive_guard_threshold defaults to 3",
            "complexity_threshold defaults to 10",
            "ast_timeout_ms defaults to 10000"
          ]
        },
        {
          "id": "rq-ss002.2",
          "title": "Partial overrides respected",
          "given": [
            "project.json has features.slop_scan.nesting_depth_threshold: 6"
          ],
          "when": "/adv-slop-scan runs",
          "then": [
            "nesting_depth_threshold is 6",
            "All other thresholds remain at defaults"
          ]
        },
        {
          "id": "rq-ss002.3",
          "title": "AST tool timeout enforced",
          "given": [
            "An AST tool takes longer than ast_timeout_ms to complete on a file"
          ],
          "when": "Phase 1 detection runs on that file",
          "then": [
            "The tool invocation is terminated",
            "The file falls back to degraded detection",
            "Report annotates the file with [DEGRADED: AST timeout]"
          ]
        }
      ],
      "meta": {
        "legacy_ids": ["rq-slopscan01.4"],
        "merged_from": "advance-meta",
        "supersedes": "Legacy advance-meta slop-scan threshold override coverage."
      }
    },
    {
      "id": "rq-ss003",
      "title": "Defensive Overkill Detection",
      "body": "/adv-slop-scan must detect overly defensive code patterns: redundant null/undefined guard chains, paranoid pre-condition checks on the same value, and unreachable fallback branches. These are reported as QUAL-011.",
      "priority": "must",
      "tags": ["detection", "defensive", "qual-011"],
      "scenarios": [
        {
          "id": "rq-ss003.1",
          "title": "Redundant guard chain detected",
          "given": [
            "A function checks the same variable for null, undefined, and falsy in separate consecutive conditions"
          ],
          "when": "Phase 1 or Phase 2 detection runs",
          "then": [
            "A QUAL-011 finding is emitted",
            "Severity is at least medium",
            "confidence field reflects detection certainty"
          ]
        },
        {
          "id": "rq-ss003.2",
          "title": "Single guard clause is not flagged",
          "given": [
            "A function has exactly one null check before using a value"
          ],
          "when": "/adv-slop-scan runs",
          "then": ["No QUAL-011 finding is emitted for that function"]
        }
      ],
      "meta": {
        "legacy_ids": ["rq-slopscan01.2", "rq-slopscan01.5"],
        "merged_from": "advance-meta",
        "supersedes": "Legacy advance-meta defensive-overkill and clean-code coverage."
      }
    },
    {
      "id": "rq-ss004",
      "title": "Always-On Structured Output Fields",
      "body": "Every finding in JSON output must include nestingDepth, complexity, confidence, and detectionMethod fields regardless of verbosity flags. Text output must surface these fields inline.",
      "priority": "must",
      "tags": ["output", "contract", "json"],
      "scenarios": [
        {
          "id": "rq-ss004.1",
          "title": "JSON findings always include diagnostic fields",
          "given": ["A finding is produced by any detection method"],
          "when": "--json output is requested",
          "then": [
            "The finding object includes nestingDepth (number or null)",
            "The finding object includes complexity (number or null)",
            "The finding object includes confidence ('high' | 'medium' | 'low')",
            "The finding object includes detectionMethod ('ast' | 'regex' | 'heuristic' | 'degraded')"
          ]
        },
        {
          "id": "rq-ss004.2",
          "title": "Text output surfaces diagnostic fields inline",
          "given": ["A finding is produced"],
          "when": "Text report is generated (no --json flag)",
          "then": [
            "The finding block includes detectionMethod and confidence",
            "nestingDepth and complexity are shown when non-null"
          ]
        }
      ],
      "meta": {
        "legacy_ids": ["rq-slopscan01.1", "rq-slopscan01.2"],
        "merged_from": "advance-meta",
        "supersedes": "Legacy advance-meta structured diagnostic field coverage."
      }
    },
    {
      "id": "rq-ss005",
      "title": "Dead Code Tool Preference",
      "body": "knip must be the primary dead code analyzer for TypeScript/JavaScript projects. ts-prune is treated as a legacy fallback only and must be documented as such.",
      "priority": "should",
      "tags": ["detection", "dead-code", "tooling"],
      "scenarios": [
        {
          "id": "rq-ss005.1",
          "title": "knip used as primary dead code tool",
          "given": ["A TypeScript or JavaScript project", "knip is available"],
          "when": "Dead code detection runs in Phase 1",
          "then": [
            "knip is invoked as the primary tool",
            "ts-prune is not invoked unless knip fails"
          ]
        },
        {
          "id": "rq-ss005.2",
          "title": "ts-prune used only as legacy fallback",
          "given": ["knip is not available", "ts-prune is available"],
          "when": "Dead code detection runs in Phase 1",
          "then": [
            "ts-prune is invoked as fallback",
            "Report annotates findings with [LEGACY TOOL: ts-prune]"
          ]
        }
      ]
    },
    {
      "id": "rq-ss006",
      "title": "False-Positive Confidence and Actionability Control",
      "body": "/adv-slop-scan must classify findings by confidence and actionability so known clean samples stay below the accepted false-positive threshold for actionable findings while true high-confidence slop remains reported.",
      "priority": "must",
      "tags": ["false-positive", "confidence", "actionability"],
      "scenarios": [
        {
          "id": "rq-ss006.1",
          "title": "Known clean sample stays below actionable false-positive target",
          "given": [
            "A named clean regression sample containing ordinary guard clauses, public API boundary validation, example snippets, and test fixtures",
            "The accepted false-positive target is 10 percent for actionable findings"
          ],
          "when": "/adv-slop-scan runs on the sample",
          "then": [
            "High-confidence and medium-confidence actionable false positives are less than or equal to 10 percent of actionable findings",
            "Single guard clauses and single I/O try/catch blocks do not produce QUAL-011 findings",
            "Clean code below the configured nesting threshold does not produce MAINT-004 findings"
          ]
        },
        {
          "id": "rq-ss006.2",
          "title": "Known dirty sample remains detected",
          "given": [
            "A named dirty regression sample containing redundant guard chains and deep nesting at or above configured thresholds"
          ],
          "when": "/adv-slop-scan runs on the sample",
          "then": [
            "A QUAL-011 finding is emitted for redundant defensive checks",
            "A MAINT-004 finding is emitted for deep nesting",
            "The findings remain actionable when confidence is high or medium and source evidence is present"
          ]
        },
        {
          "id": "rq-ss006.3",
          "title": "Phase 1 confidence defaults are deterministic",
          "given": [
            "Phase 1 emits findings from AST, regex, or degraded fallback detection"
          ],
          "when": "Findings are normalized before reporting",
          "then": [
            "AST-backed structural findings default to confidence: 'high'",
            "Regex-only defensive-overkill findings default to confidence: 'medium' unless corroborated by same-identifier redundant guard evidence",
            "Degraded fallback findings default to confidence: 'low' unless corroborated by another detector"
          ]
        }
      ]
    },
    {
      "id": "rq-ss007",
      "title": "Low-Confidence Finding Grouping",
      "body": "/adv-slop-scan must preserve low-confidence findings for auditability while grouping them separately from actionable findings in human-readable output.",
      "priority": "must",
      "tags": ["output", "confidence", "json"],
      "scenarios": [
        {
          "id": "rq-ss007.1",
          "title": "Text output separates low-confidence findings",
          "given": ["A finding has confidence: 'low'"],
          "when": "Text output is generated",
          "then": [
            "The finding appears in a low-confidence or non-blocking section",
            "The finding is separated from actionable high-confidence and medium-confidence findings",
            "The report states that low-confidence findings are not blocking by default"
          ]
        },
        {
          "id": "rq-ss007.2",
          "title": "JSON output preserves low-confidence findings",
          "given": ["A finding has confidence: 'low'"],
          "when": "--json output is requested",
          "then": [
            "The finding remains present in findings[]",
            "The finding includes confidence, detectionMethod, nestingDepth, and complexity fields",
            "The finding includes grouping or actionability metadata that marks it non-blocking"
          ]
        }
      ]
    },
    {
      "id": "rq-ss008",
      "title": "Context-Window Suppression",
      "body": "Phase 2 heuristic scanners must treat ADV context packets, examples, task summaries, and fixture descriptions as non-scannable context unless the issue is present in an actual target source file with source evidence.",
      "priority": "must",
      "tags": ["phase2", "context", "false-positive"],
      "scenarios": [
        {
          "id": "rq-ss008.1",
          "title": "Prompt context is not a finding location",
          "given": [
            "A Phase 2 scanner prompt includes ADV change context, task summaries, examples, or known fixture snippets"
          ],
          "when": "The scanner evaluates findings",
          "then": [
            "The scanner does not emit findings against prompt context text",
            "Every finding cites a target source file and line or scoped source evidence",
            "Context packet text is used only for orientation"
          ]
        },
        {
          "id": "rq-ss008.2",
          "title": "Examples and fixtures are not treated as product defects by default",
          "given": [
            "A scanned file is an example, fixture, or test sample intentionally demonstrating a smell"
          ],
          "when": "Phase 2 heuristic detection runs",
          "then": [
            "The scanner either suppresses the finding or marks it low-confidence/non-blocking unless the file is within the explicit target product scope",
            "The confidence rationale explains why the finding is or is not actionable"
          ]
        }
      ]
    },
    {
      "id": "rq-ss009",
      "title": "Structural Correctness Bypass Detection",
      "body": "/adv-slop-scan must detect correctness-boundary overreach where heuristic inference, prose convention, fuzzy matching, or LLM/agent judgment is the sole authority for correctness, security, persistence, workflow state, gate completion, or spec compliance. Findings are reported as QUAL-012 and must distinguish advisory heuristics from heuristic-owned correctness boundaries.",
      "priority": "must",
      "tags": ["structural-correctness", "heuristics", "qual-012", "p33"],
      "scenarios": [
        {
          "id": "rq-ss009.1",
          "title": "Heuristic-owned state mutation is actionable",
          "given": [
            "Code uses fuzzy title/Jaccard/similarity matching or LLM judgment to suppress, create, mutate, or complete a persistent record",
            "No exact ref, schema validation, typed field, validator result, or explicit user confirmation controls the mutation"
          ],
          "when": "/adv-slop-scan runs",
          "then": [
            "A QUAL-012 finding is emitted",
            "confidence is high",
            "actionability is blocking when the affected scope is in the current change"
          ]
        },
        {
          "id": "rq-ss009.2",
          "title": "Advisory triage heuristic is not actionable",
          "given": [
            "A heuristic only ranks, suggests, or labels a candidate for later confirmation",
            "Exact refs, typed fields, validators, or explicit user confirmation own the final decision"
          ],
          "when": "/adv-slop-scan runs",
          "then": [
            "No blocking QUAL-012 finding is emitted",
            "Any concern is omitted or grouped as low-confidence/non-blocking with a rationale"
          ]
        },
        {
          "id": "rq-ss009.3",
          "title": "Untrusted input reaches logic before recognition",
          "given": [
            "Code processes untrusted input in business logic before parser/schema/allowlist recognition and normalization"
          ],
          "when": "/adv-slop-scan runs",
          "then": [
            "A QUAL-012 or security finding is emitted",
            "The fix recommends moving recognition/normalization to the boundary before processing"
          ]
        }
      ]
    },
    {
      "id": "rq-ss010",
      "title": "Deletion Candidate Taxonomy",
      "body": "/adv-slop-scan must define deletion-candidate subtypes under MAINT-003 for unused dependency, unused export, unused file, unreachable branch, uncallable private symbol, and impossible feature-flag path. These are findings and review inputs only, not automatic deletion actions.",
      "priority": "must",
      "tags": ["dead-code", "deletion-candidate", "taxonomy", "maint-003"],
      "scenarios": [
        {
          "id": "rq-ss010.1",
          "title": "Deletion candidates use MAINT-003 subtypes",
          "given": ["/adv-slop-scan evaluates dead-code, bloat, or reachability signals"],
          "when": "A candidate maps to unused dependency, unused export, unused file, unreachable branch, uncallable private symbol, or impossible feature-flag path",
          "then": [
            "The finding is labeled as a MAINT-003 deletion_candidate subtype",
            "The finding includes confidence, detectionMethod, source evidence, grouping, and actionability",
            "The report wording does not imply automatic deletion"
          ]
        },
        {
          "id": "rq-ss010.2",
          "title": "False-positive surfaces are protected",
          "given": ["A candidate touches public exports, generated files, tests, fixtures, command modules, plugin registration surfaces, prompt context, examples, or task summaries"],
          "when": "Deletion-candidate actionability is assigned",
          "then": [
            "The candidate is low-confidence or non-blocking unless target source evidence proves the deletion is safe",
            "The rationale names the protected surface or source evidence used"
          ]
        }
      ]
    },
    {
      "id": "rq-ss011",
      "title": "Deletion Safety and Actionability Boundary",
      "body": "/adv-slop-scan must never treat heuristic-only, regex-only, text-only, or single-tool unused-code guesses as actionable removal proof. Uncertain deletion candidates must be grouped as low-confidence / user-review findings.",
      "priority": "must",
      "tags": ["dead-code", "actionability", "false-positive", "p33"],
      "scenarios": [
        {
          "id": "rq-ss011.1",
          "title": "Heuristic-only deletion guesses are user-review only",
          "given": ["A deletion candidate is supported only by text search, regex-only signals, heuristic inference, or agent judgment"],
          "when": "Report grouping is assigned",
          "then": [
            "The candidate appears only in a low-confidence / user-review section",
            "actionability is non-blocking",
            "The report states that heuristic-only or text-only unused-code guesses are not actionable removal proof"
          ]
        },
        {
          "id": "rq-ss011.2",
          "title": "Structural evidence required before actionable removal proof",
          "given": ["A deletion candidate has source evidence from tools, entrypoint/config checks, exact symbol/file/dependency references, or typed reachability roots"],
          "when": "Actionability is assigned",
          "then": [
            "The candidate may be actionable only when structural evidence is present",
            "No single external tool is the sole correctness authority for deletion safety",
            "The finding recommends verification before removal rather than direct deletion"
          ]
        }
      ]
    },
    {
      "id": "rq-ss012",
      "title": "Slop Scanner Coverage Report",
      "body": "/adv-slop-scan must summarize skipped, degraded, timed-out, absent, and missing detectors in normal text output, and expose detailed scanner coverage in JSON metadata.",
      "priority": "must",
      "tags": ["coverage", "output", "json", "degraded"],
      "scenarios": [
        {
          "id": "rq-ss012.1",
          "title": "Text output summarizes coverage gaps",
          "given": ["A detector is skipped, absent, timed out, degraded, or missing for a deletion-candidate subtype"],
          "when": "Text output is generated",
          "then": [
            "The report includes a scanner coverage summary",
            "Skipped and degraded detectors are visible without verbose mode",
            "Missing detector coverage is surfaced as a coverage gap, not hidden"
          ]
        },
        {
          "id": "rq-ss012.2",
          "title": "JSON output includes coverage details",
          "given": ["--json output is requested"],
          "when": "Report output is generated",
          "then": [
            "The JSON object includes coverage.skippedDetectors",
            "The JSON object includes coverage.degradedDetectors",
            "The JSON object includes coverage.falsePositiveProtections"
          ]
        }
      ]
    }
  ]
}
