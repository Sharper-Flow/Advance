# Acceptance

Reviewed at: 2026-07-23T06:55:10.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | **SC1** A fresh session/agent can determine "next actionable work" across all active Epics and same-project changes via a single read call, with no hand-written prose resume map. | pass | Live projection call, status, and CLI tests pass. |
| SC2 | success_criterion | **SC2** Adding a dependency cycle, self-edge, duplicate ref, or unresolved same-project target is structurally impossible — rejected at every edge-writing mutation ingress (shell-add, shell-promote, change-create) with a typed, machine-readable error. | pass | D3 integration covers all edge ingresses. |
| SC3 | success_criterion | **SC3** An already-active change's gates and archive path are immune to dependency shifts introduced after activation. | pass | Create/promote-only enforcement is pinned. |
| SC4 | success_criterion | **SC4** The projection surfaces cross-Epic blockers as redirects rather than falsely reporting an Epic as stalled. | pass | Cross-Epic redirect tests pass. |
| SC5 | success_criterion | **SC5** Existing Epics, changes, and shells with no `blocked_by` / `same_project_dependencies` (the entire pre-change corpus) continue to function identically — no migration, no flag day. | pass | Legacy no-edge corpus parses with defaults. |
| AC1 | acceptance_criterion | **AC1** `WorkNodeRefSchema` (discriminated `epic_entry` | `change`) is added to a new `plugin/src/types/work-graph.ts`. `EpicShellEntrySchema.blocked_by` and `ChangeSchema.same_project_dependencies` are added as `WorkNodeRef[].default([])`. Reproduction test: a round-trip parse of a pre-change Epic/change JSON (no `blocked_by` field) succeeds and yields `blocked_by: []`. | pass | Schema/default and legacy parse tests pass. |
| AC2 | acceptance_criterion | **AC2** Mutation signal adding a `blocked_by` edge performs iterative DFS three-color cycle detection (extracted from `plugin/src/validator/merge-order.ts:139-208` to a shared helper, converted from recursive to iterative to satisfy DDC2) and rejects with `DependencyCycleError` carrying the explicit closed cycle path. Test: a 2-node cycle A→B→A is rejected with `[A,B,A]` path. | pass | Iterative closed-path cycle and scale tests pass. |
| AC3 | acceptance_criterion | **AC3** Mutation signal adding a self-edge, duplicate ref, or unresolved same-project target ref is rejected atomically with `InvalidWorkNodeRefError` / `UnresolvedDependencyError`. No partial edge is persisted on rejection. Validation fires at EVERY edge-writing mutation ingress: `adv_epic_add_shell`, `adv_epic_promote_shell`, `adv_change_create`, and any future edge-bearing mutation. | pass | Behavioral ingress validation tests pass. |
| AC4 | acceptance_criterion | **AC4** `adv_epic_promote_shell` refuses to promote a shell whose same-project `blocked_by` prerequisites are nonterminal; rejection names the blocking prerequisite(s). Same for `adv_change_create` with `same_project_dependencies` referencing nonterminal same-project changes. | pass | Typed nonterminal blocks pass. |
| AC5 | acceptance_criterion | **AC5** An already-active change with `same_project_dependencies` passes every gate and archives normally even if a prerequisite's status changes after activation (D3 invariant — enforcement is create/promote-time only). | pass | Active paths remain free of dependency-shift gate blocks. |
| AC6 | acceptance_criterion | **AC6** `adv_resume_projection` (new read tool, ADV tool `class: orchestrator` — pure-read, no mutation surface; the "reader" concept is a sub-agent lane name, not a tool-class enum value) returns `{ generated_at, scope, ordered_next, actionable[], blocked[], active[], redirects[], diagnostics }`. A node is in `actionable` iff all hard prerequisites are terminal AND the node is not already active. | pass | Current-session tool call returns typed projection within budget. |
| AC7 | acceptance_criterion | **AC7** Cross-Epic blocker emits a `CrossEpicRedirect` (source_epic_id, target_epic_id, blocker node) instead of falsely marking the source Epic stalled. | pass | Cross-Epic redirects verified. |
| AC8 | acceptance_criterion | **AC8** Projection generation is pure read: no signal fired, no state mutation. `diagnostics: { cycles, unresolved_refs }` surfaces any cycles or unresolved refs in legacy/partial state without failing the read. | pass | Pure kernel and read-only tool behavior verified. |
| AC9 | acceptance_criterion | **AC9** `adv_status view:"summary"` next-step recommendations consume `resume_projection.ordered_next` / `actionable`. `/adv-coordinate` replaces heuristic sequencing notes with structural projection reads. `/adv-triage` portfolio "what's next" consumes the projection. `.opencode/command/adv-{coordinate,status,triage}.md` updated to specify projection consumption; existing command boundaries preserved (read-first, approval-gated, order-advisory). | pass | Status and command consumers verified. |
| AC10 | acceptance_criterion | **AC10** `bin/adv` (status / epic list / dashboard) renders actionable-next + blockers + redirects from the projection. No new CLI verb introduced (researcher-confirmed extend-existing pattern). | pass | Existing bin surfaces consume projection. |
| AC11 | acceptance_criterion | **AC11** `Epic.progress.next_entry_id` remains on the schema, computed from the projection (option a — KEEP AS ADVISORY HINT). External readers see no breaking change; the field is a derived view of the canonical projection. | pass | Local advisory skips blocked entries; external ordered_next is full authority. |
| AC12 | acceptance_criterion | **AC12** After projection parity is verified against existing roadmap examples (test fixture comparing generated `ordered_next`/`actionable` against the manually-curated roadmap rows), `bl-jernU-SM` is auto-archived with an evidence pointer to the verification test. Operator action required: none. | pass | Parity passes; bl-jernU-SM archived. |
| AC13 | acceptance_criterion | **AC13** Reproduction tests cover: 2-node cycle rejection, self-edge rejection, unresolved same-project target rejection, edge validation fires at every ingress (shell-add, shell-promote, change-create), shell-promotion blocked by nonterminal prereq, change-create blocked by nonterminal prereq, already-active change passes gates post-dep-shift, cross-Epic redirect emission, naive-corpus backward-compat (no `blocked_by` → `[]`), iterative-DFS 10k-node scale parity (no stack overflow). | pass | 149 targeted tests cover reproduction cases. |
| AC14 | acceptance_criterion | **AC14** Boundary preservation: no new mutation verbs added to `/adv-coordinate`, `/adv-status`, `/adv-triage` — they remain read-only / approval-gated / order-advisory respectively. The new tool `adv_resume_projection` is ADV tool `class: orchestrator` (pure-read; "reader" is a sub-agent lane name, not a valid tool-class enum value — agreement text corrected during design validation). | pass | No new mutation verbs; tool is pure-read orchestrator. |
| C1 | constraint | **C1** Schema is additive only. `blocked_by` / `same_project_dependencies` use Zod `.default([])`. No data migration, no backfill, no flag day. | respected | Additive defaults only. |
| C2 | constraint | **C2** Cycle detection reuses the Kahn + iterative DFS-three-color pattern from `plugin/src/validator/merge-order.ts:139-208` (extracted to a shared helper, converted from recursive to iterative DFS to satisfy DDC2 scale requirement). Returns closed cycle path `[A,B,A]`. | respected | Shared iterative Kahn/DFS helper. |
| C3 | constraint | **C3** D3 enforcement is at mutation signal time (create/promote) only, never at gate/archive time. | respected | D3 boundary integration passes. |
| C4 | constraint | **C4** Cross-project dependencies (`ExternalDependencySchema`) are NOT promoted to hard-blocking in this release; they stay advisory V1. | respected | External advisory behavior unchanged. |
| C5 | constraint | **C5** No force-override verb. User removes/reclassifies the edge or finishes the prerequisite. | respected | No force override. |
| C6 | constraint | **C6** A change linked into an Epic is ONE canonical `change` node; its Epic entry is display/membership metadata, not a second graph node. | respected | Canonical node dedup verified. |
| C7 | constraint | **C7** Seam with addDurableChangeSeeds (Capability B): A consumes canonical node identity + lifecycle only; never touches `FutureWorkContextPacketSchema` or context-carrying promotion. Dependencies never alter packet content. | respected | No future-work context changes. |
| C8 | constraint | **C8** Projection scope is current-project only (`scope: { project_id, epic_ids }`). Cross-project fan-out is `/adv-coordinate` ownership, unchanged. | respected | Current-project scope retained. |
| C9 | constraint | **C9** `next_entry_id` field stays on schema as projection-derived advisory (option a — KEEP); no breaking-change spec delta removing it. | respected | next_entry_id advisory retained. |
| DONT1 | avoidance | **DONT1** Do NOT use `ExternalDependencySchema` for same-project hard deps — its advisory contract differs; new `same_project_dependencies` field with new `WorkNodeRefSchema` instead. | respected | Dedicated WorkNodeRef fields used. |
| DONT2 | avoidance | **DONT2** Do NOT add a force-override verb for hard dependency blocks (researcher-rejected; prevents bypass-becomes-normal). | respected | No force override. |
| DONT3 | avoidance | **DONT3** Do NOT make same-project edges advisory-only (researcher-rejected; would not eliminate the resume-map symptom). | respected | Hard promotion/create blocks proven. |
| DONT4 | avoidance | **DONT4** Do NOT block gate transitions or archive of already-active changes on dependency shifts (D3 invariant; would wedge in-flight work). | respected | No gate/archive dependency enforcement. |
| DONT5 | avoidance | **DONT5** Do NOT persist dual-direction edges or maintain a separate mutable adjacency list — node-owned `blocked_by` only, reverse `blocking` derived in read projections (matches Asana/GitHub/ADV task pattern). | respected | Node-owned edges and derived reverse view. |
| DONT6 | avoidance | **DONT6** Do NOT unify backlog JSONL / Epic-shell projection / active Change into a single future-work entity (researcher-rejected; different lifecycle/storage semantics). | respected | Lifecycle models remain separate. |
| DONT7 | avoidance | **DONT7** Do NOT remove `next_entry_id` field entirely or stop populating it — KEEP as projection-derived advisory (option a) to preserve external-reader back-compat (SemVer-staged deprecation path remains available if friction surfaces). | respected | next_entry_id remains populated. |
| DONT8 | avoidance | **DONT8** Do NOT introduce a new `bin/adv next` verb in this release — extend existing roadmap/epic-list/dashboard render (researcher-confirmed pattern; revisit shorthand if real friction appears). | respected | No new CLI next verb. |
| DONT9 | avoidance | **DONT9** Do NOT classify `adv_resume_projection` as tool `class: reader` — no such enum value exists in `tool-role-policy.ts:27` (`ToolRoleClass = "orchestrator" | "operator-only" | "dual"`). Use `class: orchestrator` (matches existing pure-read tools `adv_status`, `adv_change_list`, `adv_change_show`, `adv_spec`). | respected | Tool class is orchestrator. |
| OOS1 | out_of_scope | **OOS1** Cross-project hard blocking (stays advisory V1; future release). | missing |  |
| OOS2 | out_of_scope | **OOS2** `FutureWorkContextPacketSchema`, context-carrying promotion, launcher future-work surfacing (owned by addDurableChangeSeeds). | missing |  |
| OOS3 | out_of_scope | **OOS3** The 33→8 zombie-workflow reconcile (`bl-bEd2-Jbi`) — separate operational track. | missing |  |
| OOS4 | out_of_scope | **OOS4** Jira-style sprints / estimates / assignees. | missing |  |
| OOS5 | out_of_scope | **OOS5** Force-override of dependency blocks. | missing |  |
| OOS6 | out_of_scope | **OOS6** Backlog JSONL storage changes (backlog stays JSONL; A does not touch it). | missing |  |
| OOS7 | out_of_scope | **OOS7** Contract-mint / review-matrix schema changes. | missing |  |

