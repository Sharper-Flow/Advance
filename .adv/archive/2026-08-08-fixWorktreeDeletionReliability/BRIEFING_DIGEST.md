# Archive Briefing Digest

**Change ID:** fixWorktreeDeletionReliability
**Title:** Fix worktree deletion reliability
**Status:** archived
**Generated:** 2026-08-08T16:42:09.192Z

## Identity Anchors

- CHANGE
- STATUS
- TERMINAL_GATE_SUMMARY
- Origin: discovery

## Archive Digest

**Status:** archived

| Gate | Status |
| --- | --- |
| proposal | done |
| discovery | done |
| design | done |
| planning | done |
| execution | done |
| acceptance | done |
| release | done |

## Epic Context

No Epic membership

## Durable Facts

Showing 100 of 171 durable facts (71 omitted).

- **[report_follow_up]** follow_ups: Design-phase: specify the abortable-delegate helper that wraps execFileGitCb with AbortSignal + process-group kill and verify no git subprocess or inter-git async work survives the deadline (regression test: start a slow git op, let budget expire, assert no filesystem mutation within a grace window).
- **[report_follow_up]** follow_ups: Design-phase: decide whether to reuse acquireFileLock as the per-repo cleanup lock or harden git-worktree-flock with isProcessAlive + stale-reaping; either way close the permanent-block liveness bug.
- **[report_follow_up]** follow_ups: Design-phase: add porcelain locked/prunable fields to the census fact type and the consolidated parser; type a REMOVE_FAILED-on-locked outcome distinctly from dirty/unmerged.
- **[report_follow_up]** follow_ups: Verification: confirm whether advWorktreeDelete currently calls git worktree prune in the delete path (prune is invoked at index.ts:976 in the create path); the new executor should prune before re-reading facts on apply to reject drift from half-removed administrative entries.
- **[report_follow_up]** follow_ups: Minor: packet SCOPE KEY anchor 'discovery:deletion-guarantees' was normalized to 'researcher:deletion-guarantees' to satisfy the report schema's agent-scoped scope_key pattern.
- **[research_citation]** sources: git-worktree(1) official manpage: Authoritative semantics for list/remove/prune/lock/repair. remove = clean-only unless --force; locked needs --force twice; main cannot be removed. prune cleans $GIT_DIR/worktrees entries for missing worktrees (--expire age, gc.worktreePruneExpire auto). lock structurally prevents prune/move/delete. porcelain fields: worktree/HEAD/branch refs/heads//detached/locked/prunable; -z for NUL-termination. No atomic compare-and-remove primitive; remove takes only a path and is unconditional (fails safe on dirty/locked). (https://git-scm.com/docs/git-worktree)
- **[research_citation]** sources: git-worktree(1) kernel.org mirror: Confirms lock prevents prune/move/delete and --force twice overrides for remove. Confirms administrative files live in $GIT_DIR/worktrees/<name> with HEAD/gitdir/locked. repair reestablishes corrupted/outdated links. (https://www.kernel.org/pub/software/scm/git/docs/git-worktree.html)
- **[research_citation]** sources: Node.js v24 child_process API: timeout(ms)+killSignal(default SIGTERM, ADV uses SIGKILL) sent to direct child only. signal:AbortSignal -> abort() sends killSignal. Explicit: 'On Linux, child processes of child processes may not be terminated when attempting to kill their parent.' subprocess.kill() returns boolean = signal delivered, not death confirmed. execFile supports signal option. (https://nodejs.org/docs/latest-v24.x/api/child_process.json)
- **[research_citation]** sources.omitted: 8 additional sources omitted (bounded to first 3)
- **[archive_only_evidence]** architecture_assessment: The proposal's direction (Git-census-authoritative, bounded two-phase plan/apply, single end-to-end budget, registry-reconciled-after-proof, fail-closed typed evidence) is correct and aligns with authoritative Git semantics AND the codebase's existing proven patterns (detach.ts two-phase; census.ts Git-authoritative reconciliation; state.ts durable mark/clear pending-deletes). Git offers no atomic compare-and-remove primitive, so the CAS-via-revalidation-under-lock approach is the only sound option. Key implementation constraints a design must honor: (1) per-repo cleanup lock must add fail-safe PID-liveness + stale reaping (reuse isProcessAlive/acquireFileLock; the current git-worktree-flock has a liveness bug that can permanently block all deletions); (2) the 'no mutation after timeout response' guarantee requires threading AbortSignal into EVERY git subprocess (git-binary helpers currently lack signal plumbing) and replacing/augmenting non-cancelling withTimeout with an abortable deadline, OR ensuring inner-subprocess Node timeout always fires before outer budget (current design approximates this but is not airtight for async JS work between git calls); (3) for true subprocess-tree kill use detached:true + negative-PID process-group kill (test.ts already does this; deletion path does not) - though git worktree remove rarely forks long-lived children so risk is low; (4) the planner should parse porcelain locked/prunable fields (current census/detach parsers omit them -> blind to Git-native locks, fails safe as REMOVE_FAILED but untyped); (5) consolidate the three duplicated porcelain parsers (census.ts, porcelain-parser.ts, detach.ts) into one; (6) treat Git half-state after a SIGKILL'd remove by running git worktree prune before census and offering git worktree repair for corruption. Risks of Git-as-authority are real but manageable: Git half-state, advisory-only flock (manual git remove bypasses it), and local-only 'merged' checks (mitigated by PR-merge evidence fetch in verifyBranchIntegration). No conflict found with the proposed direction.
- **[report_follow_up]** follow_ups: Planning: specify the abortable runner that wraps execFileGitCb/execFileGitAsync with one end-to-end AbortController + detached process-group kill + await-exit confirmation; add a regression test (start a slow git op, expire the budget, assert no filesystem mutation within a grace window).
- **[report_follow_up]** follow_ups: Planning: decide lease primitive - reuse acquireFileLock (fs.ts, PID+timestamp+30s stale reap) or harden git-worktree-flock with isProcessAlive + stale-reap; either way close the permanent-block liveness bug and replace unconditional release with ownership-checked release.
- **[report_follow_up]** follow_ups: Planning: enumerate which branches require terminal-status proof (change/* via durable store readback) vs merged+clean-only (release/*, ad-hoc) and encode in the planner's integration-proof step; confirm verifyBranchIntegration's terminalStatusReader path is reused.
- **[report_follow_up]** follow_ups: Planning: converge drainPendingDeletes (index.ts:2836) and archive-helpers/archived-branch-cleanup.ts onto the shared planner/executor so PLAN_REQUIRED does not break internal automated callers; decide explicit-exemption vs single-call plan+apply orchestration.
- **[report_follow_up]** follow_ups: Planning: consolidate the three porcelain parsers (census.ts parseWorktreePorcelain, porcelain-parser.ts, detach.ts parseGitWorktreePorcelain) into one NUL-aware parser; add fixtures for detached/locked/locked-with-reason/prunable.
- **[report_follow_up]** follow_ups: Planning: confirm git worktree prune runs before fact re-read on apply (currently prune is invoked in the create path at index.ts:976, not delete); surface git worktree repair as the distinct recovery path for corrupted gitdir links.
- **[report_follow_up]** follow_ups: Instrumentation: agreement.md / AC1-AC10 content was not retrievable via adv_change_show artifact includes (only proposal/problemStatement sourced from disk); validation used the proposal Success boundary. Orchestrator should confirm the persisted agreement artifact is readable before acceptance, or re-attach it for the next validation pass.
- **[research_citation]** sources: git-worktree(1) official manpage (git-scm.com): remove = clean-only unless --force; locked needs --force twice; main not removable. prune removes only MISSING worktrees' admin files, respects locked + --expire/gc.worktreePruneExpire. lock prevents prune/move/delete. repair reestablishes corrupted gitdir links (distinct from prune). No atomic compare-and-remove; remove is path-based and destructive. (https://git-scm.com/docs/git-worktree)
- **[research_citation]** sources: git/git builtin/worktree.c remove_worktree (source): remove_worktree: checks lock reason (force<2 + locked -> die), validates, then delete_git_work_tree + delete_git_dir with 'continue on even if ret is non-zero, there's no going back from here' -> confirms remove is non-atomic and destructive; no CAS primitive. (https://github.com/git/git/blob/dd9609a12e83969be6536853b2846866dafdfc98/builtin/worktree.c)
- **[research_citation]** sources: git commit 862c723 (porcelain locked/prunable): list --porcelain annotates locked (with optional escaped reason) and prunable; confirms the porcelain fields the design's NUL parser must handle. -z NUL-terminates. (https://github.com/git/git/commit/862c723d18ddc6be183abc450a9fb220ba4efb0b)
- **[research_citation]** sources.omitted: 10 additional sources omitted (bounded to first 3)
- **[archive_only_evidence]** architecture_assessment: VERDICT: VALIDATED (no design-direction conflict against authoritative Git/Node semantics, the proposal Success boundary, or proven codebase patterns). Git offers NO atomic compare-and-remove (remove_worktree: 'there's no going back from here'), so CAS-via-revalidation-under-lock is the only sound option; the self-contained expiring token + apply-revalidate-all-facts-under-lock is exactly the proven detach.ts requestId pattern generalized, and is sufficient WITHOUT server-side plan persistence because the lock + Git-census re-read is the authority (token is a structured carrier, not the authority) and crash recovery is already handled by the durable pending-delete mechanism (state.ts). The 7.5s end-to-end budget armed before target resolution is feasible: a lightweight resolver can read Git census (scanGitWorkspaceFacts needs no full store init) + reconcile (pure) within the budget; uncontended git ops are 5-16ms and store scan ~0.6-0.9s; under contention apply legitimately retain-and-retries (correct fail-closed, not a bug) while still returning typed terminal output within the host budget. prune-before-revalidate is SAFE: prune removes only MISSING worktrees' admin entries (respects locked + expire), never a present worktree, so it cleans half-removed SIGKILL'd state so the revalidation census reflects Git truth; repair is a separate recovery path for corrupted gitdir links (not half-state removal). Registry-advisory is correct and matches census.ts's existing source:'git_census' reconciliation; it relaxes DISCOVERY (which worktrees exist, enabling release/* deletion = causal path 3 fix) but MUST NOT relax SAFETY (merged-into-default + clean + in-use + terminal-for-change/*). The liveness-aware owner-token lease direction is correct and closes the CONFIRMED git-worktree-flock liveness bug (hardcoded reason + unconditional release). Process-group cancellation is achievable on Linux (primary platform) via detached:true + negative-PID group kill + await child-exit confirmation; degrades to direct-child forceful kill on Windows (no process groups), acceptable since git rarely forks long-lived children and ADV is Linux-first. No AC/Success-boundary item is compromised; the highest-risk requirement ('no subprocess or mutation continues after timeout response') is COMMITTED to by the design (abortable runner + confirmed termination) but NOT yet present in git-binary.ts, making it an implementation risk rather than a design conflict.
- **[report_follow_up]** follow_ups: Reconcile the one related index-delete test failure after parallel task tk-aaf30c4c2e56 lands; the failure is in concurrent Git-census/deletion-contract behavior, not the deadline/lease foundation.
- **[report_follow_up]** follow_ups: Wire createWorktreeOperationContext and runAbortableProcess into the planner/executor stages in the subsequent tasks; this task intentionally provides the foundation only.
- **[unresolved_action]** required_main_agent_actions: Review and reconcile the related index-delete expectation against tk-aaf30c4c2e56's canonical census behavior before acceptance.
- **[unresolved_action]** required_main_agent_actions: Integrate the exported operation context/process runner into deletion target resolution and destructive apply so the end-to-end cancellation contract is exercised.
- **[archive_only_evidence]** decisions: Created a request-local operation context with a 7.5s deadline and 500ms response reserve. — The deadline must be armed before target resolution and shared by every later stage; injected timestamps keep deadline arithmetic and stage evidence deterministic.
- **[archive_only_evidence]** decisions: Implemented POSIX process-group cancellation using detached children, TERM then KILL, and close-awaiting termination. — Killing only the direct child leaves grandchildren able to mutate after timeout; group signaling plus awaiting close provides the destructive-subtree guarantee.
- **[archive_only_evidence]** decisions: Changed the repository flock to an owner-token lease with liveness-aware dead-owner reclaim and token-matched release. — PID-only release can delete a successor's lock, while stale lock files need safe reclamation without reclaiming live peers.
- **[archive_only_evidence]** decisions: Kept existing Git helper call sites compatible while adding optional signal and remainingMs support. — Existing timeout/callback/spawn callers continue to work, while request-scoped cancellation can now bound Git operations.
- **[archive_only_evidence]** verification: pnpm exec vitest run src/utils/worktree-operation.test.ts src/utils/process-runner.test.ts src/utils/git-worktree-flock.test.ts src/utils/git-binary.test.ts (0) — Focused GREEN: 4 test files and 23 tests passed, including deadline arithmetic/stage evidence, POSIX child/grandchild timeout cleanup, unsupported-platform fail-closed behavior, owner-token lease liveness/reclaim/release, and Git signal/remaining-time options.
- **[archive_only_evidence]** verification: pnpm exec tsc --noEmit (0) — TypeScript compilation passed.
- **[archive_only_evidence]** verification: pnpm --dir plugin run check (0) — Plugin schemas, typecheck, manifest/frontmatter/isolation/lockfile checks, lint, and formatting passed.
- **[archive_only_evidence]** verification: pnpm exec vitest run src/tools/worktree/detach.test.ts src/tools/worktree/index-delete.test.ts src/tools/worktree/cleanup-git-budget.test.ts (1) — Related suite: 68/69 tests passed; one index-delete expectation conflicts with concurrent Git-census/deletion-contract changes from parallel task tk-aaf30c4c2e56 (expected INTEGRATION_REQUIRED, current result success).
- **[report_follow_up]** follow_ups: Full plugin check must be rerun after parallel task tk-fe1844db3cd6 resolves its existing lint failure in plugin/src/utils/worktree-operation.ts:59.
- **[report_follow_up]** follow_ups: A broader shared-worktree test batch also showed failures in trunk-write-firewall and index-delete while parallel lease/process-runner changes are present; do not attribute those failures to this task without rerunning after the parallel task settles.
- **[unresolved_action]** required_main_agent_actions: Review the shared-worktree parallel changes before final acceptance; rerun pnpm --dir plugin run check and the broader worktree deletion suite after tk-fe1844db3cd6 completes.
- **[archive_only_evidence]** decisions: Use shared canonical NUL parser for production census callers while retaining a narrow newline compatibility adapter. — NUL is required to preserve arbitrary paths; existing legacy fixtures/consumers remain readable without reintroducing newline parsing in canonical callers.
- **[archive_only_evidence]** decisions: Hash a stable, explicitly projected facts payload and require canonical base64url JSON in wdp1 tokens. — Object insertion order, transport fields, malformed encodings, and payload tampering must not alter or bypass destructive-operation identity.
- **[archive_only_evidence]** decisions: Expand branch census from refs/heads/change to all refs/heads and keep detached/bare/locked/prunable facts. — Git census is the authority for every branch family; lifecycle filtering belongs to later policy/planner stages, not canonical facts collection.
- **[archive_only_evidence]** decisions: Remove the duplicate census parser and route worktree-paths, detach, state, triage, and utility census through the shared parser where compatibility allowed. — One parser owns normalization of Git porcelain facts; specialized consumers retain only their local policy projections.
- **[archive_only_evidence]** verification: pnpm test -- src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/porcelain-parser.test.ts (1) — RED recorded: new deletion-contract imports were missing and canonical NUL parser assertions failed before implementation.
- **[archive_only_evidence]** verification: pnpm exec vitest run src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/census-scan.test.ts src/tools/worktree/porcelain-parser.test.ts src/tools/worktree/census.test.ts src/utils/worktree-census.test.ts src/utils/worktree-paths.test.ts src/tools/worktree/triage.test.ts src/tools/worktree/detach.test.ts (0) — GREEN: 8 files and 44 tests passed, covering deterministic/malformed/expiry/fact-drift tokens, NUL/path edge cases, detached/bare/locked/prunable records, and all branch families.
- **[archive_only_evidence]** verification: pnpm --dir plugin run typecheck (0) — TypeScript typecheck passed.
- **[archive_only_evidence]** verification: pnpm exec eslint src/index.ts src/tools/archive-helpers/git-finalize.ts src/tools/trunk-write-firewall.ts src/tools/worktree/census.test.ts src/tools/worktree/census-scan.test.ts src/tools/worktree/census.ts src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/deletion-contracts.ts src/tools/worktree/detach.ts src/tools/worktree/index.ts src/tools/worktree/porcelain-parser.test.ts src/tools/worktree/porcelain-parser.ts src/tools/worktree/state.ts src/tools/worktree/triage.ts src/utils/worktree-census.test.ts src/utils/worktree-census.ts src/utils/worktree-paths.ts (0) — ESLint passed for the touched worktree/census/parser surface.
- **[archive_only_evidence]** verification: pnpm --dir plugin run check (1) — Bounded full check reached lint, then failed only on parallel-task file plugin/src/utils/worktree-operation.ts:59 prefer-const; schemas, typecheck, manifests, frontmatter, isolation, and lockfile checks passed before lint.
- **[unresolved_action]** required_main_agent_actions: Incorporate the four in-scope reviewer edits in the shared uncommitted output before completing tk-aaf30c4c2e56 and tk-fe1844db3cd6.
- **[unresolved_action]** required_main_agent_actions: Proceed with the dependent deletion planner task; do not reintroduce standalone porcelain parsing or unbound plan-token validation.
- **[unresolved_action]** required_main_agent_actions: Leave unrelated worktree lifecycle behavior unchanged.
- **[wisdom_candidate]** wisdom_candidates: [gotcha] A plan schema that carries both facts and a wdp1 token must validate their binding at the schema boundary; validating each field independently admits an internally contradictory destructive plan.
- **[archive_only_evidence]** changes_made: plugin/src/tools/worktree/deletion-contracts.ts: Bound WorktreeDeletionPlanSchema's duplicated repository, facts, expiry, and wdp1 token into one validated plan so a structurally valid plan cannot carry a token for different destructive facts or expiry.
- **[archive_only_evidence]** changes_made: plugin/src/tools/worktree/deletion-contracts.test.ts: Added regression coverage for top-level repository drift and expiry/token mismatch.
- **[archive_only_evidence]** changes_made: plugin/src/utils/process-runner.ts: Centralized failed-child cleanup so spawn errors clear timers and unregister the operation lease, preventing later cancellation from acting on a failed child.
- **[archive_only_evidence]** changes_made: plugin/src/utils/process-runner.test.ts: Added a regression test proving an errored child is unregistered before later operation abort.
- **[archive_only_evidence]** verification: tests_run=bin/oc-test targeted -- src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/porcelain-parser.test.ts src/utils/process-runner.test.ts src/utils/worktree-operation.test.ts src/utils/git-worktree-flock.test.ts src/utils/worktree-census.test.ts src/tools/worktree/census-scan.test.ts src/utils/git-binary.test.ts, pnpm --dir plugin exec tsc --noEmit, git diff --check results=pass — Focused suite: 8 test files, 42 tests passed. TypeScript no-emit completed successfully. git diff --check emitted no errors. Live Git porcelain -z bytes confirmed NUL field delimiters plus an empty NUL record separator; shared parser matches this canonical layout.
- **[report_follow_up]** follow_ups: Existing src/tools/worktree/index-delete.test.ts test 'INTEGRATION_REQUIRED — blocks delete when branch integration fails' fails on the foundation baseline with an unexpected successful delete; the same failure was present in the required RED run before planner changes. Triage separately before relying on that legacy suite as a green signal.
- **[unresolved_action]** required_main_agent_actions: Review the pre-existing index-delete integration-test failure before the later executor/integration tasks claim the entire worktree deletion suite is green.
- **[archive_only_evidence]** decisions: Use Git census as target/path/HEAD authority and treat registry absence as warning-only. — The deletion protocol requires Git-owned worktrees to remain discoverable through registry drift.
- **[archive_only_evidence]** decisions: Arm WorktreeOperationContext before target resolution and keep target resolution store-free. — One end-to-end deadline must cover target resolution; planning must not initialize ADV Store or invoke migration.
- **[archive_only_evidence]** decisions: Extend the self-contained token to bind planner safety facts, integration proof, and terminal proof. — Executor-side revalidation must detect changes to any fact that made the plan safe.
- **[archive_only_evidence]** decisions: Normalize Git's '+' linked-worktree marker when parsing merged branches. — Git prefixes branches checked out in another worktree with '+', and ignoring that marker incorrectly refused a real merged release fixture.
- **[archive_only_evidence]** verification: pnpm exec vitest run src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/deletion-planner.test.ts (0) — Planner and token-contract tests pass: 10 tests across 2 files.
- **[archive_only_evidence]** verification: pnpm exec vitest run src/tools/worktree/census.test.ts src/tools/worktree/census-scan.test.ts src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/porcelain-parser.test.ts src/utils/worktree-census.test.ts src/utils/worktree-operation.test.ts src/utils/process-runner.test.ts src/utils/git-worktree-flock.test.ts src/tools/worktree/deletion-planner.test.ts (0) — Related census, parser, operation, process, flock, and planner suites pass: 39 tests across 9 files.
- **[archive_only_evidence]** verification: pnpm run check (0) — Plugin check passes: schemas, typecheck, manifests, frontmatter, isolation, lockfile, lint, and formatting.
- **[archive_only_evidence]** decisions: Run an explicit deps.integrationCheck before registry-origin classification. — Census-derived registry rows are advisory and must not bypass or weaken an explicitly supplied safety proof.
- **[archive_only_evidence]** decisions: Preserve the existing standardized INTEGRATION_REQUIRED hint for explicit failures. — Keeps the public deletion result contract stable while returning the injected reason and preventing destructive execution.
- **[archive_only_evidence]** decisions: Set integrationCheck undefined in tests that intentionally exercise production registry/PR/force proof paths. — Those tests must use the real production verifier; only explicit seam tests should inject integration outcomes.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/worktree/deletion-planner.test.ts src/tools/worktree/index-delete.test.ts (0) — Planner and full index-delete suites pass: 65 tests across 2 files.
- **[archive_only_evidence]** verification: pnpm run check (0) — Plugin check passes: schemas, typecheck, manifests, frontmatter, isolation, lockfile, lint, and formatting.
- **[report_follow_up]** follow_ups: Wire adv_worktree_delete, cleanup, and archive paths to executeWorktreeDeletion in tk-5707a0b3fe6a.
- **[unresolved_action]** required_main_agent_actions: Review the executor adapter contract before public convergence; pass an explicit target repository, lease directory, hooks, terminal/integration proof seams, and reconciliation callback.
- **[archive_only_evidence]** decisions: Added a standalone WorktreeDeletionExecutor and kept public delete/cleanup/archive adapters unchanged. — This task owns the shared destructive executor; public convergence is explicitly assigned to the next task.
- **[archive_only_evidence]** decisions: Used the existing liveness-aware owner-token Git worktree flock and abortable process runner. — Reuses the completed foundations for repository serialization, process-group cancellation, and post-close quiescence instead of introducing parallel primitives.
- **[archive_only_evidence]** decisions: Treats Git/filesystem disagreement after removal as indeterminate and registry reconciliation failure as a deleted result with warning. — Prevents false success or a second destructive attempt while preserving confirmed Git outcome.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/worktree/index-delete.test.ts src/tools/worktree/deletion-planner.test.ts (0) — Corrected RED baseline passed 65 existing planner/delete tests before executor implementation.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/worktree/deletion-executor.test.ts src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/deletion-planner.test.ts src/tools/worktree/index-delete.test.ts src/utils/process-runner.test.ts src/utils/git-worktree-flock.test.ts src/utils/worktree-operation.test.ts (0) — 91 tests passed across executor, contracts, planner, existing delete, process-group, lease, and operation suites.
- **[archive_only_evidence]** verification: pnpm --dir plugin run check (0) — Plugin schemas, typecheck, manifest/frontmatter/isolation/lockfile checks, lint, and formatting all passed.
- **[unresolved_action]** required_main_agent_actions: Run the orchestrator's final full verification and checkpoint this task; do not mutate task or gate state from this worker.
- **[archive_only_evidence]** decisions: Made advWorktreeDelete a strict planner/executor adapter. — Dry-run now mints a typed expiring plan/token; apply rejects missing token or blank approvalEvidence before shell/write effects, eliminating the legacy destructive branch-only path.
- **[archive_only_evidence]** decisions: Moved workspace ownership cleanup into the executor before-remove seam. — OpenCode workspace cleanup remains lifecycle-specific while Git worktree removal, revalidation, deadline, and lease behavior stay on one shared destructive authority path.
- **[archive_only_evidence]** decisions: Used reason as bounded internal cleanup evidence when no explicit manual evidence is supplied. — Queued terminal reaper/drain candidates already carry durable candidate identity; public delete still requires explicit nonblank approvalEvidence, while manual cleanup commands can pass their required reason/evidence through the shared path.
- **[archive_only_evidence]** verification: pnpm --dir plugin run check (0) — Plugin schemas, typecheck, manifest, isolation, lint, and format checks pass.
- **[archive_only_evidence]** verification: pnpm --dir plugin exec vitest run src/tools/worktree/deletion-public-contract.test.ts src/tools/worktree/deletion-planner.test.ts src/tools/worktree/deletion-executor.test.ts --reporter verbose (0) — Public plan/apply migration, placeholder rejection, target timeout mapping, planner, and executor tests pass (27 tests).
- **[archive_only_evidence]** verification: pnpm --dir plugin exec vitest run src/tools/adv-worktree.test.ts src/tools/change/handlers-archive.schema.test.ts src/tools/adv-worktree.archived-branches.test.ts --reporter verbose (0) — Public worktree wrapper, archive schema, and archived-branch cleanup suites pass.
- **[unresolved_action]** required_main_agent_actions: Checkpoint task tk-5707a0b3fe6a with the recorded GREEN evidence, then run the pending final verification task tk-b0554f647f3f.
- **[archive_only_evidence]** decisions: Bound force deletion intent into the expiring plan token and executor command. — Force is an explicit approval-controlled exception for dirty worktrees; token binding prevents apply from gaining force semantics after planning.
- **[archive_only_evidence]** decisions: Bound planner integration and terminal-proof callbacks to the same operation deadline. — A hanging integration or terminal read must return typed deadline evidence instead of holding the deletion request past its budget.
- **[archive_only_evidence]** decisions: Migrated legacy delete tests to explicit plan-then-apply behavior and Git-census authority. — Branch-only destructive calls now correctly return PLAN_REQUIRED, while registry absence remains advisory and public safety coverage exercises the new protocol.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/adv-worktree.test.ts src/tools/worktree/deletion-tool-adapter.test.ts src/tools/worktree/deletion-executor.test.ts src/tools/worktree/deletion-planner.test.ts src/tools/worktree/index-delete.test.ts (0) — Passed: 129 tests across the focused worktree/adv-worktree integration suites.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/worktree/deletion-contracts.test.ts src/tools/worktree/deletion-public-contract.test.ts src/tools/worktree/deletion-executor.test.ts src/tools/worktree/deletion-planner.test.ts src/tools/worktree/index-delete.test.ts src/tools/change/handlers-archive.schema.test.ts (0) — Passed: 90 tests across deletion contracts, public contract, planner, executor, index-delete, and archive schema suites.
- **[archive_only_evidence]** verification: pnpm --dir plugin run check (0) — Passed schemas, typecheck, generated manifest checks, frontmatter/isolation/lockfile checks, lint, and formatting.
- **[wisdom_candidate]** wisdom_candidates: [gotcha] A planner operation context beginning before target resolution is insufficient unless the target-resolution promise itself is raced against the remaining budget; otherwise a never-settling resolver can outlive the typed deadline.
- **[archive_only_evidence]** changes_made: plugin/src/tools/worktree/deletion-planner.ts: Bound target resolution by the operation deadline, so a resolver that never settles returns a typed target_resolution deadline instead of outliving the planning budget.
- **[archive_only_evidence]** changes_made: plugin/src/tools/worktree/deletion-planner.test.ts: Added regression coverage for a target resolver that never settles.
- **[archive_only_evidence]** verification: tests_run=bin/oc-test targeted -- src/tools/worktree/deletion-planner.test.ts src/tools/worktree/deletion-public-contract.test.ts src/tools/worktree/deletion-executor.test.ts src/tools/worktree/index-delete.test.ts src/tools/adv-worktree.archived-branches.test.ts src/tools/adv-worktree.archived-branches-emergency.test.ts src/tools/change.archive-convergence.test.ts src/tools/change/handlers-archive.schema.test.ts src/tools/change/archive-timeout.test.ts, pnpm --dir plugin run check results=pass — Targeted deletion/public/planner/executor/index-delete plus archive/cleanup contract suite: 9 files, 132 tests passed. Plugin schemas, typecheck, generated manifests, frontmatter, isolation, lockfile, lint, and format checks passed. git diff --check passed.
- **[archive_only_evidence]** decisions: Route target-path deletion through stateRequirement snapshot-ok while retaining mutation: !dryRun. — Target store is only lightweight terminal-proof input; Git census/planner/executor own deletion effects, while apply still uses the existing explicit target trust gate.
- **[archive_only_evidence]** decisions: Create one operation context at target-handler entry and subtract the response reserve from the remaining budget passed to deletion. — Target resolution, wrapper, planner, and executor now share one end-to-end deadline instead of starting a fresh delete budget after target routing.
- **[archive_only_evidence]** decisions: Guard the late non-cancelling target callback with the aborted/expired operation context. — A target resolution that finishes after the typed target_resolution timeout must not invoke planner, executor, or deletion.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/adv-worktree.test.ts src/tools/worktree/deletion-public-contract.test.ts src/tools/worktree/deletion-planner.test.ts (0) — 65 focused worktree public-handler, deletion-contract, and planner tests passed.
- **[archive_only_evidence]** verification: pnpm run check (0) — Plugin schemas, typecheck, manifest/frontmatter/isolation/lockfile checks, lint, and format checks passed.

## Contract / AC Coverage

No contract items.

## Unresolved Actions

- Review and reconcile the related index-delete expectation against tk-aaf30c4c2e56's canonical census behavior before acceptance.
- Integrate the exported operation context/process runner into deletion target resolution and destructive apply so the end-to-end cancellation contract is exercised.
- Review the shared-worktree parallel changes before final acceptance; rerun pnpm --dir plugin run check and the broader worktree deletion suite after tk-fe1844db3cd6 completes.
- Incorporate the four in-scope reviewer edits in the shared uncommitted output before completing tk-aaf30c4c2e56 and tk-fe1844db3cd6.
- Proceed with the dependent deletion planner task; do not reintroduce standalone porcelain parsing or unbound plan-token validation.
- Leave unrelated worktree lifecycle behavior unchanged.
- Review the pre-existing index-delete integration-test failure before the later executor/integration tasks claim the entire worktree deletion suite is green.
- Review the executor adapter contract before public convergence; pass an explicit target repository, lease directory, hooks, terminal/integration proof seams, and reconciliation callback.
- Run the orchestrator's final full verification and checkpoint this task; do not mutate task or gate state from this worker.
- Checkpoint task tk-5707a0b3fe6a with the recorded GREEN evidence, then run the pending final verification task tk-b0554f647f3f.
