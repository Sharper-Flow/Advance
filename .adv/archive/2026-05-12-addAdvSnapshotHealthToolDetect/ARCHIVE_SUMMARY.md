# Archive: Add adv_snapshot_health tool to detect and remediate OpenCode snapshot-store corruption (stale locks, zero-byte git objects, fsck errors) that has caused recurring agent freezes in busy projects like pokeedge-web.

**Change ID:** addAdvSnapshotHealthToolDetect
**Archived:** 2026-05-12T21:23:47.185Z
**Created:** 2026-05-12T14:19:22.208Z

## Tasks Completed

- ✅ Create new spec capability `.adv/specs/snapshot-health/spec.json` declaring requirements with stable `rq-` identifiers covering: probe-coverage (all 7 detection patterns), safe-default (read-only scan), repair-whitelist (3 closed actions), audit-trail (agenda entry per repair), scope-boundary (project vs global), schema-versioning (integer schema_version: 1). Include short capability description and link to docs/snapshot-health.md. Inline TDD: test that spec parses + has ≥4 requirements + all rq- IDs unique.
  > Task checkpoint completed
- ✅ Implement `plugin/src/tools/snapshot-scan.ts` — pure filesystem scan logic with TDD. Exports: `scanSnapshotHealth(opts)`, `executeRepair(opts)`, type definitions (`SnapshotFinding`, `SnapshotHealthOutput`, `RepairActionRecord`). Detection patterns: stale_lock (mtime >5min + no lsof holder), zero_byte_object (`objects/{xx}/[0-9a-f]{38,}` size 0), fsck_error (`git fsck --no-dangling --connectivity-only`, max 10 errors, skip dirs >500MB), orphan_bare_repo (worktree path missing), oversized_dir (>100MB info), legacy_layout (HEAD+objects/+refs at project root), no_snapshot_dirs (project has external state but no bare repos). Inline TDD: 8 fixture bare repos in temp dirs (one per finding type), each pattern asserts expected finding shape. File header comment: `// Tool-layer module — imports child_process for git fsck. Not workflow-safe.` Verify: `pnpm test -- snapshot-scan` passes.
  > Task checkpoint completed
- ✅ Implement `plugin/src/tools/snapshot.ts` — ADV tool wrapper exporting `snapshotHealthTools` with `adv_snapshot_health` definition. Zod arg schema: `action` (scan|repair, default scan), `scope` (project|global, default project), `repair_actions` (array of 3 whitelisted enums, optional), `approvedByUser` (boolean, optional), `approvalEvidence` (string, optional), `dryRun` (boolean, default false). Execute function: validates required args for repair action, calls `scanSnapshotHealth`, conditionally `executeRepair`, writes agenda entries via `agendaTools.adv_agenda_add` on successful repairs, returns `formatToolOutput` with schema_version: 1 shape. Inline TDD: 7 tests — scan happy path, repair rejects without approvedByUser, repair rejects without approvalEvidence, repair rejects unknown action string, dryRun returns preview no mutations, audit entry written on success, output schema validates. Verify: `pnpm test -- snapshot.test` passes.
  > Task checkpoint completed
- ✅ Register `adv_snapshot_health` in tool infrastructure: (1) `plugin/src/tool-registry.ts` — import `snapshotHealthTools` from `./tools/snapshot`, add `bindTool(snapshotHealthTools.adv_snapshot_health, "adv_snapshot_health", store)` to `createToolMap` (near other diagnostic tools), AND add `"adv_snapshot_health"` to `ADV_TOOL_NAMES` array so degraded tool map stubs it on init failure. (2) `plugin/src/manifest.ts` — add tool name declaration for agent allowlist propagation. Inline TDD: extend existing tool-registry test that asserts tool count + presence of `adv_snapshot_health` in registered tools and ADV_TOOL_NAMES. Verify: `pnpm test -- tool-registry` passes.
  > Task checkpoint completed
- ✅ Integrate snapshot health into `adv_status` output: (1) `plugin/src/utils/tool-formatters.ts` — add optional `snapshotHealth?: { critical: number; warnings: number; info: number }` to `StatusInput` interface. Inside `formatStatusOutput`, append one line to existing `healthLines[]` (NOT a new section, per validator recommendation): `Snapshot: ✓ clean` / `Snapshot: 🟨 N warnings` / `Snapshot: 🟥 N critical`. (2) `plugin/src/tools/status.ts` — add TTL-cached probe wrapper (60s TTL) calling `scanSnapshotHealth` keyed by `projectId ?? MISSING_PROJECT_ID_CACHE_KEY`. After `computeExternalStateHygiene` (~line 1124), call the probe and add `snapshot_health` to `fullOutput`. Pass summary counts to `formatStatusOutput`. Update `applyStatusView`: add `projection.snapshot_health = full.snapshot_health` in BOTH `case "health"` and `case "hygiene"`. Inline TDD: extend status tests — assert `snapshot_health` present in both views, summary line appears in formatted healthSection, summary line says ✓ clean when no findings. Verify: `pnpm test -- status` passes.
  > Task checkpoint completed
- ✅ Update agent allowlists to expose `adv_snapshot_health`. Edit repo-local agent files: `.opencode/agents/adv.md`, `.opencode/agents/adv-atc.md`, `.opencode/agents/adv-engineer.md`, `.opencode/agents/adv-researcher.md`, `.opencode/agents/adv-tron.md`. Add `adv_snapshot_health: true` to tools section in each. Provider-keyed globals (`adv-claude/gpt/glm/kimi/atc`) are sync-generated — propagation is handled by `scripts/sync-global.sh` once manifest entry exists (added in tk-d5e9612fe7ba). Inline-trivial: docs/config change only, no TDD red phase required; verify by `grep -l adv_snapshot_health .opencode/agents/*.md` returns 5 files. Mark `tdd_intent: not_applicable`.
  > Task checkpoint completed
- ✅ Write user documentation: (1) `docs/snapshot-health.md` (new) — covers: what the tool detects (all 7 patterns with examples), how to invoke (`adv_snapshot_health` scan and repair forms), repair safety model (approval gate, whitelist, agenda audit), known limitations (does not fix upstream OpenCode race), `adv_status` integration (health/hygiene views), schema_version: 1 stability contract. (2) `AGENTS.md` — add one-line entry to the existing "MCP tool selection" section (or wherever diagnostic tools are listed). (3) `project.md` — single-line reference to `adv_snapshot_health` in the diagnostic-tools section. Mark `tdd_intent: not_applicable` (docs only).
  > Task checkpoint completed
- ✅ Final verification + propagation: (1) Run `./scripts/sync-global.sh --check` then `--fix` to propagate manifest tool entry to provider-keyed agent globals (`adv-claude/gpt/glm/kimi/atc`) under `~/.config/opencode/agents/`. (2) Run live-tree validation: call `adv_snapshot_health` on the current `~/.local/share/opencode/snapshot/` tree and assert output matches the Python baseline at `/tmp/opencode/snapshot-health-report.json` (18 projects, 22 bare repos, 0 critical/0 warnings post-cleanup) — this is V2 from the agreement. (3) Verify performance: scan_duration_ms reported < 10000 (AC8). (4) Run full repo check: `pnpm run check` (typecheck + lint + format:check) and `pnpm test` (full vitest suite). All must pass clean. Document any drift in implementation_summary on task completion. Mark `tdd_intent: not_applicable` (verification-only task).
  > Task checkpoint completed

## Specs Modified

