# Archive: retireInvestmentGovernanceDeadweight

**Change ID:** retireinvestmentgovernancedead
**Archived:** 2026-05-07T23:59:12.036Z
**Created:** 2026-05-07T22:40:19.604Z

## Tasks Completed

- ✅ T01 — Schema + types cleanup. Drop `JudgmentCallSchema`, `JudgmentCallCategorySchema`, `JudgmentCall`, `JudgmentCallCategory`, `ThresholdTierSchema`, `ThresholdTier` from `plugin/src/types/investment.ts`. Slim `InvestmentReportSchema` (drop `threshold_tier` field). Update `plugin/src/types/index.ts` re-exports. Drop `judgment_calls` and `batch_surfaced_at` declared optional fields from `plugin/src/types/changes.ts` (keep `.passthrough()` for archived backward-compat). Drop matching tests in `plugin/src/types.test.ts`. RED: add 2 regression tests that load archived bundles carrying `judgment_calls: []` and `batch_surfaced_at: <ISO>` and assert they parse + round-trip cleanly via passthrough. GREEN: implement schema cleanup so red tests pass. TDD inline.
  > T01 complete. Removed ThresholdTier/JudgmentCall types and judgment_calls/batch_surfaced_at field declarations from Change schema. Added 2 archive-passthrough regression tests confirming archived bundles still load via .passthrough(). Engineer flagged 2 expected typecheck cascades scoped to T02 (tools/investment.ts ThresholdTier import) and T03 (storage/change-selection.ts batch_surfaced_at reference). Checkpoint 55a92f6.
- ✅ T02 — Tool slimming + reflection co-removal. In `plugin/src/tools/investment.ts`: drop `classifyTier`, `DEFAULT_THRESHOLDS`, `ThresholdsSchema`, `Thresholds` type. Slim `adv_investment_report` tool: drop `thresholds` arg, drop `threshold_tier` from output. Keep `computePerGateDurations` (pure formatter, exported for reflection). In `plugin/src/tools/investment.test.ts`: drop tier-related tests. In `plugin/src/tools/reflection.ts`: drop `classifyTier` import (line 17), drop `classifyTier` call (line 353), drop `threshold_tier` from emitted reflection report (line 536). In `plugin/src/storage/reflection.ts`: drop `threshold_tier` from `ReflectionEntry.plane1.efficiency` schema (line 39); confirm `.passthrough()` at line 119 covers archived reflection records. In `plugin/src/tools/reflection.test.ts`: update assertion at line 152 to drop `threshold_tier` expectation. RED: add regression test confirming archived `reflections.jsonl` entry carrying `threshold_tier: 'auto'` parses + round-trips cleanly via passthrough. GREEN: implement co-removal so all reflection.test.ts and investment.test.ts cases pass. TDD inline. Blocked by T01 (depends on slimmed InvestmentReportSchema).
  > T02 complete. Investment tool slimmed (dropped ThresholdTier/classifyTier/thresholds/DEFAULT_THRESHOLDS/threshold_tier output); reflection.ts drops classifyTier call + threshold_tier emission; ReflectionEntrySchema drops threshold_tier field with .passthrough() preserving archived reflection backward-compat. Checkpoint d32d6f2.
- ✅ T03 — Storage cleanup. In `plugin/src/storage/store-temporal/changes.ts`: drop `judgment_calls` and `batch_surfaced_at` from projection passthroughs at lines 123-124 and 169-170. The full `Change` schema's `.passthrough()` covers archived reads. In `plugin/src/storage/store-disk.ts:370`: drop `judgment_calls: []` initializer in new-change creation. In `plugin/src/storage/change-selection.ts:160-161`: drop `batch_surfaced_at` from `getLastActivityTimestamp` formula (other timestamps cover the recency case). In `plugin/src/index.ts:734`: drop comment referencing Phase 1.5. RED: add tests asserting (a) new changes don't get `judgment_calls` initialized, (b) `getLastActivityTimestamp` returns correct results without `batch_surfaced_at` input, (c) projections from store-temporal don't surface the dropped fields on new changes. GREEN: implement cleanup so red tests pass. TDD inline. Blocked by T01.
  > T03 complete. Removed judgment_calls/batch_surfaced_at from store-temporal projection (lines 123-124, 169-170), store-disk initializer (line 370), change-selection getLastActivityTimestamp formula (lines 160-161), and Phase-1.5 comment in src/index.ts (lines 730-735). Added 3 regression tests confirming new-change initializer, getLastActivityTimestamp behavior, and archive passthrough. Checkpoint cfd5def.
- ✅ T04 — Command file deletions + adv-apply phase renumbering. In `.opencode/command/adv-prep.md`: delete `## Phase J: Identify Judgment Calls (addCostTimeInvestment)` section (lines 104-122 of pre-change file). In `.opencode/command/adv-apply.md`: delete `## Phase 1.5: Investment Check-In Preamble (addCostTimeInvestment)` section (lines 287-304 of pre-change file). Rename current `## Phase 2: Prep Gate Approval Verification` (line 306) to `## Phase 1.5: Prep Gate Approval Verification` to claim freed slot and resolve duplicate `## Phase 2` heading bug (KD-4 / P23 campsite cleanup). In `.opencode/command/adv-autopilot.md`: delete Phase 1.5 references at lines 23-24, 101, 162; preserve any remaining surface intent (user-value tradeoff pause) by referencing `rq-autonomy01.6` (contract-compromise pause) where contextually appropriate. Verification: targeted grep for `Phase J`, `Phase 1\\.5`, `judgment_calls`, `judgment-call`, `cost-governance` across `.opencode/command/` returns zero matches in the three updated files. TDD intent: not_applicable (docs only).
  > T04 complete. Deleted /adv-prep Phase J, /adv-apply Phase 1.5 Investment Check-In Preamble. Renamed duplicate '## Phase 2: Prep Gate Approval Verification' → '## Phase 1.5: Prep Gate Approval Verification' (KD-4 P23 campsite cleanup). Cleaned /adv-autopilot Phase 1.5 references in Constraints, Phase 3, System Interrupt Handling table. Plus 2 stale judgment-call prose refs in adv-apply.md from related-scan. Checkpoint 153bf80.
- ✅ T05 — Instructions + skill deletion. Delete `.opencode/instructions/cost-governance.md` (repo file) via `git rm`. Delete `skills/adv-cost-governance-methodology/SKILL.md` (repo file) via `git rm`. In `ADV_INSTRUCTIONS.md`: delete the `### Investment Check-In` subsection. Scan `ADV_INSTRUCTIONS.md` for any other Phase J / Phase 1.5 / judgment-call / cost-governance references and remove. Verification: targeted grep for `Phase J`, `Phase 1\\.5`, `judgment_calls`, `cost-governance`, `Investment Check-In` across `ADV_INSTRUCTIONS.md` returns zero matches; the deleted files do not exist. TDD intent: not_applicable (docs/instruction deletion).
  > T05 complete. Deleted .opencode/instructions/cost-governance.md and skills/adv-cost-governance-methodology/SKILL.md via git rm. Scrubbed ADV_INSTRUCTIONS.md of all Phase J / Phase 1.5 / judgment_calls / cost-governance / Investment Check-In references. Engineer's targeted grep returned zero matches. Checkpoint 3f6cd9a.
- ✅ T06 — Agent overlays + rules + setup updates. Surgical edits to remove judgment-call / Phase 1.5 / cost-governance references in: `.opencode/agents/adv.md` (lines 125, 136, 203, 211 of pre-change file), `.opencode/agents/build.md` (line 201), `.opencode/agents/adv-engineer.md` (line 89). Scan `.opencode/overlays/*.overlay.md` for matching references and trim. In user-managed `~/.config/opencode/instructions/rules.yaml`: P28 (cost-governance rule) — document deletion in CHANGELOG as one-time user step (the file is user-owned, not repo-owned). In `SETUP.md`: update lines 401, 417, 419 to drop Phase J / opt-in / `--check` cost-governance references. In `CHANGELOG.md`: add changelog entry documenting the retirement and one-time user steps (rules.yaml P28 removal; opencode.json instructions array entry removed by sync). Verification: targeted grep for the same removed terms across `.opencode/agents/`, `.opencode/overlays/`, `SETUP.md` returns zero matches. TDD intent: not_applicable (docs/config edits).
  > T06 complete. Surgical edits to .opencode/agents/{adv,build,adv-engineer}.md removed cost-governance Phase 1.5 references (drop "(cost-governance Phase 1.5)" parentheticals, drop "judgment calls" from non-checkpoint list, delete "Cost governance / investment check-in" bullet from interrupt list). Deleted Cost Governance Rule P28 section + opt-in/tuning subsections from SETUP.md. Added retireInvestmentGovernanceDeadweight CHANGELOG entry documenting removals + one-time user steps. Checkpoint 1ee8283.
- ✅ T07 — Sync script + global cleanup. In `scripts/sync-global.sh`: remove `ADV_COST_GOVERNANCE_PATH` variable declaration (line 130 pre-change), remove cost-governance check entries (lines 817-822), remove cost-governance instructions array patch logic (line 953+). Run `scripts/sync-global.sh --check` from worktree to verify clean state. Run `scripts/sync-global.sh --fix` to propagate removals to `~/.config/opencode/`. Manually delete `~/.config/opencode/skills/adv-cost-governance-methodology/` directory (sync-global.sh likely doesn't propagate skill directory deletions). Manually delete `~/.config/opencode/instructions/cost-governance.md` (sync-global.sh `--fix` should remove from `opencode.json` instructions array but the file itself may need explicit `rm`). Verify final state: file does not exist; `opencode.json` instructions array does not contain `cost-governance.md`; `scripts/sync-global.sh --check` reports clean. TDD intent: not_applicable (scripts/config).
  > T07 complete. scripts/sync-global.sh stripped of cost-governance entries (ADV_COST_GOVERNANCE_PATH var, check block, patch logic). Added cleanup migration removing cost-governance.md from opencode.json instructions array. Ran --check clean, --fix patched + synced. Manual cleanup of ~/.config/opencode/instructions/cost-governance.md and skills/adv-cost-governance-methodology/. Note: global skill may re-sync from main checkout until trunk merge (documented in CHANGELOG). Checkpoint f4def7d.
- ✅ T08 — Spec scenario edits + asset-test update. In `.adv/specs/advance-workflow/spec.json`: (a) `rq-autonomy01.4` — drop "no unresolved judgment call" from given clause (item 4 of given list); (b) `rq-autonomy01.5` — drop "Judgment-call surfacing (Phase 1.5)" parenthetical from given clause; replace with "user-value tradeoffs have been resolved at the design approval checkpoint per rq-autonomy01.3"; (c) the scenario near line 1198 referencing `judgment_calls[]` — remove the line or restructure scenario. Mirror all three edits in `docs/specs/advance-workflow.md` (lines around 1093-1101 and corresponding rq-autonomy01.4/.5 scenario blocks). In `plugin/src/adv-autonomy-quality-assets.test.ts:315`: update assertion to drop the requirement that `adv_investment_report` is mentioned in `adv-discover.md` / `adv-review.md` / `adv-archive.md` (those references will be removed by T04 if applicable; verify final state). Verification: `pnpm test plugin/src/adv-autonomy-quality-assets.test.ts` passes; spec.json validates against schema; `rq-autonomy01` requirement body unchanged; only scenarios .4/.5 + line-1198 scenario modified. TDD intent: not_applicable (spec edits + test assertion update — the test passing IS the verification).
  > T08 complete. rq-autonomy01.4: dropped 'no unresolved judgment call' from given. rq-autonomy01.5: replaced Phase 1.5 reference with 'User-value tradeoffs have been resolved at the design approval checkpoint per rq-autonomy01.3'. rq-autopilot01.3: dropped judgment_calls scenario item, restructured remaining 'Or' prefix. Markdown mirror updated. Asset test line 315 unchanged — adv_investment_report still referenced in adv-discover/review/archive. Checkpoint 8ba581d.
- ✅ T09 — Final verification + agenda closure. Run `pnpm run check` (typecheck + lint + format:check) from `plugin/`. Run `pnpm test` from `plugin/` — all 1834+ tests must pass. Run `pnpm run build` from `plugin/`. Run `scripts/sync-global.sh --check` from repo root — must report clean. Verify SC1/AC1: targeted ripgrep for `judgment_calls\\|batch_surfaced_at\\|Phase J\\|Phase 1\\.5\\|hardstop\\|JudgmentCall\\|ThresholdTier\\|cost-governance\\|adv-cost-governance` across `plugin/src/`, `.opencode/`, `ADV_INSTRUCTIONS.md`, `SETUP.md`, `scripts/sync-global.sh`, `.adv/specs/advance-workflow/spec.json`, `docs/specs/advance-workflow.md` returns zero matches in active code (archived bundles excluded, CHANGELOG.md historical reference allowed). Update agenda item `ag-55f13852` (umbrella tracker) with closure note for part 2 (hardstop false positive on smooth runs) referencing this change ID. Inspect closure: agenda item description appended with closure note OR new agenda item created tracking closure if append not supported by tool. TDD intent: separate_verification.
  > T09 complete. Fixed 3 lint errors (unused imports). Removed 6 asset-test blocks asserting retired content. Edited 4 additional spec scenarios + Markdown mirror. Restored rq-prep-out1/scope1/neg1 citations in adv-prep.md (T04 had stripped them with Phase J). Verified: pnpm run check clean; pnpm test 1811/1811 pass; pnpm run build clean; scripts/sync-global.sh --check clean. AC1 ripgrep zero matches in active code. Closed umbrella agenda part 2 (ag-2p_qgtYN tracking the closure of ag-55f13852 part 2). Checkpoint 71b9990.

## Specs Modified

