# Acceptance

Reviewed at: 2026-07-20T21:20:12.457Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | `fixWorkflowReliabilityDefects`-shaped state is recoverable without fabricating poisoned-history evidence. | pass | Shipped-terminal fixture (no poison) qualifies and converges without requiring poisoned-history evidence. AC9 E2E test proves recovery. |
| SC2 | success_criterion | A genuinely healthy or incompletely shipped workflow remains non-terminable. | pass | Refusal tests: unshipped gates, incomplete phase9, missing bundle, mismatched bundle id, RUNNING without poison+proof. Proof gates idempotent paths. |
| SC3 | success_criterion | Successful recovery leaves one archived authority: show reads archived, in-flight lists omit it, archived lists include it exactly once, and merged-branch cleanup can evaluate it as archived. | pass | Convergence writes status+lifecycleState=archived atomically; readback verifies single archived authority; idempotent re-invoke routes to adv_archive_purge. |
| SC4 | success_criterion | Existing poisoned-workflow and archived-purge behavior remains regression-green. | pass | Existing poisoned-history tests pass unchanged. _recovery-writers: 30/30 (existing 28 + 2 new). change.status-repair: 15/15. |
| AC1 | acceptance_criterion | **Approval first:** missing `approvedByUser:true` or blank `approvalEvidence` performs zero change, disk, bundle, Temporal describe, terminate, refresh, or projection-write operations. | pass | Approval-first preserved (lines ~4315-4331 of change.ts). Regression sweep test: 'rejects approvedByUser !== true before any read or mutation' + 'rejects blank approvalEvidence before any read or mutation' pass unchanged. |
| AC2 | acceptance_criterion | **Separate eligibility classes:** a live `RUNNING`/`PAUSED` run is terminable only through either existing poisoned-history evidence or a new typed shipped-terminal proof; neither route silently authorizes the other. | pass | Eligibility class field returned: 'poisoned_history' OR 'shipped_terminal' OR 'none'. Test 'dryRun returns eligibilityClass=shipped_terminal' + 'dryRun preserves eligibilityClass=poisoned_history'. |
| AC3 | acceptance_criterion | **Complete shipped-terminal proof:** the alternate route requires all seven gates `done` on the durable disk projection, `phase9_status.status === "done"`, and a readable schema-valid archive bundle whose embedded change ID exactly equals the requested ID. Any missing, invalid, mismatched, or unreadable component refuses with typed evidence and zero mutation. | pass | computeShippedTerminalProof helper with 6 typed refusal codes: PROOF_INVALID_DISK_PROJECTION, PROOF_MISSING_GATES, PROOF_MISSING_PHASE9, PROOF_NO_BUNDLE, PROOF_INVALID_BUNDLE, PROOF_BUNDLE_ID_MISMATCH. 12 unit tests in shipped-terminal-proof.test.ts. |
| AC4 | acceptance_criterion | **Exact pin and preview:** one pre-mutation `describe()` supplies a non-empty `runId`; dry-run returns the eligibility class and every proof component without termination, refresh, or disk mutation; execution terminates only the handle pinned to that described `runId`. | pass | describe() called once pre-mutation; runId extracted structurally; dryRun returns eligibilityClass + proof components without terminate/refresh; execution terminates via getChangeHandle(client, projectId, changeId, runId). Test: 'dryRun returns the structured pin assessment without terminate or refresh'. |
| AC5 | acceptance_criterion | **Status safety:** unknown run status, missing run ID, unshipped gates, non-done phase 9, invalid bundle, or a normal active change refuses. `RUNNING` status alone never authorizes termination. | pass | Refusal paths: unknown status, missing runId, unshipped gates (coarse precheck), IDEMPOTENT_BUT_PROOF_MISSING for terminal-status without proof, PROOF_* refusal codes, RUNNING+approval-alone never authorizes. Test: 'refuses already-terminal run without poison AND without shipped-terminal proof'. |
| AC6 | acceptance_criterion | **Failure ordering:** a non-idempotent terminate failure occurs before cache refresh or projection mutation. If termination succeeds but terminal projection convergence fails, output reports a typed partial-recovery state with exact remediation; it never reports full success. | pass | Failure-before-projection-mutation preserved (terminate non-not-found failure returns before refresh/write). Typed partialRecovery+remediation shape on convergence failure: successorRace, lateSuccessorRace, writeFailed, readbackFailed. Tests: 'successor check #1', '#2 describe failure surfaces typed partial-recovery', 'readback failure returns typed partialRecovery'. |
| AC7 | acceptance_criterion | **Terminal convergence:** after eligible termination or idempotent completed/not-found handling, durable projection records both `status:"archived"` and `lifecycleState:"archived"`. Read-after-write proves show status archived, in-flight count 0, and archived count exactly 1 before full success. | pass | saveRecoveredChangeStatus writes both status+lifecycleState atomically when provided. verifyStatusRepairReadAfterWrite with requireLifecycleState flag asserts both. AC9 E2E test asserts readback.showStatus='archived' + readback.showLifecycleState='archived' + inFlightCount=0 + archivedCount=1. |
| AC8 | acceptance_criterion | **Successor-race guard:** post-termination verification must not claim convergence if the same workflow ID has a different live successor `runId`; it returns a typed race/conflict result naming both runs. | pass | convergeTerminalAuthority performs two successor describes (pre-write + post-readback). Tests: 'successor check #1: a different live successor before write returns typed successorRace', 'successor check #2: a late successor after readback returns typed lateSuccessorRace', 'successor check #2 describe failure surfaces typed partial-recovery'. |
| AC9 | acceptance_criterion | **Regression fixture:** automated tests reproduce the observed state: live store says draft, all seven gates done, phase 9 done, archive inventory/bundle terminal, describe returns `RUNNING` without poison text. Dry-run qualifies it and execution terminates the exact pin and converges reads. | pass | AC9 regression fixture: fixWorkflowReliabilityDefects-shaped state recovers end-to-end. Steps: dryRun qualifies shipped_terminal → execute terminates pinned run + converges → readback proves archived/in-flight=0/archived=1 → idempotent re-invoke routes to adv_archive_purge. |
| AC10 | acceptance_criterion | **Compatibility:** existing poison-qualified termination, archived-change routing, `adv_archive_purge`, `adv_change_status_repair`, and exact-pin failure tests remain passing. | pass | Regression sweep: 93/93 pass across change.workflow-terminate (29), change.status-repair (15), _recovery-writers (30), shipped-terminal-proof (12), recovery-readback (7). Poisoned-history tests unchanged. Archive-purge routing preserved. Status-repair backward-compat preserved (lifecycleState optional). |
| C1 | constraint | Operator-only posture and approval evidence remain mandatory. | respected | approvedByUser + approvalEvidence gates every code path. Operator-only posture preserved. |
| C2 | constraint | No Temporal Reset, workflow reseed, or history rewrite. | respected | No Temporal Reset, no workflow reseed, no history rewrite. Termination only via handle.terminate(). |
| C3 | constraint | Archive bundle remains preserved; no disk-bundle deletion is introduced. | respected | Archive bundle never touched. No includeDiskBundle parameter exposed. findArchiveBundle used read-only. |
| C4 | constraint | Terminal proof uses parsed structural state, never title, age, branch-name, status-string coincidence, or free-text heuristics. | respected | Proof uses ChangeSchema.parse, GATE_ORDER filter, phase9_status.status check, bundle change.id strict equality. No title/age/branch-name/status-string heuristics. |
| C5 | constraint | Existing accepted-delta immutable projection safeguards remain intact. | respected | saveRecoveredChangeStatus spread preserves existing fields. Delta add-only semantics unchanged. |
| C6 | constraint | Trunk/worktree isolation remains mandatory. Before implementation, local `trunk`, `origin/trunk`, and worktree base must still be reconciled; initial verified SHA is `412475ab88f8ddc1b0af95bfd2b67eeed55c3e9b`. | respected | All work in worktree change/fixShippedWorkflowTermination. Trunk at 412475ab verified pre-implementation. |
| DONT1 | avoidance | Do not weaken the existing poisoned-history evidence predicate. | respected | poisonedDescriptionEvidence predicate unchanged. Existing poisoned-history tests pass. |
| DONT2 | avoidance | Do not terminate from `RUNNING`/`PAUSED` plus user approval alone. | respected | RUNNING + no poison + no proof always refuses. Proof required for shipped_terminal class. |
| DONT3 | avoidance | Do not trust bundle directory naming without parsing embedded identity. | respected | Bundle change.id strictly compared to requested changeId. Test 'refuses when shipped-terminal proof fails on PROOF_BUNDLE_ID_MISMATCH'. |
| DONT4 | avoidance | Do not report success before terminal read-after-write verification. | respected | verifyStatusRepairReadAfterWrite runs before success. convergeTerminalAuthority asserts both fields. Failure returns typed partial-recovery. |
| DONT5 | avoidance | Do not modify the active `fixPoisonedRecovery` contract; this change is its fast-follow for a disproven out-of-scope premise. | respected | fixPoisonedRecovery untouched. Touch regions non-overlapping (terminate tool ~L4500-L5100 vs probe-first recovery ~L2970-L3340). |
| OOS1 | out_of_scope | General archive enumeration performance (`fixArchivedBranchCleanup`). | missing |  |
| OOS2 | out_of_scope | Legacy design-validation report tolerance (`makeLegacyDesignValidation`). | missing |  |
| OOS3 | out_of_scope | Automatic bulk repair of all shipped workflows. | missing |  |
| OOS4 | out_of_scope | New workflow reset or history-inspection tooling. | missing |  |

