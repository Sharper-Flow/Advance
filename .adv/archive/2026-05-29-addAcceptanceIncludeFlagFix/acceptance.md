# Acceptance

Reviewed at: 2026-05-29T19:23:50.363Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | `adv_change_show` with `include.acceptance` returns `_acceptance` content from Temporal state (with disk/archive fallback) | pass | Test 'returns _acceptance content when include.acceptance is set and file exists' passes. Zod schema, type, dispatch, and output all handle acceptance kind. |
| AC2 | acceptance_criterion | `adv_change_show` with `include.acceptance` on a change without acceptance content returns `_acceptance` as undefined (no error) | pass | Test 'omits _acceptance when include.acceptance is set but file is missing' passes. readArtifacts returns undefined for missing acceptance file. |
| AC3 | acceptance_criterion | After `adv_change_update` with `executiveSummary` content, `state.artifacts.executiveSummary` has both `path` and `contentHash` set in Temporal state | pass | acceptanceContractBlockers resilience test passes: content in state.documents.executiveSummary with missing state.artifacts.executiveSummary metadata no longer blocks. |
| AC4 | acceptance_criterion | `acceptanceContractBlockers` no longer reports `ACCEPTANCE_EXECUTIVE_SUMMARY_MISSING` when executive summary content was persisted via `adv_change_update` | pass | Same test as AC3 — the resilience fix prevents ACCEPTANCE_EXECUTIVE_SUMMARY_MISSING when content exists but metadata is absent. |
| AC5 | acceptance_criterion | All existing tests pass (`pnpm test`, `pnpm run check`) | pass | pnpm run check passes (typecheck + lint + format:check). pnpm test passes: 250 test files, 3377 tests. |
| C1 | constraint | No changes to Temporal workflow signal definitions or handler signatures | respected | No changes to Temporal workflow signal definitions or handler signatures. Only modified gate-readiness.ts (readiness check logic) and change.ts (tool schema). |
| C2 | constraint | `acceptance` content remains auto-generated by the workflow — agents cannot write it via `adv_change_update` | respected | acceptance content remains auto-generated by the workflow via renderAcceptanceProjection. No write path added for acceptance in adv_change_update. |
| C3 | constraint | P23 campsite: extend existing patterns, don't create new ones | respected | Extended existing include-flag pattern (exact mirror of executiveSummary). Extended existing blocker-check pattern (added fallback condition). No new patterns created. |

