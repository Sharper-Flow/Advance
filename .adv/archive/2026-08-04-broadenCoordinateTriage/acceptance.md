# Acceptance

Reviewed at: 2026-08-04T02:08:42.861Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | **sc-1** — In a project with zero active Epics, `/adv-coordinate` produces refactor-coverage findings rather than stopping after the inventory. | pass | adv-coordinate.md Phase 6.5 runs regardless of active Epics; Phase 1 terminator narrowed to Epic-dependent findings. Asserted in advance-epics-assets. Run tr_msdzpxc1_e3f0b6fc |
| SC2 | success_criterion | **sc-2** — In a project mixing linked and unlinked changes, an Epic-unlinked change receives repository-overlap and sequencing analysis on the same terms as a linked one, carrying the same evidence labels. | pass | Phase 3 Epic-unlinked comparison row with identical evidence labels; Phase 5 includes unlinked changes. Run tr_msdzpxc1_e3f0b6fc |
| SC3 | success_criterion | **sc-3** — `/adv-triage` portfolio balance includes nonterminal changes with no linked issue. | pass | portfolio-report renders unlinked nonterminal changes; rq-backlogCoord10.1 cases. Run tr_mse0e2x9_2bac24e2 |
| SC4 | success_criterion | **sc-4** — An Epic-unlinked change can appear in `ordered_next` when its own signals warrant it, with at least one Epic-linked change present. | pass | unlinkedRank; test asserts active unlinked change wins ordered_next over later-Epic work. Run tr_mse0e2x9_2bac24e2 |
| SC5 | success_criterion | **sc-5** — Coordinate honours `hasMore` / `resumeHint` until the in-flight population is exhausted rather than treating one page as complete. | pass | Phase 1 hasMore/resumeHint exhaustion sentence asserted verbatim. Run tr_msdzpxc1_e3f0b6fc |
| AC1 | acceptance_criterion | **ac-1** — `rq-epicCoordinateChangeCoverage01` is added to `.adv/specs/advance-epics/spec.json` with scenarios `.1` through `.4` as drafted in design, and `/adv-coordinate.md` reflects it: the refactor-coverage audit is no longer gated on active Epics, and Phases 3 and 5 cover Epic-unlinked changes. | pass | advance-epics v1.14.0 rq-epicCoordinateChangeCoverage01 scenarios .1-.4; adv-coordinate Phases 3/5/6.5 updated. Runs tr_msdxxey3_2d1ae9cd and tr_msdzpxc1_e3f0b6fc |
| AC2 | acceptance_criterion | **ac-2** — `rq-epicAdvisoryRankReachability01` is added to `.adv/specs/advance-epics/spec.json`, and `computeChangeRank` in `plugin/src/projection/resume-projection.ts` no longer returns an unconditional maximum rank for changes lacking `epic_membership`. Relative order among Epic-linked entries is preserved. | pass | rq-epicAdvisoryRankReachability01 added; computeChangeRank returns finite conditional ranks, never MAX_SAFE_INTEGER; Epic formula unchanged. Run tr_mse0e2x9_2bac24e2 |
| AC3 | acceptance_criterion | **ac-3** — `plugin/src/projection/resume-projection.test.ts` no longer asserts that Epic-unlinked changes must lose to every Epic-linked change, and instead asserts reachability per `rq-epicAdvisoryRankReachability01.1` and preserved Epic-linked ordering per `.2`. | pass | Six reachability and non-inversion tests including band-boundary uniqueness; stale MAX_RANK comment corrected. Run tr_mse0e2x9_2bac24e2 |
| AC4 | acceptance_criterion | **ac-4** — `rq-backlogCoord10` is added to `.adv/specs/backlog-coordination/spec.json` with scenarios `.1` through `.4` as drafted, and `plugin/src/tools/triage/portfolio-report.ts` renders nonterminal changes carrying no linked issue. | pass | backlog-coordination v1.6.0 rq-backlogCoord10 scenarios .1-.4; renderer covers unlinked changes. Run tr_mse0e2x9_2bac24e2 |
| AC5 | acceptance_criterion | **ac-5** — Membership in the unlinked-change set is derived from typed change state only. No title similarity, title prefix, or agent inference determines whether a change appears. A test asserts that a change whose title lacks any defect prefix still appears when it is nonterminal and unlinked. | pass | Membership from typed state only; tests prove a non-prefixed change appears and hint presence leaves row count and row set identical. Run tr_mse0e2x9_2bac24e2 |
| AC6 | acceptance_criterion | **ac-6** — The defect ranking hint influences ordering only, renders its evidence source, and uses `origin.kind` as primary with title prefix secondary. A test asserts the hint never removes, filters, or suppresses a row. | pass | origin.kind primary, fix prefix secondary; renders source and evidence; participates only in the sort comparator. Run tr_mse0e2x9_2bac24e2 |
| AC7 | acceptance_criterion | **ac-7** — `priority:*` labels remain GitHub-issue-scoped. No code path writes a `priority:*` label to an ADV change. | pass | No priority label written to a change; test asserts unlinked row has no priority token while issue-linked row keeps priority:high. Run tr_mse0e2x9_2bac24e2 |
| AC8 | acceptance_criterion | **ac-8** — `plugin/src/advance-epics-assets.test.ts` no longer enforces that the no-active-Epics short-circuit precedes the refactor-coverage audit, and asserts the new reachability contract instead. | pass | Additive assertions for reachability, unlinked coverage, pagination, and C1 guard (65 insertions, 0 deletions). First clause vacuous: lines 213-219 encode rq-epicCoordinateProjectInventory01.3 and are deliberately preserved. Run tr_msdzpxc1_e3f0b6fc |
| AC9 | acceptance_criterion | **ac-9** — `kind_hint` is either wired to a real producer and consumer or removed from `/adv-triage.md`. It does not remain a declaration with no mechanism. | pass | Resolved via the wired branch: kind_hint retained in adv-triage.md Phase 2 with consumer chain named (reclassify to ADV Type to Phase 4c bug loop) and documented as distinct from defectHint |
| AC10 | acceptance_criterion | **ac-10** — `/adv-coordinate.md` states explicitly that inventory reads follow `hasMore` / `resumeHint` until exhausted. | pass | Phase 1 hasMore/resumeHint sentence asserted verbatim by advance-epics-assets. Run tr_msdzpxc1_e3f0b6fc |
| AC11 | acceptance_criterion | **ac-11** — `pnpm run check` passes and the affected test files pass. | pass | pnpm run check exit 0 tr_mse0ggld_fe469469; smoke 98 tr_msdzz64i_aae7e635; targeted 55 tr_mse0e2x9_2bac24e2. Four pre-existing temporal eslint warnings, zero errors |
| C1 | constraint | **con-1** — Existing `/adv-coordinate` boundaries hold: no task creation, no gate completion, no Epic auto-enrollment, no Jira-like planning primitives, no CLI mutation verbs, no direct ADV-state filesystem access. Durable Epic actions stay approval-gated. | respected | Command Boundary MUST NOT line byte-unchanged; new asset test asserts the full clause list verbatim as a regression guard |
| C2 | constraint | **con-2** — Epic membership stays optional and Epic order stays advisory. Neither may block gates, tasks, promotion, or change progress. | respected | Epic membership optional and order advisory; Epic rank formula unchanged; preserved-order test passes |
| C3 | constraint | **con-3** — Command ownership boundaries hold: `/adv-cleanup` retains change closure, `/adv-coordinate` retains Epic repair, `/adv-triage` retains issue linkage. Neither expansion absorbs a neighbour's authority. | respected | No closure authority added; coordinate Boundary row states /adv-cleanup retains change closure |
| C4 | constraint | **con-4** — `rq-backlogCoord05` bounded Visibility rules remain in force. No per-change state read loop is introduced to annotate the pool. | respected | No per-change read loop introduced; bounded Visibility rules of rq-backlogCoord05 preserved in the rq-backlogCoord10 body |
| C5 | constraint | **con-5** — Heuristics may influence ordering only. They may not own membership, closure, suppression, gate completion, or spec compliance (P33). | respected | Heuristic confined to the sort comparator; non-removal pinned by identical row count and row set test |
| C6 | constraint | **con-6** — Zod schemas remain authoritative. If a public schema changes, `pnpm run schemas:generate` runs and `pnpm run schemas:check` stays green. | respected | No public Zod schema changed; schemas:check green within tr_mse0ggld_fe469469 |
| C7 | constraint | **con-7** — Deployed command mirrors under `~/.config/opencode/command/` are synced through `scripts/deploy-local.sh`, never hand-edited. | respected | No deploy run from the worktree; deploy-local.sh never invoked; deployed mirrors untouched |
| DONT1 | avoidance | **av-1** — Do not add a structural `change_kind` enum, and do not backfill existing changes with a classification field. Recorded as a follow-up only. | respected | No change_kind field or structural defect-classification requirement introduced; deferred as a recorded follow-up |
| DONT2 | avoidance | **av-2** — Do not let a title prefix or `origin.kind` value decide whether a change appears in any report. | respected | No prefix or origin.kind value gates appearance; AC5 test proves a non-prefixed change still appears |
| DONT3 | avoidance | **av-3** — Do not compensate for the `computeChangeRank` bias inside the command contracts while leaving the kernel unchanged (P40). | respected | Rank fix confined to resume-projection.ts; neither command contract contains compensating rank logic |
| DONT4 | avoidance | **av-4** — Do not write `priority:*` labels, or any parallel priority field, onto ADV changes. | respected | No priority label or parallel priority field on a change; hint weight is in-memory only; explicit MUST NOT bullet added |
| DONT5 | avoidance | **av-5** — Do not make `/adv-triage` close, cancel, or archive changes; that authority stays with `/adv-cleanup`. | respected | Triage gained no close, cancel, or archive authority; cleanup section still routes to /adv-cleanup |
| DONT6 | avoidance | **av-6** — Do not generate, echo, stage, or commit `ROADMAP.md` or `.adv/roadmap-snapshot.json` (`rq-backlogCoord07`). | respected | No ROADMAP.md or roadmap-snapshot reference; existing retirement assertion still passes. Run tr_msdzpxc1_e3f0b6fc |
| DONT7 | avoidance | **av-7** — Do not resurrect the retracted `adv_change_list` pagination-truncation claim; the tool paginates correctly. | respected | No adv_change_list truncation claim in any shipped artifact; retraction catalogued in design.md with the P39 population-mismatch root cause |

