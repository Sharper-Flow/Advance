# Acceptance

Reviewed at: 2026-06-15T23:51:10.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | `adv_change_show` never implies a nonexistent active artifact file is readable for Temporal-only artifact content. | pass | Review/remediation verified no agent-facing active Temporal artifact path is exposed as readable: change_show artifact metadata, gates, and _contextSnapshot gate evidence normalize missing/non-readable paths; targeted tests and final full suite pass. |
| SC2 | success_criterion | Agents can retrieve proposal/problem/agreement/design/executive-summary/acceptance content through ADV tool output or packet-provided content. | pass | Artifact content remains accessible through adv_change_show include fields and worker packet guidance; change.test verifies Temporal document content returned without path; commands inject inline content/include guidance. |
| SC3 | success_criterion | Worker instructions consistently forbid direct reads of external ADV state artifact files. | pass | Agent/command policy blocks direct external ADV artifact reads across ADV workers; asset tests enforce artifact-wide filenames, include guidance, readable caveat, and artifacts.*.path warning. |
| SC4 | success_criterion | Regression tests cover active Temporal-only artifacts, legacy disk fallback, archive fallback, and prompt/agent asset guidance. | pass | Regression coverage spans types/artifacts, store-temporal changes, change-state, gate-readiness, workflows signal handlers, change tools, specs/assets, and command/agent asset tests. Final bin/oc-test full passed after review remediation. |
| AC1 | acceptance_criterion | Given active artifact content exists in Temporal `state.documents` and no matching active `.md` file exists, when `adv_change_show include.<artifact>` runs, then artifact content is returned via the include field and no response field presents a nonexistent active file path as readable. | pass | change.test covers Temporal document content returned via include without artifact path and snapshot/top-level gate path stripping; changes.test verifies Temporal metadata signal source:temporal/readable:false/no path. |
| AC2 | acceptance_criterion | Given legacy disk-backed or archived artifact content exists, when artifact readback runs, then legacy disk and archive-bundle fallback content still works. | pass | change.ts preserves Temporal-first then disk/archive fallback read paths; change.test verifies existing readable disk path preserved; full suite passed. |
| AC3 | acceptance_criterion | Given a sub-agent receives a design/research/review packet needing ADV artifacts, when it follows instructions, then it uses inline content or ADV tools and does not read `~/.local/share/.../plugins/advance/**/{proposal,problem-statement,agreement,design,executive-summary,acceptance,change}.md/json` directly. | pass | adv-design, adv-research, and adv-refactor commands now load artifacts via include fields/inline packets and forbid dereferencing external artifacts.*.path; subagent-reports asset test enforces anchors. |
| AC4 | acceptance_criterion | Given `adv-researcher` and peer ADV agents are inspected, when asset tests run, then each relevant agent/command contract contains the artifact-wide ADV state access rule and direct-read failure recovery rule. | pass | subagent-reports-spec-assets plus agent asset tests verify adv-researcher and peer agents contain artifact-wide state access policy, direct-read recovery rule, readable caveat, and include guidance. |
| AC5 | acceptance_criterion | Given artifact metadata/evidence is emitted by gate or change read surfaces, when tests inspect active Temporal-only changes, then metadata distinguishes readable materialized paths from non-file/Temporal sources. | pass | ArtifactMetadata has optional path plus source/readable; change-state normalizes blank paths; gate-readiness emits path only for readable metadata; change_show re-validates path existence. Targeted tests and final full suite pass. |
| AC6 | acceptance_criterion | Given recovery or archive paths intentionally materialize artifact files, when those paths produce artifact evidence, then real filesystem paths remain available and verified. | pass | Recovery/archive/disk paths are preserved only when materialized/readable: review remediation derives recovery readable/path from fileExists and adds tests for materialized and non-materialized cases; archive/disk fallback unchanged. |
| C1 | constraint | Temporal `state.documents` remains canonical for active artifact content. | respected | Temporal state.documents remains canonical for active artifact content; changes.ts emits content signals to documents and metadata source:temporal/readable:false with no active path. |
| C2 | constraint | Active artifact-content disk writes must not be reintroduced as the primary source of truth. | respected | store-temporal changes path no longer passes artifacts to legacy disk writes or synthesizes active paths; no-disk-writes invariant test remains green. |
| C3 | constraint | Agents and sub-agents must not be required to read external ADV state files directly. | respected | Workers/commands route artifact access through ADV tools and inline content; direct external ADV state file reads forbidden and recovery tells agents to stop retrying filesystem paths. |
| C4 | constraint | Legacy disk and archive-bundle fallback reads must remain supported. | respected | readArtifact/readArtifacts fallback behavior retained, ARTIFACT_FILENAME centralized, readable disk path preservation test passes, full suite passes. |
| C5 | constraint | The read-surface behavior change must be machine-readable and testable, not prose-only. | respected | Behavior is represented by typed ArtifactMetadata source/readable/path? contract, readback normalizers, gate-readiness helper, specs, and regression tests; not prose-only. |
| C6 | constraint | Cross-project `target_path` reads must follow the same artifact semantics as current-project reads. | respected | change_show readArtifacts now uses activeStore for target_path include reads; normalization applies to activeStore readback and absolute metadata paths. Review scanner identified this as corrected latent target_path issue. |
| DONT1 | avoidance | Do not fix the symptom only with prompt text while leaving misleading tool output unchanged. | respected | Structural tool output/state fixes implemented in types, store signals, change-state, gate-readiness, and change_show; prompt updates are secondary, not sole mitigation. |
| DONT2 | avoidance | Do not break recovery/archive evidence paths that intentionally materialize real files. | respected | Recovery readable/path now file-existence-backed; materialized recovery path test passes; disk/archive fallback unchanged; final full suite passes. |
| DONT3 | avoidance | Do not change app-repo worktree routing or ADV worktree lifecycle behavior as part of this change. | respected | No ADV/app worktree lifecycle code changed; git diff limited to specs, tool/storage/temporal artifact handling, prompts/commands, and tests. |
| DONT4 | avoidance | Do not broaden into unrelated sub-agent prompt quality cleanup. | respected | Prompt edits scoped to artifact state access policy; review scanner found no unrelated prompt cleanup or out-of-scope files. |
| DONT5 | avoidance | Do not hide artifact content from agents; replace fake file paths with reliable tool/payload access. | respected | Content remains available via include fields and packet guidance; tests verify _design/Temporal content returned while false filesystem paths are omitted. |

