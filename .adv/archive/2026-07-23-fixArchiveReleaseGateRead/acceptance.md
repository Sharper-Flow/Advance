# Acceptance

Reviewed at: 2026-07-23T20:26:58.467Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1 — Query timeout tolerates dev-server / long-history latency (Gap 1):** `QUERY_TIMEOUT_MS` (shared.ts:326) is overridable via `ADV_TEMPORAL_QUERY_TIMEOUT_MS` and its default is raised so a gate/state query slower than the old 5s cap but within the new default succeeds in `store.gates.get` where it previously timed out and fell back to a stale projection. | pass | RED tr_mrxy56xv → GREEN tr_mrxy5rgl (10 tests): QUERY_TIMEOUT_MS env-configurable via ADV_TEMPORAL_QUERY_TIMEOUT_MS, finite-positive validation, default raised 5s→15s |
| AC2 | acceptance_criterion | **AC2 — `fireSignalAndRefresh` no longer re-poisons the cache (Gap 2a):** on the confirmed-done path, `fireSignalAndRefresh` (gate.ts:1157) drops the cache entry (`store.changes.invalidate`) instead of issuing a racing `refresh` readback, mirroring archive-gate.ts:1211. A subsequent `store.gates.get` queries fresh and observes `done`. | pass | gate.ts release-gate caller invalidates cache after fireSignalAndRefresh confirms done; createMockStore invalidate spy added; no-breakage (125 tests, tr_mrxydwys) |
| AC3 | acceptance_criterion | **AC3 — Archive `alreadyDone` branch invalidates the cache (Gap 2b):** when `runReacquiringChangeQuery` returns `done` in `completeReleaseGateAfterFinalization` (archive-gate.ts:1143), the cache is invalidated before returning, so the second `verifyReleaseGateDurableForArchive` `store.gates.get` misses cache and observes `done` instead of a poisoned `pending`. | pass | RED tr_mrxy7c7z → GREEN tr_mrxy87k0: archive alreadyDone branch calls store.changes.invalidate before returning ok |
| AC4 | acceptance_criterion | **AC4 — Durable-proof disk read resolves to the live projection for archived-status changes (Gap 3):** `loadAuditedDiskReleaseGate` (archive-gate.ts:904) reads the **live disk projection** (where `saveRecoveredGateCompletion` writes) before the frozen archive-store snapshot. After disk-direct recovery writes `done`, the durable-proof read returns the `done` gate for an archived-status change (today returns null). | not_applicable | Gap 3 hypothesis disproven by independent validator: loadChange and saveRecoveredGateCompletion use identical store.paths.changes path — no archive-store-vs-live-disk disconnect. Real archive-blocking cause was Gaps 1+2 (query timeout + cache poisoning), now fixed. No additional fix needed. |
| AC5 | acceptance_criterion | **AC5 — Forge guard preserved:** a non-shipped change with a forged/unrecognized recovery audit is still rejected by `verifyReleaseGateDurableForArchive` (no regression to `fixDurableProofFallback` / #305 AC4). The `RELEASE_GATE_RECOVERY_REASONS` allowlist and the strict evidence-match guard are unchanged. | pass | tr_mrxyhs3b: two-path forge-guard regression — non-shipped+forged reason rejected (strict match); shipped+done(no audit, non-matching evidence) accepted via shipped bypass. RELEASE_GATE_RECOVERY_REASONS allowlist unchanged. |
| AC6 | acceptance_criterion | **AC6 — No regression:** existing `archive-gate.test.ts` + `change.archive-phase9.test.ts` suites (62 tests) stay green; `pnpm run check` green (schemas, typecheck, lint, format, manifests, test-isolation, lockfile). | pass | pnpm run check green (tr_mrxyfrvr: schemas/typecheck/lint/format/manifests/test-isolation/lockfile); 153/154 tests pass — 1 failure pre-existing (retry-wrapper unhandled error, proven identical on trunk, unrelated to this change) |
| C1 | constraint | **C1:** Preserve `rq-releaseProjectionDurability01.1` — the store-backed read remains the authority for the durable proof. | respected | store-backed read remains authority (strengthened: now observes done via raised timeout + cache invalidation); no caller-trusted bypass |
| C2 | constraint | **C2:** Preserve `rq-releaseRepairRecovery01` — the audited recovery writer (`saveRecoveredGateCompletion`) remains completed/poisoned-workflow only; do NOT broaden its authority. | respected | recovery writer (saveRecoveredGateCompletion) authority unchanged — completed/poisoned-workflow only; not broadened |
| C3 | constraint | **C3:** Do not add new `RELEASE_GATE_RECOVERY_REASONS` allowlist entries without audit. | respected | RELEASE_GATE_RECOVERY_REASONS allowlist unchanged — no new entries added |
| C4 | constraint | **C4:** No change to git-finalize / Phase 9 evidence (already returns `shipped` correctly). | respected | git-finalize / Phase 9 evidence path untouched |
| C5 | constraint | **C5:** Cache invalidation is cache-drop only (`store.changes.invalidate`) — no readback, no disk write on the invalidate path (consistent with #305). | respected | invalidate is cache-drop only (store.changes.invalidate, #305 primitive) — no readback, no disk write on the invalidate path |
| DONT1 | avoidance | **AV1:** Do not turn the durable-proof read into a caller-trusted flag or bypass the evidence-match guard. | respected | no caller-trusted flag or evidence-match bypass introduced |
| DONT2 | avoidance | **AV2:** Do not make the archive store mutable from arbitrary paths — only the recovery writer / convergence step may update it. | respected | archive store not made mutable from recovery/arbitrary paths — only convergence writes it |
| DONT3 | avoidance | **AV3:** Do not introduce polling loops or unbounded waits to work around slow queries — the env-configurable timeout is bounded. | respected | no polling loops or unbounded waits — env-configurable timeout is bounded (finite-positive) |
| DONT4 | avoidance | **AV4:** Do not touch unrelated store paths (wisdom, epics, deltas) — scope is the archive release-gate read convergence. | respected | no unrelated store paths touched (wisdom, epics, deltas) — scope is archive release-gate read convergence |
| DONT5 | avoidance | **AV5:** Do not broaden the fix into a general "make reads disk-authoritative" refactor — that is `makeReadsDiskAuthoritative`'s scope; this change is the minimal read-convergence patch. | respected | minimal read-convergence patch — not broadened into the disk-authoritative refactor (makeReadsDiskAuthoritative's scope) |

