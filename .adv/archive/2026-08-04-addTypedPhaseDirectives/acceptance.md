# Acceptance

Reviewed at: 2026-08-04T21:08:17.407Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | Starting `/adv-review` produces a transcript the user can scroll and parse — the phase procedure appears as one collapsed entry rather than inline prose. | pass | Directive arrives intact inside the phase-plan tool result (change.test.ts contentHash-completeness assertion) — the exact payload the OpenCode TUI renders as one collapsed entry. Live visual confirmation deferred to post-deploy per design + T7 reclassification; surfaced at acceptance checkpoint, no user objection. |
| SC2 | success_criterion | The full procedure remains available on demand by expanding that entry. | pass | Collapsed tool results are expandable in the TUI (discovery rendering evidence, sst/opencode sources + binary 1.18.13); directive content is complete in the result (47,324 chars verified vs sha256 hash). |
| SC3 | success_criterion | `/adv-review` produces the same gate outcome, checkpoints, and evidence as before the change. | pass | Gate outcome unchanged: no workflow/gate mutation paths touched (assembler pure; replay-determinism 15/15 without patch marker). Acceptance review READY 0 findings. |
| SC4 | success_criterion | Instructions belonging to a branch the phase did not enter do not appear in the session at all. | pass | Directive content is happy-path-only (registry holds only the entered-branch procedure); non-entered branch instructions are not emitted (AC2 identity behavior proven in phase-directive.test.ts). |
| AC1 | acceptance_criterion | Given an actionable phase plan for the review gate, when the phase launcher reads the plan, then the plan carries an executable directive covering that gate's happy path. | pass | withPhaseDirective attaches PHASE_DIRECTIVES['adv-review'] when plan.kind==='actionable' && plan.command==='adv-review' (phase-directive.test.ts 9/9; change.test.ts attach integration, runId tr_msf4qex2_2df3c432). |
| AC2 | acceptance_criterion | Given a non-actionable phase plan variant — blocked, recovery-required, approval-required, terminal, or degraded — when the plan is read, then no directive is emitted and the blocking reason is surfaced instead. | pass | directive field exists only on ActionablePhasePlanSchema (structural exclusion); assembler returns identity for non-actionable variants (phase-directive.test.ts). Blocking reason still surfaced by plan. |
| AC3 | acceptance_criterion | Given a failing directive read, when the launcher exhausts its bounded retry attempts, then the phase halts and surfaces the failure rather than proceeding on partial instructions. | pass | Launcher decision-table row: read error -> bounded retry x2 -> halt + surface failure (adv-review.md; anchor asserted in phase-directive-launcher.test.ts). |
| AC4 | acceptance_criterion | Given a directive read that fails once and succeeds on retry, when the retry returns an actionable plan, then the phase proceeds normally with no user intervention. | pass | Launcher decision-table row: fail-once-then-retry-success -> proceed normally (adv-review.md; anchor asserted in phase-directive-launcher.test.ts). |
| AC5 | acceptance_criterion | Given the inline fallback retained in the piloted command file, when it is compared against the directive source, then it is verified as a strict subset, and any drift between them fails the check. | pass | phase-directive-launcher.test.ts AC5 strict-subset check: FALLBACK BEGIN/END-delimited inline fallback verified token-subset of directive content; drift fails the check. |
| AC6 | acceptance_criterion | Given a phase launcher, when it is inspected for procedure that the directive already supplies, then manual duplication is rejected. | pass | phase-directive-launcher.test.ts AC6 no-duplication check: launcher inspected for directive-supplied procedure; manual duplication rejected (directive-first concat order). |
| AC7 | acceptance_criterion | Given the piloted change, when its command-asset invariants are checked, then every previously pinned phrase and fenced block is still satisfied. | pass | Composed-surface helper readCommandSurface retargeted 10 pinned-invariant suites; all previously pinned phrases/fenced blocks still satisfied — 397/397 across 16 suites (runId tr_msf4qex2_2df3c432); matrix-uniqueness suite asserts directive content directly. |
| AC8 | acceptance_criterion | Evidence: unchanged Temporal replay behavior is proven by the existing replay-determinism suite passing without a new patch marker. | pass | Replay-determinism suite 15/15 unmodified; no PHASE_PLAN_VERSION bump, no wf.patched marker (runId tr_msf2gnl8). |
| C1 | constraint | Must not add an ADV tool or an `adv_change_show` include flag. | respected | Branch diff review: no tool-registry additions, no new include flag (lean shaping consumes the existing include map). Zero new ADV tools. |
| C2 | constraint | Must not bump `PHASE_PLAN_VERSION` or add a `wf.patched` marker for this change. | respected | No PHASE_PLAN_VERSION bump, no wf.patched; replay suite 15/15 passes unmodified. |
| C3 | constraint | Must not place authored directive content in any module reachable from the workflow bundle's static import graph. | respected | phase-directive-content.ts is host-only; workflow-bundle-boundary.test.ts guard appended asserts the authored registry is unreachable from the worker bundle root. |
| C4 | constraint | Must keep `/adv-review` functional when the directive read is unavailable. | respected | Inline fallback retained as strict subset (AC5 check); launcher remains functional when directive read unavailable (fallback path in decision table). |
| C5 | constraint | Must preserve contract tokens verbatim — tool names, gate IDs, MUST/NEVER, slash commands, code blocks, status markers. | respected | Directive content ported verbatim-token from the prior launcher; launcher protocol anchors asserted by phase-directive-launcher.test.ts. |
| DONT1 | avoidance | Do not set `subtask: true` on phase commands. ADV has seven human checkpoints in the primary context; a child session strands them, and upstream sst/opencode issue #10802 documents the parent-appears-stuck failure mode. | respected | Branch diff contains no subtask:true usage (reviewer diff audit). |
| DONT2 | avoidance | Do not delete recovery or reference material. Relocate it, branch-scoped. | respected | Recovery/reference material relocated into the delimited fallback and decision table, not deleted (adv-review.md 142 lines retain fallback + protocol). |
| DONT3 | avoidance | Do not let the directive complete gates or grant authority. It is a read projection. | respected | withPhaseDirective is a pure function (no I/O, no gate mutation); directive is read-only plan projection data. No completion path consumes it. |
| DONT4 | avoidance | Do not patch, fork, or rebuild opencode core. | respected | Diff touches only plugin/ and .opencode/command/; no opencode core packages patched. |
| DONT5 | avoidance | Do not reintroduce `skill("adv-review-methodology")` or `skill("adv-apply-methodology")`. Both are recorded as stale/hallucinated references. | respected | No skill('adv-review-methodology') reintroduced in launcher or elsewhere in the diff. |
| OOS1 | out_of_scope | The other four lifecycle commands — prep, apply, harden, archive. The pilot outcome decides whether they follow. | not_applicable | Other lifecycle commands (prep/apply/harden/archive) conversion explicitly deferred past pilot; not attempted in branch diff. |
| OOS2 | out_of_scope | `adv-proposal`, `adv-discover`, `adv-design`, `adv-research` command bodies. | not_applicable | Out-of-scope item per approved agreement; not attempted in branch diff (acceptance reviewer diff audit). |
| OOS3 | out_of_scope | Cross-file deduplication of the repeated blocks — `Target Resolution` and `Key Tools` appear in 14 command files each. Deferred past the pilot; a single-command pilot cannot exercise it. | not_applicable | Cross-file dedup of Target Resolution/Key Tools recorded as follow-up candidate; not attempted in branch diff. |
| OOS4 | out_of_scope | `ADV_INSTRUCTIONS.md` and `adv.md` runtime prompt assembly, owned by archived `refactorAdvPrompt`. | not_applicable | Out-of-scope item per approved agreement; not attempted in branch diff (acceptance reviewer diff audit). |
| OOS5 | out_of_scope | Broader ADV tool-surface reduction, owned by Epic `optimizeAdvToolSurface`. | not_applicable | Out-of-scope item per approved agreement; not attempted in branch diff (acceptance reviewer diff audit). |
| OOS6 | out_of_scope | The upstream sst/opencode `display-summary` / synthetic-body feature request. File separately; do not block on it. | not_applicable | Out-of-scope item per approved agreement; not attempted in branch diff (acceptance reviewer diff audit). |
| OOS7 | out_of_scope | Procedural conformance checking and checkpoint reply-instruction canonicalization. Both are opportunity-scout follow-ups. | not_applicable | Out-of-scope item per approved agreement; not attempted in branch diff (acceptance reviewer diff audit). |

