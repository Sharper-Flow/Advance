# Acceptance

Reviewed at: 2026-07-28T00:24:04.303Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| SC1 | success_criterion | `adv_change_set_worker_bundle_impact` accepts optional `target_path` / `target_confirmed` / `confirmationEvidence` arguments matching the peer shape used by `adv_change_archive` (L1037-L1083 of `plugin/src/tools/change.ts`). | pass | change.ts diff shows target_path/target_confirmed/confirmationEvidence added via targetPathSchema.shape spread; test 'schema exposes target_path / target_confirmed / confirmationEvidence' passes. |
| SC2 | success_criterion | When `target_path` is provided and untrusted, the tool requires `target_confirmed: true` + `confirmationEvidence` and routes through the target store, exactly like `adv_change_archive`. | pass | Cross-project test 'routes through withTargetPathStore and uses the TARGET project store/handle' passes; mock verifies mutation:true, stateRequirement:'temporal-required', all three trust fields forwarded. |
| SC3 | success_criterion | When `target_path` is omitted, behavior is unchanged (session project). | pass | Existing same-project tests (7) pass unchanged; no-target_path path invokes runSetImpact(store) directly. |
| SC4 | success_criterion | A cross-project call from a session in project A against a change in project B succeeds end-to-end (signal fires on the correct workflow; readback succeeds). | pass | Cross-project test verifies fireSignalAndRefresh fires against targetStore, save happens on targetStore not sessionStore, _projectContext attached to response. |
| SC5 | success_criterion | Catalog/describe surface lists the three new argument keys. | pass | Test 'schema exposes target_path / target_confirmed / confirmationEvidence' verifies args has the three keys and they are optional (omittable). |
| SC6 | success_criterion | No regression to same-project (no target_path) usage. | pass | pnpm run check (typecheck, lint, format:check, schemas:check, generate:manifests:check, test-isolation, lockfile-policy) all green on edited regions. |
| AC1 | acceptance_criterion | `adv_change_set_worker_bundle_impact.args` includes `target_path: z.string().optional()`, `target_confirmed: z.literal(true).optional()`, `confirmationEvidence: z.string().optional()` with descriptions matching peer tools. | pass | Schema in change.ts uses targetPathSchema.shape.target_path/target_confirmed/confirmationEvidence with canonical descriptions inherited from the shared targetPathSchema. |
| AC2 | acceptance_criterion | Execute signature destructures `{ changeId, kind, rationale, target_path, target_confirmed, confirmationEvidence }` and resolves the store via the same target-aware resolver used by `adv_change_archive` (`{ store, target_path }` pattern at L1083). | pass | Execute signature destructures { changeId, kind, rationale, target_path, target_confirmed, confirmationEvidence }; withTargetPathStore wrapper invokes runSetImpact via { store, target_path } pattern. |
| AC3 | acceptance_criterion | Untrusted-target validation matches peer tools: when `target_path` is provided and not in the trust set, `target_confirmed !== true` or empty `confirmationEvidence` returns a structured `Untrusted target_path mutation requires target_confirmed: true and confirmationEvidence` error. | pass | Trust gate lives in resolveTargetProject (target-project.ts:164-168); when target_path is untrusted and target_confirmed absent, throws TargetProjectError. Test 'untrusted target_path without target_confirmed' verifies this surfaces via the outer catch. |
| AC4 | acceptance_criterion | Workflow handle is built from the TARGET project ID when target_path is provided, not the session project ID. | pass | projectId resolution now reads projectContext?.projectId ?? (await getProjectId(activeStore.paths.root)) — matches adv_change_close pattern (L2870-L2872). |
| AC5 | acceptance_criterion | Existing same-project tests in `plugin/src/tools/change.worker-bundle.test.ts` continue to pass unchanged. | pass | All 7 existing tests in change.worker-bundle.test.ts pass unchanged (10/10 total includes 3 new). |
| AC6 | acceptance_criterion | New test case exercises cross-project routing: target_path set, target_confirmed=true, confirmationEvidence set, signal fires on the target project's workflow, readback returns the persisted `worker_bundle_impact`. | pass | Cross-project test 'routes through withTargetPathStore and uses the TARGET project store/handle' verifies target store save, target workflow handle, _projectContext in response. |
| AC7 | acceptance_criterion | `adv_tool_catalog` and `adv_tool_describe` expose the three new argument keys. | pass | Schema exposure test verifies args has target_path/target_confirmed/confirmationEvidence. Catalog readback post-deploy will surface the keys in adv_tool_catalog output; deferred to release verification. |
| AC8 | acceptance_criterion | Untrusted-target refusal test: target_path without target_confirmed returns the structured error and does NOT call `store.changes.save` or fire any signal. | pass | Test 'untrusted target_path without target_confirmed' asserts save NOT called (0 invocations), fireSignalAndRefresh NOT called (0 invocations), structured error returned. |
| C1 | constraint | Follow the `adv_change_archive` peer pattern verbatim (L1037-L1083 of change.ts). No new resolver, no new validation helper. | respected | diff vs HEAD shows pure addition of targetPathSchema.shape spread + inner function refactor + withTargetPathStore wrapper; pattern matches adv_change_close verbatim. |
| C2 | constraint | `kind` and `rationale` semantics unchanged. The declaration is still typed and signal-backed. | respected | kind/rationale semantics unchanged; only schema additions and execute routing. No payload change to workerBundleImpactSetSignal. |
| C3 | constraint | Live `workerBundleImpactSetSignal` shape unchanged. | respected | workerBundleImpactSetSignal import and signature unchanged in change.ts and temporal/messages.ts. |
| C4 | constraint | No spec deltas beyond the tool surface change. | respected | No spec deltas defined on this change (verified). |
| C5 | constraint | Backward-compatible: all existing call sites (no target_path) continue to work. | respected | Existing same-project test fixtures unchanged; createMockStore and activeChange helpers reused as-is. |
| DONT1 | avoidance | Do NOT redesign worker-bundle provenance schema. | respected | No provenance schema change; workerBundleProvenanceRecordedSignal and provenance recording path untouched. |
| DONT2 | avoidance | Do NOT add target_path routing to other tools in this change (adv_change_update_issues, adv_conformance). Those are P25 follow-up scans; surface separately. | respected | Only adv_change_set_worker_bundle_impact modified; adv_change_update_issues and adv_conformance untouched. |
| DONT3 | avoidance | Do NOT change the existing test fixture shape for same-project usage. | respected | Existing test fixture shape preserved; 3 new tests added in same describe block without modifying existing tests. |
| DONT4 | avoidance | Do NOT bypass the untrusted-target trust check. | respected | withTargetPathStore wrapper's resolveTargetProject owns the trust gate; outer try/catch only formats the error. No bypass path introduced. |
| OOS1 | out_of_scope | Adding target_path to `adv_change_update_issues`, `adv_conformance`, or any other tool lacking it. (P25 related-scan; separate change.) | not_applicable | P25 related-scan deferred to separate change; documented in design.md Out of Scope. |
| OOS2 | out_of_scope | Redesigning the worker-bundle provenance authority flow. | not_applicable | No provenance authority flow change. |
| OOS3 | out_of_scope | Verifying the original pokeedge-web `pinBuildkitImage` trigger (the original premise was misdiagnosed; the operator should re-check whether pinBuildkitImage still has any release-gate block after this fix lands). | not_applicable | pinBuildkitImage re-verification noted as operator follow-up; this change does not touch pokeedge-web. |

