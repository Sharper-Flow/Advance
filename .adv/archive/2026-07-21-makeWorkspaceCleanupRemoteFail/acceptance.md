# Acceptance

Reviewed at: 2026-07-21T00:44:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | **AC1** — When `findWorkspaceByDirectoryChecked` returns `{ok: true, workspace: null}` (no peer workspace owns the dir), deletion proceeds. Existing behavior preserved. | pass | tk-4aa6068e8954 preserves the {ok:true, workspace:null} early return at index.ts:1501; covered by existing test 'preserves walk-up behavior' in index-delete.test.ts and full suite tr_mrtxeg8b_c323a60d (239 pass). |
| AC2 | acceptance_criterion | **AC2** — When `findWorkspaceByDirectoryChecked` returns `{ok: true, workspace: <handle>}` (a peer workspace owns the dir), the existing `deleteAdvWorkspace` call runs and may block deletion on its own failure. Existing behavior preserved. | pass | tk-4aa6068e8954 preserves the {ok:true, workspace:<handle>} -> deleteAdvWorkspace path; WORKSPACE_CLEANUP_FAILED still returned on workspace-delete failure. Existing test at index-delete.test.ts:440 (workspace cleanup failed) still passes in tr_mrtxeg8b_c323a60d. |
| AC3 | acceptance_criterion | **AC3** — When `findWorkspaceByDirectoryChecked` returns `{ok: false, reason}` (remote unreachable / 5xx / malformed response), `cleanupOpenCodeWorkspaceForWorktree` returns `{ok: true, warning: <message>}` instead of `{ok: false, error: "WORKSPACE_OWNERSHIP_UNCERTAIN"}`. Deletion proceeds. The warning includes the failure reason. | pass | tk-4aa6068e8954 changed !lookup.ok branch (index.ts ~1488) to return {ok:true, warning}; tk-c4ed7637ef03 tests 'proceeds with a warning when workspace ownership is uncertain (list request fails)' and 'proceeds with a warning when the workspace list lookup throws' assert the new behavior. Targeted tr_mrtxk1j6_058d8953: 59 pass. |
| AC4 | acceptance_criterion | **AC4** — The local `isWorktreeInUse(worktreePath)` check (Linux `/proc/*/cwd`) upstream in `advWorktreeDelete` remains the primary safety gate. No change to its behavior. | pass | isWorktreeInUse (in-use.ts) and its upstream check at index.ts:2237 are unchanged. in-use.test.ts passes in tr_mrtxcuxd_5eb23a55 (113 pass). |
| AC5 | acceptance_criterion | **AC5** — `WORKSPACE_OWNERSHIP_UNCERTAIN` error code is NOT removed from the union type (backward compat for callers and tests) but is no longer produced by the remote-failure path. | pass | OpenCodeWorkspaceCleanupResult union at index.ts:1467 retains both WORKSPACE_OWNERSHIP_UNCERTAIN and WORKSPACE_CLEANUP_FAILED members. Code is unreachable from !lookup.ok path but type-compatibility preserved. |
| AC6 | acceptance_criterion | **AC6** — Spec `rq-terminalCleanupSafety01` is amended (or a new scenario `rq-terminalCleanupSafety01.3` is added) documenting that local CWD scan is the safety authority and the remote workspace-list API is advisory for stale-entry cleanup. | pass | tk-f44f3d2543fe amended rq-terminalCleanupSafety01 body in both .adv/specs/worktree-lifecycle/spec.json and docs/specs/worktree-lifecycle.md; added scenario rq-terminalCleanupSafety01.3. schemas:check passed (tr_mrtxdsi7_779fd1c0); jq confirms scenario present and JSON valid. |
| AC7 | acceptance_criterion | **AC7** — A regression test asserts: remote-fail + local-clean → deletion proceeds and a warning is logged. | pass | tk-c4ed7637ef03 added explicit regression test 'remote workspace registry unreachable is advisory and surfaces reason in warning'. Reviewer remediation added log.warn assertions to two existing tests proving the warning is logged. tr_mrtxk1j6_058d8953: 59 pass. |
| C1 | constraint | No change to `isWorktreeInUse` behavior or its Linux-only graceful-degradation contract. | respected | No change to isWorktreeInUse or in-use.ts. |
| C2 | constraint | No removal of the remote workspace-list call entirely — it still runs when reachable to clean up stale registry entries (preserves the happy path). | respected | Remote workspace-list call still runs via findWorkspaceByDirectoryChecked; reachable case preserves stale-entry cleanup. |
| C3 | constraint | No change to the `OpenCodeWorkspaceCleanupResult` type shape — `WORKSPACE_OWNERSHIP_UNCERTAIN` and `WORKSPACE_CLEANUP_FAILED` remain valid union members. | respected | OpenCodeWorkspaceCleanupResult shape unchanged; both error codes remain valid union members. |
| C4 | constraint | No new tool flag, env var, or opt-in toggle. The fix is the new default. | respected | No new flag/env/toggle introduced. Fix is unconditional new default. |
| C5 | constraint | No change to the `WORKSPACE_CLEANUP_FAILED` path (when remote succeeds and finds a workspace, but workspace deletion fails — still blocks). | respected | WORKSPACE_CLEANUP_FAILED path unchanged in source. |
| DONT1 | avoidance | Do not silently swallow remote failures — the warning must surface the reason for observability. | respected | Warning includes the upstream reason string; not swallowed. |
| DONT2 | avoidance | Do not bypass `deleteAdvWorkspace` when the remote lookup succeeds and returns a workspace handle — that path still owns stale-entry cleanup. | respected | deleteAdvWorkspace still called when lookup succeeds with workspace handle. |
| DONT3 | avoidance | Do not add retry logic, backoff, or queuing — out of scope; the local check already provides the safety answer. | respected | No retry/backoff/queue logic added. |
| DONT4 | avoidance | Do not change `findWorkspaceByDirectoryChecked`'s contract — callers (including tests) depend on the `{ok: false, reason}` shape for remote failures. | respected | findWorkspaceByDirectoryChecked signature and result shape unchanged. |
| DONT5 | avoidance | Do not touch `warpFlagEnabled` — the fix applies whether or not Warp mode is enabled; when Warp is disabled, the lookup short-circuits to `{ok: true, workspace: null}` already. | respected | warpFlagEnabled short-circuit at workspace-warp.ts:299 unchanged. |
| OOS1 | out_of_scope | macOS `lsof +D` fallback for `isWorktreeInUse` (current Linux-only graceful degradation remains). | missing |  |
| OOS2 | out_of_scope | Bulk cleanup of stale workspace registry entries left by past unreachable-during-delete events. | missing |  |
| OOS3 | out_of_scope | Telemetry/metrics on remote-fail frequency. | missing |  |
| OOS4 | out_of_scope | Changes to `adv_worktree_cleanup` reaper timing or pending-delete lifecycle. | missing |  |

