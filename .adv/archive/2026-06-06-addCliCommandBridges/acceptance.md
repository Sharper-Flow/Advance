# Acceptance

Reviewed at: 2026-06-05T19:40:00.000Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | `adv status` default renders active changes from live Temporal-backed state, not disk projection files. | pass | bin/lib/live-status.test.ts verifies live Temporal workflow states are listed/query-by-name and build live payload from queried rows; `bun test bin/` passed 66 tests. |
| AC2 | acceptance_criterion | `/adv-status` remains a thin shell-output bridge over `adv status --no-color`; command file contains no `adv_*` MCP fanout instructions. | pass | plugin/src/cli-bridge-contract.test.ts verifies `.opencode/command/adv-status.md` contains `!`adv status --no-color`` and forbids adv_* MCP fanout tokens; targeted test passed 16 tests. |
| AC3 | acceptance_criterion | Stale disk-only active changes are not shown as current live active changes. | pass | STATUS LIVE DEFAULT GUARDS assert `bin/adv` does not contain `join(root, "changes")` or `isDashboardActiveStatus`; live-status payload tests exclude disk-only IDs. |
| AC4 | acceptance_criterion | If live Temporal status is unavailable, `adv status` exits non-zero and prints clear remediation; it does not silently fall back to a disk table. | pass | `ADV_STATUS_TIMEOUT_MS=1000 bun bin/adv status --json` exited 2 with JSON `live:false`, `stale:false`, `error`, and remediation; no table/disk active fallback emitted. |
| AC5 | acceptance_criterion | Standalone `adv status` outside OpenCode uses the same live-default behavior. | pass | bin/adv.test.ts verifies standalone `adv status` no longer requires a disk ADV state directory; `bun test bin/` passed 66 tests. |
| AC6 | acceptance_criterion | `adv status --json` reports source/freshness/error fields sufficient for CI to distinguish live success from failure. | pass | bin/adv.test.ts and live failure runtime evidence verify JSON fields: source, live, stale, generated_at, project_id/counts/changes, error, remediation. |
| AC7 | acceptance_criterion | `rq-statusCliBridge01` is updated from disk-only/no-fallback law to live-default/no-silent-stale law. | pass | .adv/specs/advance-meta/spec.json rq-statusCliBridge01 updated to live-default/no-silent-stale law; cli-bridge-contract test asserts live Temporal-backed/fail-closed/disk-not-active wording. |
| AC8 | acceptance_criterion | Existing roadmap bridge/file-snapshot behavior remains unchanged unless explicitly re-scoped later. | pass | STATUS LIVE DEFAULT GUARDS verify `/adv-roadmap` bridge remains `!`adv roadmap --no-color`` and roadmap CLI keeps `unavailable_cli_file_mode`; roadmap parity/matrix targeted suite passed 23 tests. |
| AC9 | acceptance_criterion | No CLI mutation authority is added: no gate completion, archive, cancellation, task mutation, destructive repair, or automatic Temporal restart. | pass | NO-CLI-MUTATION GUARD and STATUS LIVE DEFAULT GUARDS scan status implementation for mutation dispatch/signal/start/update/restart patterns; targeted test passed. |
| AC10 | acceptance_criterion | Tests cover live success, stale disk exclusion, Temporal unavailable failure, `/adv-status` bridge no-fanout contract, and spec anchor drift. | pass | Coverage commands passed: `bun test bin/` 66 tests; `bin/oc-test targeted -- src/cli-bridge-contract.test.ts` 16 tests; CLI bridge/parity/matrix suite 23 tests. |
| C1 | constraint | Use `node:util parseArgs`; add no new heavy CLI dependencies. | respected | bin/adv still uses node:util parseArgs and no new package dependencies were added. |
| C2 | constraint | `--no-color` remains stable for slash-command bridge rendering. | respected | `--no-color` bridge flag preserved in `.opencode/command/adv-status.md` and tests; output remains stable for command rendering. |
| C3 | constraint | `--json` output must be CI-readable and structurally distinguish live success from live failure. | respected | `adv status --json` emits structured success/failure fields; failure evidence shows CI-readable live:false/stale:false/error/remediation. |
| C4 | constraint | Live status source must not silently blend stale disk-only active changes into the current active table. | respected | Guard test prevents active rows from disk `changes` directory in default status; live-status module owns Temporal read path. |
| C5 | constraint | Remediation text belongs in CLI stdout/stderr/JSON, not in `.opencode/command/adv-status.md`, so bridge-command no-fanout tests remain meaningful. | respected | Remediation text exists in bin/lib/live-status.ts failure JSON/stderr path, not in `.opencode/command/adv-status.md`; no-fanout command test passes. |
| C6 | constraint | Existing roadmap CLI/file-snapshot mode is not changed by this re-entry. | respected | Roadmap bridge/file-snapshot tests unchanged and passing; roadmap guard verifies `unavailable_cli_file_mode`. |
| DONT1 | avoidance | Do not remove agent-needed MCP tools without a recorded matrix justification. | respected | ADV_TOOL_NAMES frozen registry guard still passes; no MCP tools removed. |
| DONT2 | avoidance | Do not implement `adv validate` / `adv doctor` / `adv hygiene` in this change. | respected | No adv validate/doctor/hygiene CLI implementation added; bin/adv dispatch guard allows only status and roadmap. |
| DONT3 | avoidance | Do not add CLI gate/archive/cancellation/task/destructive mutation authority. | respected | No CLI gate/archive/cancel/task/destructive mutation dispatch added; mutation guard tests pass. |
| DONT4 | avoidance | Do not emulate live Temporal status from stale disk projections. | respected | Default status active rows come from Temporal live reader; disk active substitution guard passes; runtime failure emitted no disk table. |
| DONT5 | avoidance | Do not make `/adv-status` ask the agent to call `adv_status`, `adv_change_list`, `adv_change_show`, `adv_gate_status`, or `adv_spec`. | respected | /adv-status command file still contains no adv_status, adv_change_list, adv_change_show, adv_gate_status, or adv_spec fanout instructions; bridge tests pass. |
| DONT6 | avoidance | Do not automatically restart Temporal from `adv status`; provide remediation only. | respected | Status failure remediation instructs user to verify/restart services; code only reports remediation and contains no worker restart operation. Guard tests scan for restart mutation identifiers. |

