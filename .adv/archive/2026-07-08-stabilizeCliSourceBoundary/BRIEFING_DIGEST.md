# Archive Briefing Digest

**Change ID:** stabilizeCliSourceBoundary
**Title:** Stabilize CLI source boundary
**Status:** archived
**Generated:** 2026-07-08T00:54:42.169Z

## Identity Anchors

- CHANGE
- STATUS
- TERMINAL_GATE_SUMMARY

## Archive Digest

**Status:** archived

| Gate | Status |
| --- | --- |
| proposal | done |
| discovery | done |
| design | done |
| planning | done |
| execution | done |
| acceptance | done |
| release | pending |

## Epic Context

Epic: optimizeAdvPerformanceStructure · Stabilize CLI source boundary (order 4)

## Durable Facts

Showing 63 of 63 durable facts.

- **[agenda]** follow_ups: Design gate should decide adapter-barrel-vs-flat-imports (candidate 2) and spec-delta-vs-test-only (candidate 5) — both need orchestrator/user input.
- **[agenda]** follow_ups: Confirm with `pnpm run build` emit that contracts.ts/list-change-workflows.ts type-only ../types imports are stripped (high-confidence from source read; build-emit check would make it structural evidence).
- **[archive_only_evidence]** sources: bin/lib/cli-projection-import-safety.test.ts: Existing Bun guard asserts cli-projection.ts has zero heavy imports + no import statements, and checks changes.ts/types.ts re-export shape. Covers ONLY the projection module; does not guard the Temporal reach-through set.
- **[archive_only_evidence]** sources: bin/lib/live-status.ts imports: CLI reaches into plugin/src/temporal/{list-change-workflows,client,contracts,lifecycle-visibility} — four deep imports with no import-safety guard.
- **[archive_only_evidence]** sources: bin/lib/epic-list.ts imports: CLI reaches into plugin/src/temporal/{client,list-epic-workflows} — unguarded deep imports.
- **[archive_only_evidence]** sources: plugin/src/temporal/contracts.ts: All ../types references are `import type {` or inline `import("../types").X` type positions — fully type-erased at runtime. contracts.ts is runtime-safe for CLI. Only runtime value imports are ./digest (type) and constants.
- **[archive_only_evidence]** sources: plugin/src/temporal/list-change-workflows.ts + lifecycle-visibility.ts: Both import `type { ChangeStatus } from ../types` (erased). Runtime-safe today, but one accidental runtime import from ../types would silently break the CLI with no failing test.
- **[archive_only_evidence]** sources: plugin/src/temporal/client.ts: Imports @temporalio/client (real runtime dep). This is the intentional, required live-behavior dependency; not a deep plugin-internal coupling.
- **[archive_only_evidence]** sources: plugin/src/shared/cli-projection.ts: Zero-import plain-TS contract module. Correct existing pattern: a designed CLI-facing surface, not an accidental deep import.
- **[archive_only_evidence]** sources: plugin/src/cli-gate-order-parity.test.ts: Vitest parity guard: shared GATE_ORDER equals plugin GATE_DEFS-derived order; asserts changes.ts import/re-export shape. Establishes the parity-test precedent to extend.
- **[archive_only_evidence]** sources: Module public-API + enforced import boundary (canonical): Standard reference pattern for CLI/plugin decoupling: expose a designated public surface (index barrel / shared module) and enforce allowed import edges structurally via lint (eslint-plugin-boundaries / no-restricted-imports) or dependency-cruiser, rather than relying on convention.
- **[archive_only_evidence]** architecture_assessment: The CLI-to-plugin boundary has TWO surfaces with asymmetric protection. Surface 1 (plugin/src/shared/cli-projection.ts) is a correctly-designed zero-import contract module with a Bun import-safety guard — this is the by-the-book pattern and should be preserved. Surface 2 is a set of unguarded deep reach-throughs from bin/lib/{live-status,epic-list}.ts into plugin/src/temporal/{client,list-change-workflows,list-epic-workflows,contracts,lifecycle-visibility}.ts. Evidence confirms these Temporal modules are currently RUNTIME-SAFE for the Bun CLI: every ../types reference across contracts.ts, list-change-workflows.ts, lifecycle-visibility.ts, and list-epic-workflows.ts is `import type` or inline type position (erased at emit); the only real runtime import in the reach-through set is @temporalio/client (intentional, required for live status). So the boundary is not broken today — but it is unenforced. There is no structural guard preventing a future refactor from adding a runtime import from ../types (or storage/tools) into any of those five modules, which would silently break the Bun CLI with zero failing tests. This is a P33 structural-correctness gap (correctness by convention, not by machine check) and is the highest-leverage, in-scope target. The lowest-risk, highest-payoff shape is: (a) formalize the CLI-safe Temporal surface as a designated adapter/barrel, and (b) extend the existing import-safety guard to cover the full reach-through allowlist so any new deep import or accidental runtime coupling fails a test.
- **[agenda]** follow_ups: No RESEARCHER_REPORT anchor was missing; all identity anchors present in packet.
- **[agenda]** follow_ups: Consider (future Epic work, not this change) promoting cli-projection from a relative deep import to a formally published plugin entrypoint/exports map for an even cleaner contract.
- **[archive_only_evidence]** sources: bin/lib/types.ts (source): Re-exports GATE_ORDER + projection types from plugin/src/shared/cli-projection only. Single guarded import surface.
- **[archive_only_evidence]** sources: bin/lib/changes.ts (source): Imports GATE_ORDER/GateId from plugin/src/shared/cli-projection; otherwise uses fs/promises + path only. No other plugin coupling.
- **[archive_only_evidence]** sources: bin/lib/live-status.ts (source): Live tier: imports @temporalio/client plus temporal/list-change-workflows, temporal/client, temporal/contracts, temporal/lifecycle-visibility. Required for live Temporal-backed active rows.
- **[archive_only_evidence]** sources: bin/lib/epic-list.ts (source): Live tier: imports temporal/client + temporal/list-epic-workflows. Same narrow live surface as live-status.
- **[archive_only_evidence]** sources: plugin/src/shared/cli-projection.ts (source): Plain-TS module, zero runtime imports by design; documented as Bun-safe CLI contract. Explicitly the intended projection boundary.
- **[archive_only_evidence]** sources: cli-projection-import-safety.test.ts (guard): Bun test: FORBIDDEN_IMPORT_PATTERNS denylist (@temporalio, temporal/, zod, ../storage/, ../tools/, ../tool-registry, ../index, node:) enforced against the shared module source + asserts zero import statements; also asserts changes.ts/types.ts re-export from shared module.
- **[archive_only_evidence]** sources: cli-gate-order-parity.test.ts (guard): Vitest: shared GATE_ORDER equals plugin types/gates GATE_ORDER, and bin/lib/changes.ts imports+re-exports GATE_ORDER from shared module. Cross-boundary parity is already structural.
- **[archive_only_evidence]** sources: temporal/list-change-workflows.ts + contracts.ts + client.ts (source): Live-tier transitive graph is narrow: contracts.ts imports only ../types (type-only) + ./digest; client.ts imports @temporalio/client + contracts; list-* import contracts + lifecycle-visibility. No ../storage, ../tools, ../tool-registry, or plugin-init reachable from the CLI live imports.
- **[archive_only_evidence]** sources: adv_spec search (governance): Zero spec results. No existing capability spec governs CLI source-boundary imports; discovery agenda item confirmed. Enforcement today is test-only.
- **[archive_only_evidence]** architecture_assessment: Two distinct import tiers exist and must be governed differently. Tier A (projection): the only CLI->plugin coupling for the pure-computation CLI path is plugin/src/shared/cli-projection.ts, a zero-runtime-import plain-TS module already fenced by a denylist Bun test and a parity Vitest. This is the canonical 'published internal contract' pattern (single narrow module + import-safety guard) and is by-the-book. Tier B (live): bin/lib/live-status.ts and bin/lib/epic-list.ts deep-import temporal/{client,contracts,list-*,lifecycle-visibility} plus the real @temporalio/client dependency. These deep imports are load-bearing: the proposal's 'Must Not' bans regressing live active rows to stale disk projection, so removing the Temporal coupling would violate a stated constraint. The transitive graph reached from those CLI live imports is narrow (contracts is type-only over ../types + ./digest; client adds @temporalio/client) and does NOT reach ../storage, ../tools, ../tool-registry, or plugin-init. The correct strictness is therefore a two-tier allowlist, not blanket prohibition: keep Tier A at its existing zero-import strictness, and make Tier B's currently-implicit allowlist explicit and structurally enforced (an allowlisted set of temporal/* CLI-contract modules) so future refactors that widen CLI coupling into storage/tools/plugin-init fail a test. Deep imports are not inherently unsafe here; unbounded/undocumented deep imports are. The boring, proven move is codify-the-allowlist, not re-architect the Temporal path.
- **[agenda]** follow_ups: Candidate tracked follow-up title: 'Assess adapter + Temporal hot-path v2 decomposition seams' — promote the two remaining optimizeAdvPerformanceStructure shells ('Optimize adapter surfaces', 'Deep-scan Temporal workflow hot path') into bounded design changes with behavior-preserving + state-machine test strategy.
- **[agenda]** follow_ups: If the user wants a harder cost signal before funding the seam program, a quick line-count/complexity pass on change.ts / task.ts / tool-registry.ts would quantify decomposition effort (change.ts already ~56KB by outline byte offsets).
- **[agenda]** follow_ups: TASK_SCOPE anchors present and consumed; no packet-defect. Assessment intentionally did not mutate any ADV state beyond this report.
- **[archive_only_evidence]** sources: Epic optimizeAdvPerformanceStructure (narrative + 7 entries): Active repo-scoped structural-optimization initiative. 4/7 entries already archived (slop-scan fail-fast, command-boundary, archived-listing timeout, status probe TTL). Remaining shells target the exact 'v2' surfaces: 'Optimize adapter surfaces' (change/task/tool-registry coupling) and 'Deep-scan Temporal workflow hot path'. Constraint: do not weaken spec-law, Temporal replay safety, or typed tool boundaries. This IS an incremental modernization program already in motion.
- **[archive_only_evidence]** sources: plugin/src tools directory census: ~200+ source files, ~1:1 test co-location. tools/ already decomposed into change/, apply-helpers/, archive-helpers/, session/, worktree/ subdirs plus _adapters.ts. change.ts alone ~56KB. Evidence of active in-place seam extraction, not a monolith awaiting greenfield replacement.
- **[archive_only_evidence]** sources: Capability specs (adv_spec list): 19 capability specs, ~370 total requirements (advance-workflow alone: 97; advance-meta: 49; subagent-reports: 21; advance-delivery: 23). Specs are branch-local laws enforced at archive gate. A rewrite must re-satisfy every rq-* requirement or explicitly retire it — the spec surface is the true rewrite cost, not the code.
- **[archive_only_evidence]** sources: Temporal replay-safety invariant (AGENTS.md + boundary tests): Signal/query-only workflow surface; reintroducing defineUpdate poisons replay. workflow-bundle-boundary.test.ts is a transitive import walker enforcing workflow-safe module graph. A rewrite re-incurs every determinism/replay landmine already paid down here.
- **[archive_only_evidence]** sources: Discovery scout report on current change (P33 boundary gap): Concrete example of ADV's actual defect class: correctness-by-convention gaps (unguarded CLI->plugin deep imports) closed by a designated surface + extended deterministic guard. Payoff came from a targeted structural test, not architectural replacement.
- **[archive_only_evidence]** sources: Martin Fowler — Strangler Fig Application (2024 rewrite): Canonical guidance: cut-over rewrites of business-embedded systems are 'overflowing with risk'; old bugs and features must be re-added; big-cutover pressure degrades quality. Preferred approach is gradual seam-based replacement giving steady value and earlier ROI. Prerequisite: crystal-clear desired outcomes — muddled objectives are the top failure mode.
- **[archive_only_evidence]** sources: Microsoft Azure Architecture Center — Strangler Fig Pattern: Reinforces incremental façade-based replacement to reduce migration risk while the system stays functional; recommended precisely when replacing large systems / key components / complex features that introduce risk.
- **[archive_only_evidence]** architecture_assessment: Advance is a mature, spec-governed, Temporal-backed OpenCode plugin (~200+ source files, ~370 spec requirements across 19 capability specs, dense ~1:1 test co-location, replay-safety and layer-boundary invariants enforced by tests). The 'evolved over a year through many updates' history is visible as real but bounded structural debt: a few large coupled adapter modules (change.ts ~56KB, tool-registry, task.ts), Temporal hot-path/replay sensitivity, and correctness-by-convention gaps (e.g. unguarded CLI deep imports). Crucially, the codebase is NOT an undifferentiated monolith — tools/ is already decomposed into subdirectories (change/, apply-helpers/, archive-helpers/, session/, worktree/) with _adapters.ts seams, and the optimizeAdvPerformanceStructure Epic is already running the exact modernization program a 'v2' would demand (4/7 entries archived; remaining shells are 'Optimize adapter surfaces' and 'Deep-scan Temporal workflow hot path'). The dominant asset and the dominant rewrite cost are the same thing: the spec/law surface plus the paid-down Temporal determinism and typed-tool-boundary invariants. A full v2 rewrite would re-incur all ~370 requirement obligations, every replay/determinism landmine, and every gate/state-machine edge case — with no new user value during the rebuild — which is precisely the 'overflowing with risk' cut-over Fowler warns against and the strangler-fig pattern exists to avoid. The high-leverage seams the user is sensing (large adapters, Temporal hot path) are real and worth investment, but they are extract-and-decompose targets reachable in-place, not justification for greenfield replacement.
- **[agenda]** follow_ups: Scout leverage candidates (≤5) delivered in recommendation; no source mutations performed.
- **[agenda]** follow_ups: Confirm ../types has no accidental heavy runtime pull the CLI can't tolerate under Bun — draft's 'source-level importability' test covers this; keep it.
- **[archive_only_evidence]** sources: bin/lib/live-status.ts (current deep imports): 5 scattered plugin/src/temporal/* imports: list-change-workflows, client (createTemporalClientBundle,buildChangeWorkflowId), contracts (CHANGE_WORKFLOW_QUERY_NAMES), lifecycle-visibility (escapeVisibilityValue). Exactly the surface the Tier B boundary would consolidate.
- **[archive_only_evidence]** sources: bin/lib/epic-list.ts: Imports client + list-epic-workflows from plugin/src/temporal, AND listChanges from ./changes (disk enumeration in loadCurrentChildByEpicId). Disk read is same-tier CLI, not a plugin-src crossing.
- **[archive_only_evidence]** sources: bin/lib/cli-projection-import-safety.test.ts: Existing Tier A test = zero-import source scan + FORBIDDEN_IMPORT_PATTERNS regex denylist (@temporalio, storage, tools, tool-registry, index, node:, zod). Reusable pattern for Tier B, but Tier B must NOT reuse the zero-@temporalio rule.
- **[archive_only_evidence]** sources: plugin/src/temporal/client.ts + contracts.ts + lifecycle-visibility.ts transitive imports: Boundary re-export targets transitively pull ../types and ../types/artifacts (contracts uses import type but ../types has runtime exports e.g. createDefaultGates/EpicStatusSchema). client.ts pulls @temporalio/client. Confirms Tier B is legitimately non-zero-runtime-import.
- **[archive_only_evidence]** sources: plugin/src/temporal/workflow-bundle-boundary.test.ts (cited in AGENTS.md): Repo already ships a transitive import-walker denylist test for the workflow bundle (bans storage/tools/tool-registry/plugin-init/node:*). Direct precedent/pattern for a Tier B CLI boundary walker; reuse the walker approach rather than inventing new tooling.
- **[archive_only_evidence]** architecture_assessment: Design is boring and correct for a source-boundary hardening change. Two-tier split (Tier A zero-import projection preserved as-is; Tier B named CLI Temporal boundary module) matches the existing repo idiom: the codebase ALREADY enforces layer boundaries via a transitive import-walker denylist (workflow-bundle-boundary.test.ts) and a zero-import source scan (cli-projection-import-safety.test.ts). The named barrel over a scattered-path allowlist is the right LBP call — it makes the CLI runtime contract legible and is cheaper to maintain. Structural (test-enforced, denylist) over heuristic/prose satisfies P33 and the DONT1 avoidance. The 5 current deep imports map 1:1 onto the proposed boundary re-exports; behavior preservation is low-risk because no call sites change semantics, only import source paths. The single real design subtlety the draft already names but under-specifies: Tier B tests must assert an import-STRING structural denylist (no storage/tools/tool-registry/plugin-init/non-allowlisted plugin surfaces), NOT a transitive-zero-import invariant — because the Temporal client legitimately imports @temporalio/client and ../types. No blockers.
- **[agenda]** follow_ups: Planning: Tier B enforcement test should walk the barrel's TRANSITIVE import graph, not just direct barrel imports, to catch drift where a re-exported temporal target later acquires a forbidden import.
- **[agenda]** follow_ups: Planning: redirect only bin/lib/live-status.ts and bin/lib/epic-list.ts to the new barrel; leave bin/lib/types.ts and bin/lib/changes.ts on Tier A cli-projection to avoid touching AC6-protected surfaces.
- **[archive_only_evidence]** sources: Tier A source — plugin/src/shared/cli-projection.ts: Confirmed pure: zero import statements, header states zero-runtime-import contract. Holds GATE_ORDER + CLI projection types only.
- **[archive_only_evidence]** sources: Existing crossings — bin/lib/live-status.ts: Imports listChangeWorkflowIds, createTemporalClientBundle, buildChangeWorkflowId (temporal/client, temporal/list-change-workflows), CHANGE_WORKFLOW_QUERY_NAMES (temporal/contracts), escapeVisibilityValue (temporal/lifecycle-visibility). These are the scattered crossings the boundary consolidates.
- **[archive_only_evidence]** sources: Existing crossings — bin/lib/epic-list.ts: Imports createTemporalClientBundle (temporal/client), listEpicWorkflowIds + ListEpicClient (temporal/list-epic-workflows).
- **[archive_only_evidence]** sources: Re-export target cleanliness — temporal/client.ts: Imports only @temporalio/client and ./contracts. No storage/tools/tool-registry/plugin-init/index. Provides createTemporalClientBundle, buildChangeWorkflowId.
- **[archive_only_evidence]** sources: Re-export target cleanliness — temporal/contracts.ts: Imports only ../types (workflow-safe) and ./digest. Exports CHANGE_WORKFLOW_QUERY_NAMES (line 49). Clean.
- **[archive_only_evidence]** sources: Re-export target cleanliness — list-change-workflows / list-epic-workflows / lifecycle-visibility: list-change-workflows imports ../types + ./lifecycle-visibility + ./contracts; list-epic-workflows imports ./contracts; lifecycle-visibility exports escapeVisibilityValue. None import storage/tools/tool-registry/plugin-init/index. The design's Tier B denylist matches the boundary these modules already respect.
- **[archive_only_evidence]** sources: Denylist evidence — which temporal modules DO pull heavy deps: Only activities.ts (storage/json), workflow-start.ts (storage/store-temporal), health-probe.ts (plugin-init) import forbidden surfaces. NONE are in the design's re-export set — confirms Tier B stays clean transitively for the chosen exports.
- **[archive_only_evidence]** sources: Existing test pattern — cli-projection-import-safety.test.ts: Bun source-scan test with FORBIDDEN_IMPORT_PATTERNS regex + zero-import assertion. Proven deterministic pattern the design extends for Tier B root-crossing enforcement.
- **[archive_only_evidence]** sources: Spec law — advance-meta rq-statusCliBridge01 / rq-statusCliWorkerFree01: Require live Temporal-backed active rows, fail-closed on Temporal/Visibility failure, no stale-disk active fallback. AC5 warrants these. A pure re-export barrel preserves runtime behavior, so no regression risk.
- **[archive_only_evidence]** sources: Contract — AC7 authorizes design shape choice: AC7: 'Design decides whether to keep explicit allowlist paths or consolidate Temporal imports behind a named CLI-safe adapter.' Design chooses named boundary module — directly satisfies AC7 and DONT2 (does not blanket-ban Temporal CLI imports).
- **[archive_only_evidence]** architecture_assessment: The design is correct, minimal, and spec-law compliant. It solves SC1-SC3/AC1-AC7 by making an already-existing informal boundary explicit and machine-checkable (P33 structural-over-heuristic), backed by deterministic Bun source-scan tests that mirror the proven cli-projection-import-safety pattern. Source evidence confirms the two-tier split is real: Tier A (cli-projection.ts) is verifiably zero-import; the five Tier B re-export targets (client, contracts, list-change-workflows, list-epic-workflows, lifecycle-visibility) each transitively import only ../types, sibling temporal/* modules, and @temporalio/client — none reach storage/tools/tool-registry/plugin-init/index. The only temporal modules that pull heavy deps (activities.ts, workflow-start.ts, health-probe.ts) are explicitly outside the re-export set, so the Tier B denylist is satisfiable as written. Because the boundary module is a pure re-export barrel with no behavior change, AC5's fail-closed live-Temporal warrants (rq-statusCliBridge01, rq-statusCliWorkerFree01) and AC6 (cli-projection safeguards) are structurally preserved. AC7 is directly satisfied — the design consciously picks the named adapter over a bare allowlist, honoring DONT2. Scope is tight: no v2 rewrite (C4/OOS1), no OCA work (C3/OOS2), no broad adapter refactor (DONT5/OOS4). One minor caution: the named-barrel approach must guard against future drift where an editor adds a heavy import to the barrel or one of its targets; the design mitigates this with a Tier B denylist test on the barrel's own imports plus the target modules, which is the correct structural guard. Two small implementation-precision notes (non-blocking): (1) the Tier B denylist test should assert the barrel module's transitive import graph, not just the barrel file's direct imports, to prevent a target module later acquiring a storage import from silently leaking through; (2) the design should keep bin/lib/types.ts and bin/lib/changes.ts pointed at Tier A cli-projection (unchanged) and only redirect the two temporal-crossing files, to avoid touching AC6-protected surfaces.
- **[wisdom_candidate]** wisdom_candidates: [gotcha] Structural import-boundary tests should parse dynamic import("...") literals as well as static import/export declarations; otherwise forbidden paths can bypass boundary policy while still using module imports.
- **[archive_only_evidence]** changes_made: bin/lib/cli-source-boundary.test.ts: Hardened import-specifier scanner to include literal dynamic imports and added a regression test so dynamic imports cannot bypass the CLI source-boundary guard.
- **[archive_only_evidence]** verification: tests_run=bun test bin/lib/cli-source-boundary.test.ts, bun test bin/, pnpm --dir plugin exec prettier --check ../bin/lib/cli-source-boundary.test.ts, git diff --check results=pass — Path preflight OK for all expected files. Reviewed diff against origin/trunk for specs, docs, CLI boundary test, live-status, epic-list, and temporal-boundary. Boundary test passed after reviewer hardening: 3 pass, 0 fail. Full Bun CLI suite passed: 197 pass, 0 fail. Prettier check for edited test passed. git diff --check passed.
- **[unresolved_action]** required_main_agent_actions: Proceed with release/archive path; no harden blockers or issues remain.
- **[unresolved_action]** required_main_agent_actions: During release packaging or local deploy verification, confirm the deployed adv CLI still resolves the approved plugin/src/cli/temporal-boundary import from the deployed payload.
- **[archive_only_evidence]** verification: tests_run=git status --short && git diff --stat && git diff -- .adv/specs/advance-meta/spec.json docs/specs/advance-meta.md bin/lib/cli-source-boundary.test.ts bin/lib/epic-list.ts bin/lib/live-status.ts plugin/src/cli/temporal-boundary.ts, git diff --stat origin/trunk...HEAD && git diff --name-only origin/trunk...HEAD, lgrep_search_text ../../plugin/src/, lgrep_search_text plugin/src/storage, bun test bin/lib/cli-source-boundary.test.ts, pnpm run schemas:check, pnpm run format:check, bun test bin/, bin/oc-test targeted -- src/cli-bridge-contract.test.ts src/cli-gate-order-parity.test.ts, git status --short results=pass — Path preflight OK for all scoped files. Worktree clean before and after harden pass. Branch diff vs origin/trunk limited to 6 in-scope files: .adv/specs/advance-meta/spec.json, docs/specs/advance-meta.md, bin/lib/cli-source-boundary.test.ts, bin/lib/epic-list.ts, bin/lib/live-status.ts, plugin/src/cli/temporal-boundary.ts. Boundary import scan showed only approved root CLI crossings: shared cli-projection and cli/temporal-boundary. No direct bin imports into storage/tools/tool-registry/plugin-init/plugin index found in scoped boundary. bun test bin/lib/cli-source-boundary.test.ts: 3 pass, 0 fail. pnpm run schemas:check: passed. pnpm run format:check: passed. bun test bin/: 197 pass, 0 fail. bin/oc-test targeted -- src/cli-bridge-contract.test.ts src/cli-gate-order-parity.test.ts: 2 files/21 tests passed.
- **[epic_terminal_note]** epic.membership: optimizeAdvPerformanceStructure · Stabilize CLI source boundary (order 4)

## Contract / AC Coverage

| ID | Kind | Status |
| --- | --- | --- |
| SC1 | success_criterion | pass |
| SC2 | success_criterion | pass |
| SC3 | success_criterion | pass |
| AC1 | acceptance_criterion | pass |
| AC2 | acceptance_criterion | pass |
| AC3 | acceptance_criterion | pass |
| AC4 | acceptance_criterion | pass |
| AC5 | acceptance_criterion | pass |
| AC6 | acceptance_criterion | pass |
| AC7 | acceptance_criterion | pass |
| C1 | constraint | respected |
| C2 | constraint | respected |
| C3 | constraint | respected |
| C4 | constraint | respected |
| C5 | constraint | respected |
| C6 | constraint | respected |
| DONT1 | avoidance | respected |
| DONT2 | avoidance | respected |
| DONT3 | avoidance | respected |
| DONT4 | avoidance | respected |
| DONT5 | avoidance | respected |
| OOS1 | out_of_scope | not_applicable |
| OOS2 | out_of_scope | not_applicable |
| OOS3 | out_of_scope | not_applicable |
| OOS4 | out_of_scope | not_applicable |
| OOS5 | out_of_scope | not_applicable |
| OOS6 | out_of_scope | not_applicable |

## Unresolved Actions

- Proceed with release/archive path; no harden blockers or issues remain.
- During release packaging or local deploy verification, confirm the deployed adv CLI still resolves the approved plugin/src/cli/temporal-boundary import from the deployed payload.
