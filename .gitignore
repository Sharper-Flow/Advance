# Dependencies
node_modules/
plugin/node_modules/
plugin/node_modules
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Per-user OpenCode plugin dev artifacts (not tracked so users get their own)
.opencode/package.json
.opencode/package-lock.json
.opencode/bun.lock
.opencode/node_modules/

# SQLite database (derived, not committed)
.adv/db/
*.db
*.db-wal
*.db-shm

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*

# Test coverage
coverage/
.nyc_output/

# Environment
.env
.env.local
.env.*.local

# Temporary files
tmp/
temp/
*.tmp

# Provider eval output (regenerated per run)
scripts/provider-eval-results/

# ADV working state (local project data - in .adv directory)
.adv/changes/
.adv/archive/

# Validation-only generated Temporal history fixture (committed only once populated intentionally)
plugin/src/temporal/__tests__/replay/histories/smoke-captured.json
