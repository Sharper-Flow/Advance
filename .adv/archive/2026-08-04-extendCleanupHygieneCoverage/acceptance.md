# Acceptance

Reviewed at: 2026-08-04T02:21:49.444Z

## Contract Review Matrix

| ID | Kind | Requirement | Status | Evidence |
|---|---|---|---|---|
| AC1 | acceptance_criterion | `/adv-cleanup` in default dry-run mode renders a report with a distinct section for each of the four hygiene surfaces, routing discovery through `adv_change_list`/`adv_change_show`, `adv_worktree_triage`, `adv_worktree_cleanup mode archived_branches dryRun true`, and `adv_status view hygiene`; empty surfaces render an explicit empty state rather than being omitted. | pass | Command Phase 1 issues four numbered discovery calls (adv_change_list, adv_worktree_triage, adv_worktree_cleanup mode archived_branches dryRun true, adv_status view hygiene). Four-surface table in command and skill. Explicit empty-state snippets for all four surfaces, incl. the surface-1 snippet added during review remediation. Guarded by AC1 test block in adv-cleanup-contract-assets.test.ts; run tr_mse0z1mf_6590a14d exit 0. |
| AC2 | acceptance_criterion | Every candidate bucket in the report carries an explicit reversible or irreversible marker, irreversible buckets state their recovery path, and the branch-deletion bucket states the reflog recovery time bound. | pass | Reversibility/recovery table in both files marks each bucket reversible or irreversible with recovery path; branch bucket states reflog bound ~30-90d citing gc.reflogExpireUnreachable and gc.reflogExpire. Report template carries inline markers. AC2 test block green; word-boundary regex confirmed not to match inside 'irreversible'. |
| AC3 | acceptance_criterion | An irreversible bucket rejects the reply `approve all` and re-prompts; it accepts `delete all N` only when N equals the exact listed candidate count, plus `delete N,M`, `skip`, and `stop`/`abort`; a count mismatch re-prompts and no reply path uses LLM fallback. | pass | Phase 4b and skill Irreversible buckets carry count-matched delete-all regex with adjacent count-equality prose, subset, skip, and halt replies; explicit approve-all rejection; count-mismatch re-prompt; no LLM fallback. AC3 test block uses per-file heading-anchored section extraction after CORRECT-1 remediation. |
| AC4 | acceptance_criterion | Irreversible bucket prompts list each candidate by number with its exact worktree path or branch name, never a count-only summary. | pass | Both files require every candidate listed numbered with exact worktree path or branch name, never a count-only summary or abbreviated path. AC4 test block asserts path, branch name, and never-a-count phrasing within the irreversible section. |
| AC5 | acceptance_criterion | Approved deletions are executed by `adv_worktree_delete` or `adv_worktree_cleanup`, `adv_worktree_triage` classification is never used as deletion authority, and tool safety refusals are surfaced verbatim in result lines rather than retried or bypassed. | pass | Deletion delegates to adv_worktree_delete / adv_worktree_cleanup; direct git worktree remove and git branch -d prohibited; triage output declared advisory and never deletion authority; refusals surfaced verbatim. Strengthened at review by the OrphanClass mapping (SEC-1) so eligibility references categories triage.ts:54-62 actually emits, with unrecognized classes failing closed. |
| AC6 | acceptance_criterion | With no execute flag, no change is closed, no worktree is deleted, and no branch is deleted, and the footer states how to re-run with execution enabled. | pass | Dry-run stated as default in command intro, flag table, and skill Defaults; footer states how to re-run with execution enabled. No mutation path reachable without --execute. AC6 test block green. |
| AC7 | acceptance_criterion | Active-change bucket precedence, the child-lineage guard, hot-recency-band exclusion, the reversible-bucket reply parser, per-bucket atomicity, and continue-after-bucket-failure all behave exactly as before. | pass | Bucket Precedence, Normalization, Inspection Sequence and Child-Lineage Guard verified intact in SKILL.md after being dropped and restored during task 4. Hot-band exclusion, reversible parser tokens, per-bucket atomicity and continue-after-failure preserved. Contract scanner traced all seven facets with file:line evidence; full unit suite green confirms no regression. |
| AC8 | acceptance_criterion | `rq-cleanupHygieneScope01` exists in `.adv/specs/advance-workflow/spec.json` with the five scenarios from design.md and is cited from the command or skill so `spec-citation-invariant.test.ts` finds an anchor. | pass | rq-cleanupHygieneScope01 present exactly once in .adv/specs/advance-workflow/spec.json (136 requirements) with 5 scenarios and priority must, verified by jq assertion in run tr_msdxxg5w_c8658ac0. Cited from command, skill and test file; spec-citation-invariant.test.ts green. |
| AC9 | acceptance_criterion | `plugin/src/adv-cleanup-contract-assets.test.ts` asserts the new contract, with the two report-only-invariant tests rewritten rather than left to pass falsely on the surviving phrase "drift report". | pass | The two report-only-invariant tests were replaced, not appeased. Non-mutating discrimination proof: the retired-invariant guard evaluates false against base commit d14963c7 content for both files and true against current content, proving it distinguishes old from new contract rather than passing on the surviving phrase 'drift report'. |
| AC10 | acceptance_criterion | `pnpm run check` passes and the affected asset tests pass. | pass | pnpm run check exit 0 incl. prettier (run tr_mse119qe_919cdfe9). Full unit project run tr_mse0z1mf_6590a14d exit 0: 541 files, 8346 passed, 0 failures. Guard suite grew 56 to 66 tests. Full-suite run also caught and led to fixing a manifest-doc-drift regression the earlier narrowed run missed. |
| C1 | constraint | Deletion delegates to `adv_worktree_delete` or `adv_worktree_cleanup`; never `git worktree remove` or `git branch -d` directly, per `rq-terminalCleanupSafety01`. | respected | Both files delegate deletion to adv_worktree_delete / adv_worktree_cleanup and explicitly prohibit direct git worktree remove and git branch -d, satisfying rq-terminalCleanupSafety01 sole-deletion-authority. |
| C2 | constraint | No background sweeps, daemons, or session-start auto-cleanup; operator-explicit invocation only, per `rq-archiveBranchCleanup01` and P37. | respected | No background sweeps, daemons, or session-start auto-cleanup introduced. Invocation remains operator-explicit via --execute plus per-bucket approval, consistent with rq-archiveBranchCleanup01 and P37. |
| C3 | constraint | Whitelist and regex reply parsing only, with no LLM fallback and no fuzzy matching on any bucket, per `rq-inlineApproval01.3`. | respected | Both reversible and irreversible parsers are regex/whitelist only with explicit no-LLM-fallback statements; no fuzzy matching added. Count mismatch re-prompts rather than inferring intent. Asserted in AC3 test block. |
| C4 | constraint | The `skill("adv-cleanup")` reference and its inline fallback phrase must survive within the bounded window checked by `skill-loading-policy-assets.test.ts` lines 231-245. | respected | skill("adv-cleanup") reference and its fallback phrase retained within the bounded window; no new skill literal added. skill-loading-policy-assets.test.ts green including SKILL_REF_INVENTORY set equality. |
| C5 | constraint | Spec citations `rq-inlineApproval01.3`, `rq-inlineApproval01.4`, and `rq-autonomy01` must retain an anchor in the command file or be re-cited elsewhere. | respected | rq-inlineApproval01.3, rq-inlineApproval01.4 and rq-autonomy01 citations retained in the command; spec-citation-invariant.test.ts green in the full unit run. |
| C6 | constraint | The skill stays read-only guidance while the command owns all tool calls and mutation. | respected | Skill retains 'Read-only guidance only; command owns ADV mutations' in Constraints; all tool invocation instructions remain in the command. Guarded by the C6 test block. |
| C7 | constraint | No `## Next` or `## Next stage` heading and no gate-handoff spine in this command, per `commands-spine-assets.test.ts`. | respected | No ## Next or ## Next stage heading and no Problem/Chosen direction/Delivered spine added; commands-spine-assets.test.ts green plus explicit negative assertions in the C7 test block. |
| DONT1 | avoidance | Do not modify `adv_worktree_delete`, `adv_worktree_cleanup`, `adv_worktree_triage`, or `adv_status` tool behavior; this is consumer-side orchestration only. | respected | No file under plugin/src/tools/ modified. Change touches only command markdown, skill markdown, spec.json, the guard test, one manifest.ts description line, and two doc table rows. Tool behavior for adv_worktree_delete/cleanup/triage and adv_status is unchanged; both blockers were fixed in consumer-side prose rather than by changing tool defaults. |
| DONT2 | avoidance | Do not split `/adv-cleanup` into separate discover and destroy commands, since the fused shape is validated as the established norm. | respected | /adv-cleanup remains a single fused command; no separate discover or destroy command created. Validated during design against docker, git, kubectl, npm, cargo, nix and terraform precedent. |
| DONT3 | avoidance | Do not add a second execute flag, because the user selected typed confirmation at the prompt over flag escalation. | respected | Single --execute flag retained; no --execute-destructive or equivalent escalation flag added. Explicit negative assertion in the DONT3 test block. |
| DONT4 | avoidance | Do not fold archived or closed state-leak remediation into an executable bucket, since it routes to `adv_archive_purge` which owns its own operator approval contract. | respected | State leaks remain report-only and route to adv_archive_purge, which owns its own operator approval contract; surface 4 marked non-executable in the four-surface table. Asserted in the DONT4 test block. |
| DONT5 | avoidance | Do not weaken the reversible-bucket parser to accommodate the new irreversible one; the two prompts stay distinct. | respected | Reversible parser table preserved verbatim (approve all, reject all, keep N,M, cancel N,M, stop/abort); the irreversible parser is a separate table with distinct tokens. Guarded by the AC7/DONT5 test block. |

