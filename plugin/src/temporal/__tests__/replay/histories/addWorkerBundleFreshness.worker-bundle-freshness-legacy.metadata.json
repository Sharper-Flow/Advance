{
  "name": "addWorkerBundleFreshness worker-bundle-freshness-legacy",
  "workflowType": "changeWorkflow",
  "workflowId": "adv/change/replay-fixture-project/replayFixtureWorkerBundleFreshnessLegacy",
  "source": "Controlled Temporal test-workflow history generated by `pnpm exec tsx scripts/gen-replay-fixture.ts --branch worker-bundle-freshness-legacy` against a local dev server on a non-default port, which runs a controlled legacy-branch variant of changeWorkflow (current code with the WORKER_BUNDLE_FRESHNESS_PROVENANCE_PATCH `wf.patched` call replaced by `false` so the marker is not recorded). Exported with `temporal workflow show --address 127.0.0.1:17234 --namespace default --output json --no-json-shorthand-payloads --workflow-id adv/change/replay-fixture-project/replayFixtureWorkerBundleFreshnessLegacy`, then sanitized (identity only). No event history hand-authored.",
  "covers": [
    "WORKER_BUNDLE_FRESHNESS_PROVENANCE_PATCH (worker-bundle-freshness-v1) (legacy): a pre-patch history completes the release gate without recording the WORKER_BUNDLE_FRESHNESS_PROVENANCE_PATCH marker and without worker_bundle_impact or workerBundleProvenance state.",
    "Branch-specific coverage: current patched code replays this history deterministically through the legacy skip-provenance path because the WORKER_BUNDLE_FRESHNESS_PROVENANCE_PATCH marker is absent; the release gate completes as it did before the patch."
  ],
  "expected": "Worker.runReplayHistory resolves against the current workflow bundle.",
  "sanitized": true,
  "eventCount": 8,
  "incidentEventId": "8",
  "incidentEventType": "EVENT_TYPE_WORKFLOW_TASK_COMPLETED"
}
