# Archive Briefing Digest

**Change ID:** fixTemporalTimeoutsWorker
**Title:** Fix temporal timeouts and worker reaping
**Status:** archived
**Generated:** 2026-07-14T03:01:22.390Z

## Identity Anchors

- CHANGE
- STATUS
- TERMINAL_GATE_SUMMARY
- Origin: roadmap #216

## Archive Digest

**Status:** archived

| Gate | Status |
| --- | --- |
| proposal | done |
| discovery | done |
| design | done |
| planning | done |
| execution | done |
| acceptance | done |
| release | pending |

## Epic Context

No Epic membership

## Durable Facts

Showing 100 of 103 durable facts (3 omitted).

- **[archive_only_evidence]** decisions: Capture process.ppid ONCE at watchdog start and probe that fixed pid with signal 0 — After the parent dies the child is reparented and process.ppid then points at init/a systemd subreaper (alive) — re-reading it each tick would never detect the orphan. Probing the captured pid with process.kill(ppid,0) is robust to subreapers, unlike a ppid===1 check which misses non-init adoptive parents.
- **[archive_only_evidence]** decisions: Watchdog poll interval = 3000ms (DEFAULT_PARENT_WATCHDOG_INTERVAL_MS) — Within the task's 2-5s coarse band; signal-0 existence check is cheap and the unref'd timer adds negligible overhead while staying responsive enough to reap orphans quickly.
- **[archive_only_evidence]** decisions: Arm the watchdog at the top of runTemporalWorkerFromEnv (single insertion point), not inside each worker.run() — runTemporalWorkerFromEnv is THE child entry function (invoked when argv[1]===worker.ts) and branches to both single-queue and multi-queue modes; one arm covers both before the blocking poll loop.
- **[archive_only_evidence]** decisions: Self-exit with code 1 (distinct from clean-shutdown exit 0) and treat non-ESRCH kill errors (EPERM) as parent-alive — A distinct exit code makes crash-orphan self-exits distinguishable in logs; EPERM means the process exists but isn't signalable by us, so treating it as alive prevents spurious self-exit on a permission quirk.
- **[archive_only_evidence]** decisions: Factored watchdog into startParentLivenessWatchdog with injectable isAlive + exit + ppid + intervalMs — Enables deterministic unit tests (mock process.kill to throw ESRCH, inject a fake exit) without invoking real process.exit in the test runner, per the task's testability requirement.
- **[archive_only_evidence]** verification: pnpm exec vitest run src/temporal/worker.test.ts (1) — RED (phase:red): exactly the 5 new startParentLivenessWatchdog tests failed with 'startParentLivenessWatchdog is not a function'; other 15 passed. runId tr_mrjvzhjx_b82d5d96
- **[archive_only_evidence]** verification: pnpm exec vitest run src/temporal/worker.test.ts (0) — GREEN (phase:green): 20/20 pass including all 5 watchdog tests (ESRCH exit, alive no-exit, single-exit, EPERM-treated-alive, default process.exit/process.ppid). runId tr_mrjw1v7a_1e31a0a0
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/temporal/worker.test.ts src/temporal/runtime-manager.test.ts src/temporal/worker-multi.test.ts src/temporal/out-of-process-worker.test.ts (0) — 62/62 tests pass across 4 files (touched + import-dependent: worker, runtime-manager, worker-multi which mocks buildTemporalWorkerProcessSpec, out-of-process-worker).
- **[archive_only_evidence]** verification: pnpm run check (0) — schemas:check, typecheck (tsc --noEmit), test-isolation, lockfile-policy, eslint, prettier format:check all green ('All matched files use Prettier code style!'). Prettier --write applied to worker.test.ts and re-verified.
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm exec vitest run src/temporal/worker.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm exec vitest run src/temporal/worker.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/temporal/worker.test.ts src/temporal/runtime-manager.test.ts src/temporal/worker-multi.test.ts src/temporal/out-of-process-worker.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm run check
- **[archive_only_evidence]** decisions: Made classifier purely advisory (no probes, no IO) — gate_complete has no bundle-first durable write like archive — its sole durable mutation is fireSignalAndRefresh(gateCompletedSignal). Only adv_gate_status can authoritatively answer 'did the signal land?'. A probe in the classifier would either mask the original timeout (if the worker is hung) or provide false confidence. Probe discipline mirrors archive-timeout.ts:17-19.
- **[archive_only_evidence]** decisions: Chose 30_000ms timeoutMs — Per task spec. Lighter than archive's 420s (git finalization is much heavier than a single Temporal signal) but comfortably above the 10s default that was firing under worker contention.
- **[archive_only_evidence]** decisions: Colocated classifier in plugin/src/tools/gate-timeout.ts (not tools/change/) — gate.ts lives at plugin/src/tools/gate.ts, so gate-timeout.ts follows the same locality pattern that archive-timeout.ts follows (next to its owning tool).
- **[archive_only_evidence]** decisions: Returned undefined for missing/invalid changeId — If we can't even identify the change, the advisory 'verify via adv_gate_status' would be meaningless. Falling back to the generic timeout preserves the standard malformed-args hint.
- **[archive_only_evidence]** verification: pnpm vitest run src/tool-registry.test.ts src/tools/gate-timeout.test.ts (phase=red) (1) — RED recorded: 2 failures — tool-registry gate_complete block lacked timeoutMs/onToolTimeout; gate-timeout module not found. runId=tr_mrjvl85j_6c34f9f9.
- **[archive_only_evidence]** verification: pnpm vitest run src/tool-registry.test.ts src/tools/gate-timeout.test.ts (phase=green) (0) — GREEN: 35/35 pass (29 tool-registry + 6 gate-timeout). runId=tr_mrjvmlk9_7d87f8ff.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tool-registry.test.ts src/tools/gate-timeout.test.ts (0) — Repo wrapper green: 35/35 pass.
- **[archive_only_evidence]** verification: pnpm run check (0) — schemas:check, typecheck, isolation, lockfile, lint, format:check all green. No schemas:generate needed (no public Zod changes).
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm vitest run src/tool-registry.test.ts src/tools/gate-timeout.test.ts (phase=red)
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm vitest run src/tool-registry.test.ts src/tools/gate-timeout.test.ts (phase=green)
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tool-registry.test.ts src/tools/gate-timeout.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm run check
- **[agenda]** follow_ups: adv_run_test (and Temporal-backed ADV tools generally) are timing out on 5s queries in this session; if it persists after this change ships and the worker is redeployed, investigate the deployed worker's query path under contention.
- **[unresolved_action]** required_main_agent_actions: If rq-TDD009seq requires adv_run_test-persisted evidence for task completion, re-run `bin/oc-test targeted -- src/tool-registry.test.ts -t "adv_change_archive registers safeExecute"` via adv_run_test (phase green) after the fixed worker is deployed/restarted; bash evidence is attached in verification[] otherwise.
- **[archive_only_evidence]** decisions: Extended the pre-existing archive guard test with AC2 traceability instead of adding a second, weaker test — An identical guard already exists (fixArchiveTerminalProjection SC3/AC4, commit 66f33bcf): same describe block, same extractRegistrationBlock pattern as T1's gate_complete test, asserting timeoutMs > 300_000 (strictly stronger than AC2's > 10_000) plus onToolTimeout presence. Duplicating it with a weaker assertion would be redundant slop (P19/P29); a comment + test-name suffix gives AC2 acceptance traceability without weakening the guard.
- **[archive_only_evidence]** decisions: Guard inspects source text of tool-registry.ts, not the runtime registry object — Matches the established pattern in this describe block: readFileSync(tool-registry.ts) + extractRegistrationBlock() walks parens from 'adv_change_archive: registerTool(' to capture the full safeExecute options literal, then regex-asserts timeoutMs value and onToolTimeout. Source-level inspection directly pins the registration site (the regression surface for #216) and needs no store/Temporal fixtures.
- **[archive_only_evidence]** decisions: RED demonstrated by temporarily replacing the options object with {} in tool-registry.ts, then restoring via git checkout — Simulates the exact #216 regression (override silently dropped). Guard failed at expect(block!).toMatch(/timeoutMs:\s*\d/) (exit 1), proving it catches removal. Restoration verified: only the test-file change remains in git diff.
- **[unresolved_action]** blockers: adv_run_test evidence recording blocked by Temporal query timeouts
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tool-registry.test.ts -t "adv_change_archive registers safeExecute" (with archive override temporarily removed from tool-registry.ts) (1) — RED demonstrated: guard fails at tool-registry.test.ts:553 expect(block!).toMatch(/timeoutMs:\s*\d/) when the override is dropped — proves it catches the #216 regression. Output saved at /tmp/red-out.txt. NOTE: recorded via bash because adv_run_test failed 4x with TemporalQueryTimeout.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tool-registry.test.ts (0) — GREEN: all 29 tests pass with the real source (timeoutMs: 420_000 + onToolTimeout present). Output at /tmp/green-out.txt. adv_run_test green attempt also blocked by TemporalQueryTimeout; bash used instead.
- **[archive_only_evidence]** verification: pnpm run check (plugin/) (0) — schemas:check, typecheck, test-isolation, lockfile-policy, eslint, prettier all green.
- **[unresolved_action]** consumer_warnings: verification_missing: RED/GREEN evidence could not be persisted via adv_run_test (TemporalQueryTimeout x4, persisting after adv_temporal_reconnect). Verification was performed via bash with explicit exit codes instead; see verification[]. Orchestrator may re-record via adv_run_test once the deployed worker carries this change's timeout fixes.
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tool-registry.test.ts -t "adv_change_archive registers safeExecute" (with archive override temporarily removed from tool-registry.ts)
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tool-registry.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm run check (plugin/)
- **[archive_only_evidence]** decisions: Linux /proc scan (readdir + per-pid cmdline/status reads) instead of pgrep/ps spawn — No subprocess spawn needed, no shell quoting, portable failure mode (missing dir → null), fast local reads naturally bounded by the existing 2s probe-cache timeout
- **[archive_only_evidence]** decisions: Orphan flag computed via process.kill(ppid, 0) ESRCH check on the CURRENT ppid, not ppid===1 — Matches the AC4 watchdog (worker.ts isParentAlive) semantics; ppid===1 misses subreaper adoptions. Advisory only — a reparented orphan whose new parent is alive is not detectable from outside, which the AC4 in-child watchdog already covers; documented in module header
- **[archive_only_evidence]** decisions: orphan:false when ppid is unreadable (status file missing) — Never guess orphan status — false positives would trigger spurious kill recommendations
- **[archive_only_evidence]** decisions: isProcessAlive duplicated in worker-process-probe.ts rather than imported from worker.ts — Keeps the status probe free of any worker-module dependency (worker.ts pulls in the Temporal SDK); 6-line function, documented as a deliberate mirror
- **[archive_only_evidence]** decisions: Plan flag workerProcesses=true only for the health view — Task scopes the section to the adv_status health probe; summary/changes/hygiene stay unchanged
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts (phase:red via adv_run_test runId tr_mrjwml6n_9a857523) (1) — RED recorded: module ./worker-process-probe missing + fetchStatusWorkerProcesses missing + health projection missing — 3 failed / 1 passed as expected
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts (0) — GREEN: 13/13 tests pass (enumeration, orphan ESRCH flag, null degradation, malformed entries, custom marker, default isProcessAlive, probe fetch shape + freshness, health-view projection, summary-view omission). NOTE: adv_run_test green-phase evidence could not be recorded — tool backend returned TemporalQueryTimeout on 4 attempts with backoff; verification executed via identical bash command instead.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/tools/status.test.ts src/tools/status-enrich.test.ts src/tools/status-recommendations.test.ts src/tools/probe-cache.test.ts src/tools/status.probe-helpers.test.ts (0) — Regression sweep on neighboring status/probe suites: 79/79 pass
- **[archive_only_evidence]** verification: pnpm run check (from plugin/) (0) — schemas:check + typecheck + test-isolation + lockfile-policy + eslint + prettier all clean
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts (phase:red via adv_run_test runId tr_mrjwml6n_9a857523)
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tools/status.test.ts src/tools/status-enrich.test.ts src/tools/status-recommendations.test.ts src/tools/probe-cache.test.ts src/tools/status.probe-helpers.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm run check (from plugin/)
- **[agenda]** follow_ups: For a change whose workflow has ALREADY completed (recoveryMutation), phase9_status cannot be recorded via signal; that split-brain variant is the adv_change_status_repair concern (disk-projection status flip), not the idempotent re-run. Worth confirming adv_change_status_repair also projects phase9_status done for the completed-workflow #216 variant.
- **[archive_only_evidence]** decisions: Targeted reconcileArchivedBundleRetry directly (not full adv_change_archive) — It IS the classifier-directed idempotent re-run path (returns noOp, reconciles Phase 9 metadata) and is exported/directly callable. The full adv_change_archive path requires 6-gate + contract review matrix + validation preflight that is orthogonal to the phase9-recording assertion. The identical guard defect was also fixed in the main path (change.ts:2912) via related-scan.
- **[archive_only_evidence]** decisions: Used live Temporal harness (TestWorkflowEnvironment.createTimeSkipping + createInProcessWorker + initStsl + createTemporalStoreBackend), NOT a disk mock — Validator CRITICAL constraint: every recordPhase9Status branch routes through getService()→Temporal signal. A disk-only stub would false-green/spuriously fail. Pattern mirrors e2e-tool-calls.itest.ts and concurrent-signaling.itest.ts.
- **[archive_only_evidence]** decisions: Gated phase9 recording on !releaseResult.recoveryMutation — Initial unconditional fix broke mock test 1053 (poisoned/completed-workflow recovery must not fire signals). recoveryMutation is set exactly when the release gate is disk-recovered because the workflow completed, so it correctly distinguishes live-workflow (record phase9) from completed-workflow (skip) cases.
- **[archive_only_evidence]** decisions: Seeded workflow status='draft' with 6 prior gates done, release pending — Seeding status='archived' made the workflow complete immediately (terminal_status_detected) so post-completion signals failed; release readiness (PRIOR_GATE_INCOMPLETE) requires all prior gates done. 'draft' is the only non-terminal workflow seed status.
- **[archive_only_evidence]** decisions: Store projectId set to getProjectId(legacy.paths.root); tempDir made a real git repo with change/{id} merged into main — Test-mode getProjectId returns synthesizeTestProjectId(root) only for a real git repo, and the store's projectId must equal it so buildChangeWorkflowId(projectId, changeId) targets the same workflow the re-run signals. no-remote route + merged branch yields a shipped (reachable) finalization for verifyReleaseEvidenceFromMain.
- **[archive_only_evidence]** verification: adv_run_test phase:red — bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts (1) — RED recorded (runId tr_mrk0grli_a0c42f9c). Failed cleanly at expect(phase9_status?.status).toBe('done') → received undefined, against LIVE Temporal. Release-gate assertion passed, proving the re-run reached the phase9 step.
- **[archive_only_evidence]** verification: adv_run_test phase:green — bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts (0) — GREEN recorded (runId tr_mrk0k5x9_e108d8b1). phase9_status durably recorded as done via live phase9StatusUpdatedSignal after the idempotent re-run.
- **[archive_only_evidence]** verification: bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts src/tools/change.archive-phase9.test.ts src/tools/change/archive-gate.test.ts (0) — Final state: 3 files, 42/42 tests pass. Includes regression check that mock test 1053 (poisoned/completed-workflow recovery) still passes with the recoveryMutation gate.
- **[archive_only_evidence]** verification: pnpm run check (from plugin/) (0) — schemas:check, typecheck (tsc --noEmit), test-isolation, lockfile-policy, eslint, prettier all green.
- **[archive_only_evidence]** consumer_warnings: consumer_failure: During final verification, the ADV Temporal store became intermittently unresponsive: adv_run_test(phase:verify), adv_temporal_diagnose, and adv_status all timed out (TemporalQueryTimeout / ToolExecutionTimeout). This is ADV-tooling infra, NOT the test/fix — the earlier phase:red (tr_mrk0grli_a0c42f9c) and phase:green (tr_mrk0k5x9_e108d8b1) evidence was recorded before degradation, and bash confirms 42/42 tests + pnpm run check green. If the store stays wedged, an OpenCode/plugin-host restart may be needed.
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: adv_run_test phase:red — bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: adv_run_test phase:green — bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts src/tools/change.archive-phase9.test.ts src/tools/change/archive-gate.test.ts
- **[unresolved_action]** consumer_warnings: verification_missing: No adv_run_test evidence found for reported command: pnpm run check (from plugin/)
- **[agenda]** follow_ups: Re-verify whether the CURRENTLY LOADED live-session dist differs from ~/.local/share/Advance/plugin/dist (which already has the override); if the running session predates the override, AC2 activation may still apply for THAT session only.
- **[agenda]** follow_ups: Investigate the real #217 orphan surface: Temporal SERVER spawn (runtime-manager.ts:410 detached:true) and plugin hot-reload teardown of createMultiWorker's child, not the dead spawnTemporalWorkerProcess.
- **[agenda]** follow_ups: If AC4 keeps the drop-detached path, also delete the dead spawnTemporalWorkerProcess function per P19/YAGNI rather than editing dead code.
- **[archive_only_evidence]** sources: safe-execute.ts raceWithTimeout + SafeExecuteOptions: DEFAULT_TOOL_TIMEOUT_MS=10_000. raceWithTimeout uses Promise.race([promise, timeoutPromise]); on timeout it REJECTS but does NOT cancel/abort the original execute promise. Docstring L262 + comment L494-495: the uncancellable execute promise keeps running in the background either way. onToolTimeout(args,error) runs in the CATCH/timeout path and its returned string REPLACES the agent-visible response; the background promise's own return value/finally CANNOT reach the agent.
- **[archive_only_evidence]** sources: tool-registry.ts archive override (trunk source): adv_change_archive already wired with timeoutMs:420_000 + onToolTimeout:formatArchiveTimeoutResult. Confirms AC2's 'existing override' exists on trunk.
- **[archive_only_evidence]** sources: DEPLOYED build already has archive override (AC2 premise falsified): Deployed build contains `timeoutMs: 42e4` (420000) + `onToolTimeout: formatArchiveTimeoutResult`. formatArchiveTimeoutResult defined at dist line 33634. The deployed build is NOT stale for the archive override — AC2's stated premise ('deploy activates existing override; deployed build lacks it') is FALSE.
- **[archive_only_evidence]** sources: formatArchiveTimeoutResult classifier (the correct AC3-style mechanism): Disk-only, worker-query-free classifier. Probes candidate archive dirs via findArchiveBundle; if bundle durable returns typed still_finalizing/retrySafe result; else returns undefined (declines) so genuine failures surface. This is the pattern AC3 should follow for phase9 — record/report from the timeout path, not from a background finally.
- **[archive_only_evidence]** sources: adv_gate_complete registration (no override) + handler durable-write ordering: gate_complete uses bare safeExecute (10s default, no override). Handler does read-only Temporal queries (querySignal tasks, release blocker checks) BEFORE the gateCompletedSignal; the ONLY durable mutation is fireSignalAndRefresh(gateCompletedSignal). No pre-signal disk write, no artifact hashing in the hot path.
- **[archive_only_evidence]** sources: phase9 sync path is fully awaited try/catch — NO background dispatch (AC3 premise falsified): Comment L2738-2740: 'Sync mode ... there is no detached async dispatch, so the call returns a terminal outcome.' finalizeRelease is awaited inside try/catch; a THROWN finalization is caught and recordPhase9Status(status:failed) is written durably, then a terminal result returns. There is no background execute promise whose finally could fire post-timeout.
- **[archive_only_evidence]** sources: spawnTemporalWorkerProcess (only detached:true worker spawn) has ZERO live callers: grep across src: spawnTemporalWorkerProcess is referenced only by its own definition. detached:true here. Not wired into plugin-init.ts or any live worker path.
- **[archive_only_evidence]** sources: Live worker paths use detached:false / in-process (AC4 evidence): plugin-init spawns createInProcessWorker OR createOutOfProcessWorker→createMultiWorker. createMultiWorker's spawnChild uses detached:false with IPC stdio pipes + restart/backoff supervision + SIGKILL orphan reaping. in-process-worker docstring: the earlier spawn+detach model was REPLACED by the in-process worker. The runtime-manager Temporal SERVER spawn (L410) is detached:true, but that is the server, not the worker.
- **[archive_only_evidence]** architecture_assessment: The recon premise that motivated three of five ACs is partially falsified by source evidence. (1) AC2: the deployed dist build ALREADY contains the archive 420s override + formatArchiveTimeoutResult classifier (chunk-OGQQUMUY.js:39732); the override is not stale, so AC2 collapses to a build/deploy no-op plus a source-presence guard test — there is no activation work. (2) AC3's chosen mechanism is unsound: safeExecute does not run a handler-level try/finally that can deliver results after the outer timeout wins the Promise.race. The execute promise is uncancellable and keeps running, but the tool call has ALREADY returned the timeout (or onToolTimeout classifier) response — any finally that fires later in the orphaned background promise writes into a dead call frame and cannot reach the agent, and is racing an already-torn-down/hung worker. The DURABLE, agent-visible mechanism is the onToolTimeout classifier itself (proven by formatArchiveTimeoutResult), which runs synchronously in the timeout catch path and whose return value replaces the response. Moreover the current phase9 sync path (change.ts:2737-2792) is already a fully-awaited try/catch that records phase9_status=failed on throw — there is explicitly 'no detached async dispatch' (comment L2738). So AC3 as written both (a) mis-describes safeExecute cancellation semantics and (b) mis-describes the existing phase9 flow as background. (3) AC4: detached:true is NOT load-bearing for the live worker — the only detached worker spawn (spawnTemporalWorkerProcess) is dead code with zero callers; the live path (createMultiWorker) already uses detached:false with supervised restart + orphan SIGKILL. The 'drop detached' preferred approach is therefore either a no-op (dead code) or must target the correct live surface. (4) AC1 is sound: gate_complete does only read-only queries before the single durable signal; a 30s budget + advisory classifier following the archive pattern is reasonable, and a 'may-have-landed' classifier is HONEST because the sole durable write is the signal.
- **[unresolved_action]** validation.blockers: AC3 mechanism unsound: safeExecute's outer raceWithTimeout does NOT keep a handler-level try/finally reachable for agent-visible results after timeout. The execute promise is uncancellable but its post-timeout finally cannot deliver output to the already-returned tool call (safe-execute.ts:292-306,447-509). AC3 must record/report phase9_status from the onToolTimeout classifier (timeout path), mirroring formatArchiveTimeoutResult, NOT from a 'background execute promise continuing'. Additionally the current phase9 sync path (change.ts:2737-2792) is already awaited try/catch with durable failed-status recording and explicitly has 'no detached async dispatch' — AC3's premise misdescribes the existing code.
- **[unresolved_action]** validation.blockers: AC2 premise falsified: the deployed dist build ALREADY contains timeoutMs:42e4 + formatArchiveTimeoutResult (chunk-OGQQUMUY.js:39732). The archive override is not stale. AC2 as scoped (deploy to activate a missing override) has no activation work; re-scope to a source-presence regression guard only, or re-verify against the actual live session's loaded artifact if a genuinely older build is in play.
- **[agenda]** follow_ups: AC2: consider a deploy-freshness check as a follow-up — a source-presence guard cannot catch a future stale-deploy recurrence, which was the actual historical failure mode.
- **[agenda]** follow_ups: AC4: verify parent-liveness detection is robust under systemd user-level subreapers (ppid may not be exactly 1 on reparent); prefer process.kill(ppid,0) over ppid===1 alone.
- **[archive_only_evidence]** sources: Deployed dist (archive override present): timeoutMs: 42e4 (=420000ms) + onToolTimeout: formatArchiveTimeoutResult already minified into the deployed bundle. formatArchiveTimeoutResult defined L33634. Confirms AC2 first-pass finding: archive override is already deployed; no deploy work needed.
- **[archive_only_evidence]** sources: recordPhase9Status is Temporal-only: recordPhase9Status calls getService(); if bundle is null it throws 'Temporal service not available for phase9 status update', then fires phase9StatusUpdatedSignal via fireSignalAndRefresh. Confirms the disk-only timeout classifier CANNOT record phase9_status. AC3 re-scope is factually justified.
- **[archive_only_evidence]** sources: formatArchiveTimeoutResult is disk-only by design: Docstring: 'Probe discipline: disk-only. After a timeout the Temporal worker may be the thing that hung; the classifier must not issue workflow queries.' Implementation only calls findArchiveBundle over candidate dirs; no recordPhase9Status. Confirms record-during-timeout is architecturally forbidden, validating the AC3 re-scope away from it.
- **[archive_only_evidence]** sources: Archive re-run records phase9_status (all branches): Sync finalization path: thrown finalizeRelease -> recordPhase9Status failed (L2765); pending_merge -> recordPhase9Status (L2812); success -> recordPhase9Status done when prior status != done (L2916). All go through the Temporal-only recordPhase9Status. AC3 'test the re-run records/reconciles phase9_status' is satisfiable, but ONLY with a live Temporal service in the test harness — the re-run reconcile bails (throws) if Temporal is down.
- **[archive_only_evidence]** sources: spawnTemporalWorkerProcess is dead code: Only the export definition exists; zero call sites. Confirms AC4 first-pass finding that targeting it was dead-code work. Live spawn path is worker-multi.ts spawnChild.
- **[archive_only_evidence]** sources: Live worker spawn uses detached:false: spawn(nodePath, args, { stdio:['pipe','pipe','pipe'], detached:false }). Clean shutdown reaps via SIGTERM (L511) then SIGKILL escalation after MULTI_SHUTDOWN_GRACE_MS (L527). detached:false on Linux does NOT kill the child when the parent CRASHES (no SIGTERM sent) — child is reparented to pid 1. Confirms crash-orphaning gap AC4 targets.
- **[archive_only_evidence]** sources: Worker child entry point (watchdog insertion site): runTemporalWorkerFromEnv reads env, resolves queues, then blocks in worker.run(). Executable entry guard at L340-345 (process.argv[1] === currentFile). Clean single insertion point for a ppid-liveness watchdog: start an unref'd poll before worker.run(), self-exit(0) when parent dies. No IPC/reload survival contract exists — reloads go through explicit restart, so self-exit on parent death is safe.
- **[archive_only_evidence]** sources: Existing heartbeat-lock primitive (adjacent, do not conflate): startWorkerLockHeartbeat renews a lock every 10s with a 90s SERVICEABILITY_GRACE_MS. This is a lock-liveness mechanism, NOT a parent-liveness watchdog. The AC4 watchdog should be a distinct, faster ppid check; reusing the 90s grace would be too slow to relieve contention.
- **[archive_only_evidence]** architecture_assessment: All three first-pass FAIL corrections hold under source re-verification. (A) AC2/AC3 premises confirmed: archive 420s override + classifier already in deployed dist (AC2 correctly re-scopes to a source-presence regression guard, no deploy work); recordPhase9Status is Temporal-only and the timeout classifier is disk-only by explicit design (AC3 correctly abandons record-during-timeout, re-scopes to the re-run reconciliation path). (C) Archive re-run DOES record phase9_status across failed/pending_merge/done branches via recordPhase9Status, so the AC3 test is satisfiable — caveat: recordPhase9Status throws when Temporal is unavailable, so the AC3 reconcile test must run against a live Temporal/STSL service, not a disk-only stub. (B) AC4 watchdog is sound: spawnTemporalWorkerProcess is dead code, live spawn is detached:false (no reap on parent crash), worker.ts runTemporalWorkerFromEnv is a clean insertion point for a ppid-poll self-exit. Caution: worker-heartbeat.ts already exists with a 90s grace — the watchdog must be a separate, faster ppid check (prefer process.kill(ppid,0)), not a reuse of that grace.
- **[wisdom_candidate]** wisdom_candidates: [pattern] Bounded health probes must propagate their AbortSignal into per-entry I/O; an outer timeout alone does not prevent unbounded Promise.all work from continuing after the caller returns.
- **[archive_only_evidence]** changes_made: plugin/src/temporal/worker.ts: Stopped the parent-liveness watchdog in runTemporalWorkerFromEnv's finally path, preventing an unref'd watchdog from surviving clean worker completion or invalid input in in-process callers.
- **[archive_only_evidence]** changes_made: plugin/src/temporal/worker.test.ts: Added regression coverage that a clean runTemporalWorkerFromEnv return clears the watchdog before its first 3s probe.
- **[archive_only_evidence]** changes_made: plugin/src/utils/worker-process-probe.ts: Bounded /proc scanning to serial abort-aware reads, threading the health probe AbortSignal into readFile so a timed-out census does not launch or retain unbounded concurrent reads.
- **[archive_only_evidence]** changes_made: plugin/src/utils/worker-process-probe.test.ts: Added deterministic aborted-signal regression coverage for the worker-process census.
- **[archive_only_evidence]** changes_made: plugin/src/tools/status-health.ts: Passed the probe-cache timeout AbortSignal into the worker-process census.
- **[archive_only_evidence]** verification: tests_run=bin/oc-test targeted -- src/temporal/worker.test.ts src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts src/tools/gate-timeout.test.ts src/tool-registry.test.ts src/temporal/__tests__/archive-phase9-splitbrain.itest.ts, pnpm run check results=pass — Targeted suite: 6 files, 71 tests passed. Includes live Temporal split-brain reconciliation integration test. pnpm run check passed schemas, typecheck, isolation/lockfile checks, ESLint, and Prettier. git diff --check passed.
- **[unresolved_action]** required_main_agent_actions: Deploy the current branch plugin bundle using the normal release/deployment path, then restart OpenCode sessions or otherwise bounce the six existing Temporal worker processes shown by deploy-local so they load dist/temporal/worker.js.
- **[unresolved_action]** required_main_agent_actions: After bounce, rebuild current branch and compare both deployed dist/temporal/worker.js and workflows.js against branch output; require exact parity before marking release ready.

## Contract / AC Coverage

| ID | Kind | Status |
| --- | --- | --- |
| AC1 | acceptance_criterion | pass |
| AC2 | acceptance_criterion | pass |
| AC3 | acceptance_criterion | pass |
| AC4 | acceptance_criterion | pass |
| AC5 | acceptance_criterion | pass |
| C1 | constraint | respected |
| C2 | constraint | respected |
| C3 | constraint | respected |
| C4 | constraint | respected |

## Unresolved Actions

- verification_missing: No adv_run_test evidence found for reported command: pnpm exec vitest run src/temporal/worker.test.ts
- verification_missing: No adv_run_test evidence found for reported command: pnpm exec vitest run src/temporal/worker.test.ts
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/temporal/worker.test.ts src/temporal/runtime-manager.test.ts src/temporal/worker-multi.test.ts src/temporal/out-of-process-worker.test.ts
- verification_missing: No adv_run_test evidence found for reported command: pnpm run check
- verification_missing: No adv_run_test evidence found for reported command: pnpm vitest run src/tool-registry.test.ts src/tools/gate-timeout.test.ts (phase=red)
- verification_missing: No adv_run_test evidence found for reported command: pnpm vitest run src/tool-registry.test.ts src/tools/gate-timeout.test.ts (phase=green)
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tool-registry.test.ts src/tools/gate-timeout.test.ts
- verification_missing: No adv_run_test evidence found for reported command: pnpm run check
- If rq-TDD009seq requires adv_run_test-persisted evidence for task completion, re-run `bin/oc-test targeted -- src/tool-registry.test.ts -t "adv_change_archive registers safeExecute"` via adv_run_test (phase green) after the fixed worker is deployed/restarted; bash evidence is attached in verification[] otherwise.
- adv_run_test evidence recording blocked by Temporal query timeouts
- verification_missing: RED/GREEN evidence could not be persisted via adv_run_test (TemporalQueryTimeout x4, persisting after adv_temporal_reconnect). Verification was performed via bash with explicit exit codes instead; see verification[]. Orchestrator may re-record via adv_run_test once the deployed worker carries this change's timeout fixes.
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tool-registry.test.ts -t "adv_change_archive registers safeExecute" (with archive override temporarily removed from tool-registry.ts)
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tool-registry.test.ts
- verification_missing: No adv_run_test evidence found for reported command: pnpm run check (plugin/)
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts (phase:red via adv_run_test runId tr_mrjwml6n_9a857523)
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/utils/worker-process-probe.test.ts src/tools/status-health.worker-processes.test.ts
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/tools/status.test.ts src/tools/status-enrich.test.ts src/tools/status-recommendations.test.ts src/tools/probe-cache.test.ts src/tools/status.probe-helpers.test.ts
- verification_missing: No adv_run_test evidence found for reported command: pnpm run check (from plugin/)
- verification_missing: No adv_run_test evidence found for reported command: adv_run_test phase:red — bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts
- verification_missing: No adv_run_test evidence found for reported command: adv_run_test phase:green — bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts
- verification_missing: No adv_run_test evidence found for reported command: bin/oc-test targeted -- src/temporal/__tests__/archive-phase9-splitbrain.itest.ts src/tools/change.archive-phase9.test.ts src/tools/change/archive-gate.test.ts
- verification_missing: No adv_run_test evidence found for reported command: pnpm run check (from plugin/)
- AC3 mechanism unsound: safeExecute's outer raceWithTimeout does NOT keep a handler-level try/finally reachable for agent-visible results after timeout. The execute promise is uncancellable but its post-timeout finally cannot deliver output to the already-returned tool call (safe-execute.ts:292-306,447-509). AC3 must record/report phase9_status from the onToolTimeout classifier (timeout path), mirroring formatArchiveTimeoutResult, NOT from a 'background execute promise continuing'. Additionally the current phase9 sync path (change.ts:2737-2792) is already awaited try/catch with durable failed-status recording and explicitly has 'no detached async dispatch' — AC3's premise misdescribes the existing code.
- AC2 premise falsified: the deployed dist build ALREADY contains timeoutMs:42e4 + formatArchiveTimeoutResult (chunk-OGQQUMUY.js:39732). The archive override is not stale. AC2 as scoped (deploy to activate a missing override) has no activation work; re-scope to a source-presence regression guard only, or re-verify against the actual live session's loaded artifact if a genuinely older build is in play.
- Deploy the current branch plugin bundle using the normal release/deployment path, then restart OpenCode sessions or otherwise bounce the six existing Temporal worker processes shown by deploy-local so they load dist/temporal/worker.js.
- After bounce, rebuild current branch and compare both deployed dist/temporal/worker.js and workflows.js against branch output; require exact parity before marking release ready.
